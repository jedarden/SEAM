# This file is a POINTER, not a manifest. It used to hold a stale early
# snapshot of the seam-ci WorkflowTemplate (single-pod monolith verify,
# github.com clone) that ArgoCD never applied — editing it changed no CI
# behavior, which is the exact mistake class behind the 2026-09 red-gate
# incidents. Removed 2026-09-25 (bead seam-5515cac0).
#
# The live seam-ci WorkflowTemplate is GitOps-managed from the authoritative
# repository:
#
#   jedarden/declarative-config
#   k8s/iad-ci/argo-workflows/seam-ci-workflowtemplate.yml
#
# Edit CI behavior THERE (commit to that repo's main; ArgoCD app
# argo-workflows-ns-iad-ci syncs it). See AGENTS.md "Where things live".
