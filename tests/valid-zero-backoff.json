{
  "$comment": "VALID example - x-loop-guard.backoff_ms: 0 should be ACCEPTED",
  "description": "This should PASS validation - backoff_ms of 0 is the minimum allowed value",
  "x-seam-schema": "v1",
  "x-seam-owner": "test-service",
  "x-api-version": "v1",
  "x-instance-param": "region",
  "x-upstream-map": {
    "us-east": {
      "url": "https://api.example.com",
      "vaultPath": "seam/routes/test-service/token",
      "injectAs": { "kind": "bearer" }
    }
  },
  "paths": {
    "/test": {
      "get": {
        "summary": "Test with zero backoff_ms (minimum valid value)",
        "x-loop-guard": {
          "max_iterations": 10,
          "backoff_ms": 0
        },
        "x-cost-per-call": 0.01,
        "x-quota": {
          "limit": 100,
          "window_seconds": 60
        },
        "responses": {
          "200": { "description": "OK" }
        }
      }
    }
  }
}
