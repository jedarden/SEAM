# SEAM Plan

## Overview

SEAM (Self-documenting Endpoint Access Mediator) is a single unified endpoint that acts as a proxy to multiple authenticated services. It injects the required secret into a request received from an agent, sends the request on to the destination service, and passes the response back — the agent never sees the secret. It also self-documents: a merged OpenAPI spec drives `/docs` and `/openapi.json`, and malformed requests get a structured error naming the bad field and pointing at the right shape, instead of a bare 400.

## Motivation

Today, each backend service (ArgoCD read-only proxy, per-cluster kubectl-proxy, the z.ai/GLM proxy, the twitterapi.io proxy) is documented by hand in CLAUDE.md with its own discovery and auth pattern. This doesn't scale: every new service needs new hand-written prose, agents have no standard way to discover what's available, and there's no mechanism to guide an agent that sends a malformed request back toward the correct one.

## Architecture

- Single gateway process (reverse proxy) in front of N upstream services.
- Route table = one OpenAPI spec, assembled at startup (and reloaded on change) by merging multiple fragment files. Each fragment is an OpenAPI paths/components fragment plus custom extension fields — `x-upstream` (the base URL to forward to), `x-vault-path` (which OpenBao secret to fetch) and `x-inject-as` (how to inject it — an object naming both the kind, header / bearer / query, and the header or parameter *name*; see Data Models) — before proxying upstream.
- Route fragments are delivered as Kubernetes ConfigMaps, one per upstream service, mounted into the gateway pod via a `projected` volume at `/etc/gateway/routes.d/*.yaml`. Each fragment is owned and synced independently from its own service's path in declarative-config — no single monolithic file to edit for every new route. **That independence is over routes, not over onboarding** (clarified 2026-07-20): adding, editing, versioning or retiring a route inside an already-onboarded service touches that service's own directory and nothing else, which is the property this model exists for and the change that actually recurs. *Onboarding a new service* additionally adds one entry to SEAM's own projected-volume source list — see the owner-binding rule below, which states that cost rather than leaving this bullet and the reload bullet asserting there is none.

  **Where fragments live in declarative-config, exactly** (decided 2026-07-20 — the owner chain that carries the whole anti-exfiltration control starts at "the declarative-config directory name", and this plan named that directory only as "its own service's path" and "`<svc>/`" — neither of which is a location. The repo's layout is `k8s/<cluster>/...`, so `<svc>/` was ambiguous between a per-cluster path, a top-level `seam/` path, and the service's own ArgoCD application directory — and `seam lint`, whose distinguishing value is that it is the only place the whole chain is visible at once, cannot walk a chain whose first link has no address). The location is fixed: **`k8s/rs-manager/seam/routes/<svc>/<name>.yaml`** — one directory per service, and **N fragment files in it, not one** (corrected 2026-07-20 — this was first filed as "exactly one `fragment.yaml`", which silently foreclosed the entire version-coexistence model. Version Migration Strategy §2 ships a breaking change as a *new* fragment mounted **alongside** the old one for as long as anything still calls it, and the Fragment Toolchain collision key (path, method, `x-api-version`) exists precisely so that two fragments of one service differing only in version are a legal coexistence pair rather than a collision. One file per service made that pair unfileable, ungeneratable as a ConfigMap, and unprojectable — both would land at the same basename and one would silently overwrite the other — so the only case the version-keyed collision rule exists for was the one case it could never reach, and Phase 8, plus the `_unversioned`-alongside-`v1` coexistence this plan calls the mechanism's very first use, were unbuildable as filed). Each file in the directory becomes **one key in the single ConfigMap `seam-routes-<svc>`**, and that ConfigMap's N keys are projected into a **subdirectory named for the owner**: `items: [{key: <name>.yaml, path: <svc>/<name>.yaml}]`, landing at `/etc/gateway/routes.d/<svc>/<name>.yaml`. **The gateway derives `<svc>` from the parent directory of each file it loads, never from the basename** — so `<name>` is the author's to choose (`fragment.yaml`, `v1.yaml`, `pods-v2.yaml`), carries no meaning and constrains nothing, while the owner segment remains the one part of the path the author cannot write. Every forgery-resistance property is preserved unchanged, because the property never lived in the *file* name: the `path:` rename still lives in SEAM's own Deployment, so a fragment author editing only `routes/<svc>/` still cannot make their content land under another owner's directory, and still cannot smuggle a key into another service's slot. And **SEAM's own Deployment, Service, `seam-upstream-allowlist` and `seam-upstream-ca` ConfigMaps live one level up at `k8s/rs-manager/seam/`**, outside every `routes/<svc>/` directory. That separation is the point rather than a filing convenience: the projected `path: <svc>/<name>.yaml` rename and the upstream-host allowlist are the two operator-owned links a fragment author must not be able to edit, so they sit in a directory no fragment author has business touching, while `routes/<svc>/` is the only place that author writes. `k8s/rs-manager/` because SEAM runs on rs-manager from Phase 6a (Hosting); a fragment describing an upstream *in* another cluster still files here, since the directory names the pod that mounts it, never the cluster it talks to.

  **`<svc>` is SEAM's own owner token and need not equal the service's ArgoCD app directory.** The upstream service typically has its own application directory elsewhere in declarative-config (`k8s/<cluster>/<app>/`), which is where that service is *deployed*; `k8s/rs-manager/seam/routes/<svc>/` is where its SEAM route contract is *published*, and the two are separate PRs against separate paths with separate reviewers. Where the names can match they should, purely for legibility, but nothing depends on it: `<svc>` is bound by the projected **parent directory** and `x-seam-owner` and constrains only the `seam/routes/<svc>/*` OpenBao prefix, so co-ownership is well-defined even for an upstream that has no declarative-config presence at all (twitterapi.io, z.ai). `<svc>` matches `^[a-z0-9]([a-z0-9-]*[a-z0-9])?$` — one flat segment, no slashes, no dots — so it is simultaneously a legal directory name, a legal ConfigMap name suffix, a legal filename stem, and a legal OpenBao path segment, and the chain cannot be broken by a name that is valid at one link and not the next.
- Route reloads are **in-process and sub-second** (decided 2026-07-16, superseding the earlier restart-via-`reloader` idea): the gateway file-watches `/etc/gateway/routes.d/`, re-merges and re-validates on change, and atomically swaps the route table — in-flight requests finish against the old table, new requests see the new one, zero dropped connections. With dozens of concurrent workers hitting this authentication layer, restart-based reload would be the disruptive option; no blue/green machinery or Traefik-side intelligent routing is needed for config changes, because nothing about the pod changes. Kubelet propagates projected-ConfigMap updates on its sync period (git→pod latency up to ~1 min after ArgoCD sync); if that latency ever matters, the upgrade path is watching ConfigMaps via the Kubernetes API (informer) instead of file mounts. Gateway **binary** deploys are the narrow exception, and they are *not* zero-downtime (corrected 2026-07-20 — this clause previously called SEAM "a stateless proxy that already provides blue/green's guarantee without running a second fleet", pre-amendment text the single-replica decision invalidated). SEAM is emphatically **not** stateless: guard counters, quota ledgers, last-2xx, and breaker state are per-process and load-bearing enough to forbid a second replica, so Version Migration Strategy §1 mandates `replicas: 1` with `maxSurge: 0` — a rollout never runs two SEAM pods at once and therefore has no overlap at all. A binary deploy therefore opens a **real gap, on the order of seconds to tens of seconds** — the sum of `terminationGracePeriodSeconds`, in-flight drain, pod scheduling, image pull, process start, initial merge and OpenBao login, and the first passing readiness probe, **typically 5-30 seconds** (corrected 2026-07-20; this clause previously said "sub-second", which was wrong by one to two orders of magnitude). **Version Migration Strategy §1 owns that sizing and the caller requirement it implies** — any agent calling SEAM must retry with backoff over at least 60 seconds. The gap is nonetheless cheap precisely because it is rare: **route and contract changes are ConfigMap edits that never restart the pod**, so the hot-reload path above — not a deploy strategy — is what carries the traffic that actually changes.

  **One honest exception: *onboarding* a service is not a ConfigMap edit** (stated 2026-07-20 — this bullet and the fragment-delivery bullet above both read as though every route change were restart-free, while the projected volume's explicit per-source `items:` puts the onboarding half in SEAM's *pod template*). The distinction is exactly: **within** an already-onboarded service — every fragment added, edited, deprecated, versioned or retired under an existing `routes/<svc>/` — is a pure ConfigMap edit, hot-reloaded, no restart, and that remains the overwhelming majority of changes and all of the ones an agent notices. **Onboarding a new service** adds a source to the projected volume, which is a pod-template change, which is a rollout under `maxSurge: 0` — the same **typically 5-30 second** real gap sized above, not a hot reload. It is a once-per-service event and it is already a PR against SEAM's own manifest for the upstream-host allowlist regardless, so the marginal cost is the gap and not the process; but **Phases 3-5 each onboard services, so each of those is a rollout**, and the 60-second caller-retry requirement §1 imposes covers them for the same reason it covers a binary deploy.
- `/docs` serves a human-readable rendering of the merged spec (Redoc/Swagger UI); `/openapi.json` serves the raw merged spec for agents to fetch programmatically.
- Incoming requests are validated against the merged spec. A malformed request gets a structured 400 naming the bad field, what was expected, and a link to `/docs/{route}`.
- **`/docs/{route}` is a real server-side endpoint, not a `/docs` anchor** (decided 2026-07-20; Phase 1a deliverable). The pointer carried by every structured 400 is the affordance the Overview names as SEAM's differentiator, and its consumer is an agent, not a browser — so it must resolve to something fetchable in one request. A Redoc/Swagger UI section anchor (`/docs#/paths/...`) cannot serve that role: URL fragments are client-side only, never sent to the server, and no server-side path template can produce one. SEAM therefore serves **`GET /docs/route?path=<openapi-path-template>`**, with optional `&method=` and `&version=` selectors — e.g. `GET /docs/route?path=/k8s/{cluster}/api/v1/pods&method=GET&version=v2` — defaulting to every method declared on that path. **The route selector is a query parameter, not a path segment** (corrected 2026-07-20). It was previously specified as `GET /docs/{route}` with `{route}` percent-encoded into a single segment (`/docs/%2Fk8s%2F%7Bcluster%7D%2Fapi%2Fv1%2Fpods`), which cannot survive the stack this plan actually chose: Go's `net/http` mux — and most routers, and Traefik, which fronts several fleet services and is named below — normalize and clean request paths before matching, decoding `%2F` to `/`, so that URL arrives as the different, multi-segment path `/docs/k8s/{cluster}/api/v1/pods` and never matches. Encoded slashes in a path segment are a well-known failure mode there, and this endpoint is a Phase 1a deliverable and the target of every structured 400, every loop-breaker 429, and every `docsUrl` in `/changes` — so a normalization failure would break the plan's stated differentiator on day one. A query parameter is normalization-safe: nothing in the chain rewrites a query value, `/` and `{}` need no special treatment there, and the endpoint's own path is a fixed two-segment literal every router matches trivially. Elsewhere in this plan and in the sibling notes this endpoint is still referred to by the shorthand **`/docs/{route}`**; that name denotes *this* endpoint, and this bullet is the authority on its wire shape. The response is that route's slice of the merged spec and nothing else: parameters, request and response schemas, `x-required-scope`, any guard annotations in force, and one concrete worked example request. It is `application/json` by default (the agent path) and `text/html` under content negotiation, where it redirects to the corresponding `/docs` anchor for a human. 
  **Which version it shows** (decided 2026-07-20; resolves the disagreement with Version Migration Strategy §2, where `/docs` advertises the *newest* version as canonical while unversioned requests are *served* the oldest). Every SEAM-generated pointer at this endpoint carries an explicit `&version=` equal to the version that actually handled the request — the same value echoed in that response's `X-SEAM-API-Version`. So a structured 400's pointer, and the loop-breaker 429's, always render the contract in force **for that caller**, which is the whole reason the pointer exists: a pointer that showed a different contract from the one rejecting the request would manufacture the confusion it was added to prevent. A **bare** `/docs/route?path=<p>` with no `&version=` resolves by §2 — the oldest still-served version, i.e. what this caller would get if it sent no `X-SEAM-API-Version` — and says so prominently in the response rather than leaving it inferred: the payload carries `version`, `isDefaultForUnversionedCallers: true`, `canonicalVersion` (the newest), `deprecated`, and a `versions[]` list of every coexisting version with its own `docsUrl`. `/docs` and `/openapi.json` keep advertising newest-as-canonical unchanged; the two surfaces no longer disagree because this one now states which of the two it is showing.

  **When each half of that contract lands** (decided 2026-07-20; the split is built in Phase 1a and completed in Phase 8). Before Phase 8 there is exactly one served contract, keyed `_unversioned`: `/docs/route` takes `?path=`/`?method=`, emits `version: _unversioned` and `isDefaultForUnversionedCallers: true`, and omits `canonicalVersion`, `deprecated` and `versions[]`, which are Phase 8 additions. Pointers carry `&version=_unversioned` from Phase 1a — there is no bare-pointer form in any phase.

  **Scope filtering discloses nothing a caller cannot already see** (decided 2026-07-20). It is scope-filtered exactly like `/openapi.json` (Per-Agent Tool Scoping point 5), and the filter is applied *before* the existence check: a route outside the caller's filtered spec returns **404, identically to a route that does not exist**. The earlier "404 for unknown, 403 naming the missing scope for a route you may not invoke" was an enumeration oracle — a caller could probe path templates one request at a time and reconstruct the withheld route inventory of every service SEAM fronts, which is exactly the reconnaissance that filtering `/openapi.json` and gating `/scopes?all=1` exist to withhold. The same rule binds level-2 `/changes?since=&route=` (see Version Migration Strategy §3, whose 403-for-out-of-grant-route text this supersedes). The scope-naming 403 is **not** abolished, only narrowed to where it leaks nothing: it applies when the caller can already see the route in its own filtered spec but lacks the scope for the specific method it is attempting — the visible-but-not-invocable case, which is real (a caller holding `k8s-ro:get` sees the path and is refused `DELETE`). That preserves the plan's "authorization debugging becomes ask the gateway" value for every route an agent legitimately knows about. For a route it does *not* yet know about, the sanctioned path is a scope, not an oracle: `/scopes?all=1` behind `seam:scopes:read-all`, plus `/whoami`. The 404 body therefore names `/whoami` and the caller's `X-SEAM-Scope-Version` and states that an unreachable-but-believed-real route is an operator request; SEAM logs the resolved route server-side on every such 404 so the question is answerable off the request path.

  Because the structured 400 (Architecture), the loop-breaker 429 (Per-Route Guards), and level 1 of `/changes` all point at it, it ships in **Phase 1a** alongside `/docs` and `/openapi.json`, not in a later phase. Note that a level-1 `/changes` entry carries **two distinct links, not one** (decided 2026-07-20 — Version Migration Strategy §3 and `docs/notes/versioning.md` §C call the level-1 field a "drill-down link" and define level 2 as the field-level diff, while this bullet previously claimed "the level-2 drill-down" pointed here; they answer different questions and both are wanted): **`diffUrl`** → `/changes?since=<version>&route=<path>`, the level-2 field-level schema diff — *what changed*; and **`docsUrl`** → `/docs/route?path=<path>&version=<v>`, this endpoint — *what the contract now is*. It is `docsUrl` that points here. That is the canonical `docsUrl` shape everywhere it appears in this plan and in `docs/notes/versioning.md` §C; the older `/docs/{route}?version=<v>` spelling is superseded by the query-parameter correction above.
- Secret injection reads from OpenBao (the central secrets store — the 4th instance runs on rs-manager) at request time, and never surfaces secret values to the calling agent.
- **OpenBao authentication and secret-path allowlist** (decided 2026-07-20; Phase 2 deliverable). SEAM authenticates to OpenBao via **Kubernetes auth** — the pod's projected ServiceAccount token is exchanged for a short-lived OpenBao token, renewed in-process; no static token, AppRole secret-id, or credential file on disk (AppRole is the fallback only for a future non-Kubernetes deployment). Critically, because fragments are authored largely by agents, **the fragment must not be able to name an arbitrary secret**: SEAM's OpenBao policy grants `read` on a single bounded prefix, `seam/routes/*`, and nothing else — so even a hostile fragment cannot reach `armor/`, `kalshi/`, cluster kubeconfigs, or any other tenant's material, because SEAM's own token has no capability there. The allowlist is enforced twice: `seam lint` rejects any `x-vault-path` outside the prefix at CI time, and the gateway re-checks it at merge time and quarantines the fragment (never trusting lint to have run). Additionally, a fragment's `x-vault-path` must be **co-owned with its upstream target**: a fragment shipped from declarative-config service directory `<svc>/` may only reference `seam/routes/<svc>/*`, so the exfiltration shape "point service A's secret at service B's upstream" fails on ownership even inside the allowlist. Path traversal, globs, and templated segments in `x-vault-path` are rejected outright.

  **How the gateway knows `<svc>` at runtime** (decided 2026-07-20). The co-ownership rule was previously declared enforceable twice — at lint time and again by the gateway "never trusting lint to have run" — but the gateway half was unimplementable as written: at runtime SEAM sees only files at `/etc/gateway/routes.d/*.yaml`, and nothing in that projection carried the originating declarative-config directory, leaving lint (which this design explicitly refuses to trust) as the sole enforcement of the anti-exfiltration control. The owner is therefore made a **first-class, projection-preserved property** in two mutually-checking forms:
  - **The projected parent directory is authoritative** (corrected 2026-07-20 from "the projected filename", which permitted only one fragment per service and therefore foreclosed version coexistence — see the filing rule above). Each service's fragments ship as the single ConfigMap `seam-routes-<svc>` generated from declarative-config directory `routes/<svc>/`, and SEAM's `projected` volume mounts that source with an explicit per-key rename into an owner subdirectory: `items: [{key: <name>.yaml, path: <svc>/<name>.yaml}]`, one entry per file the service ships. The gateway derives `<svc>` from the **parent directory** of each file it loads — the basename is free-form and load-bearing for nothing — and enforces `x-vault-path` ⊂ `seam/routes/<svc>/*` against *that*. This is the forgery-resistant half, and it is exactly as forgery-resistant per-directory as it was per-file: the `path:` rename lives in **SEAM's own Deployment manifest**, not in the service's directory, so a fragment author editing only `routes/<svc>/` cannot change the owner segment their content is filed under, cannot smuggle a key into another service's directory, and cannot make their file land under `otherservice/`. Changing the binding requires a PR against SEAM's own manifest, which is exactly where that decision belongs.

    **The explicit `items:` is kept deliberately, and it is not free** (decided 2026-07-20 — stated because two other design principles assert the opposite and leaving all three standing would have hidden a real cost). The source list lives in SEAM's pod template, so **onboarding a new service is a Deployment edit and therefore a rollout**, with the same 5-30 second gap a binary deploy carries — it is not the restart-free ConfigMap edit the reload bullet promises for route changes. The restart-free alternatives were considered and each trades away the control this whole subsection exists to hold: a single aggregated ConfigMap for all services returns key naming to whoever writes the files, which is the forgery the rename prevents; deriving the source list mechanically from the `routes/*` directory listing keeps forgery resistance but still edits the pod template, so it buys nothing here; and reading ConfigMaps through an API informer would genuinely make onboarding restart-free, but it is the explicitly *unshipped* upgrade path in the reload bullet and Phase 6a deliberately grants SEAM **no ConfigMap API permission at all**. So the cost is accepted and named rather than traded away silently: **onboarding is rare, per-service, and already a manifest PR for the upstream-host allowlist anyway; route, contract and version changes inside an already-onboarded `routes/<svc>/` — the traffic that actually changes — stay pure ConfigMap edits with no restart.** If onboarding cadence ever makes that gap expensive, taking the informer upgrade is the change that removes it.
  - **`x-seam-owner: <svc>` is required in every fragment and must match.** A required in-file owner field on its own would be worthless — a hostile fragment author simply lies in it — so it is never the authority, only a redundant declaration checked *against* the filename. A fragment whose `x-seam-owner` disagrees with the projected **parent directory** it was loaded from, or that omits the field, is quarantined whole (never partially loaded) with the mismatch named at `/config/status`. The value is that it makes misfiling loud rather than silent, gives `seam lint` something to check in CI before any volume exists, and turns a mounting mistake in SEAM's own manifest into a quarantine instead of a privilege grant.

  `seam lint` checks the full chain — directory name → ConfigMap name → projected `path:` owner segment → `x-seam-owner` → `x-vault-path` prefix — **for every file in every `routes/<svc>/` directory, since a service ships N of them** (amended 2026-07-20 with the filing rule above; the check is per-file and the chain is per-directory, so a service adding a second fragment adds a check, never a new chain). The gateway independently re-checks the last three at merge time from what it can actually observe. Belt and braces are now both real.
- **Upstream-host allowlist — the peer control to the vault-path allowlist** (decided 2026-07-20; Phase 2 deliverable). Everything above constrains **which secret** a fragment may name and nothing at all constrains **where that secret is sent**: `x-upstream` and every `x-upstream-map` entry `url` were unbounded. That leaves the whole anti-exfiltration design guarding the wrong half. Co-ownership defeats "point service A's secret at service B's upstream", but the exfiltration that actually matters is simpler and passes every check written so far: a fragment filed correctly under `routes/<svc>/`, declaring `x-seam-owner: <svc>` and `x-vault-path: seam/routes/<svc>/token` — its own secret, its own slot, no rule broken — and `x-upstream: https://attacker.example`. SEAM fetches the live credential and injects it into a request to that host, exactly as designed. **None** of the plan's other controls touch this: response scrubbing is response-side and the attacker needs no response; TLS verification passes, since an attacker controls a host with a perfectly valid certificate; `x-upstream-plaintext` is irrelevant because `https://` is the *preferred* form here. Since fragments are authored largely by agents, this is the shortest path from "hostile fragment merged" to "credential in an attacker's log", and it must be closed by the same posture as the vault prefix:
  - **The allowlist is an operator-owned set of permitted upstream hosts and suffixes, and it lives in SEAM's own manifest directory — never in a fragment.** This is the load-bearing half. A fragment-supplied allowlist is self-certifying and therefore worthless: the author who names the malicious host also names the rule permitting it. Like the projected `path:` binding and the `seam-upstream-ca` ConfigMap above, changing it requires a PR against SEAM's own manifest, which is exactly where that decision belongs.

    **How the running gateway actually reads it** (decided 2026-07-20 — "read from SEAM's own Deployment manifest" named an *owner*, never a *mechanism*, leaving the control that carries half the anti-exfiltration design with no stated delivery: env var, mounted file, and a third projected source were all equally consistent with the text, and one of the two operator-owned controls in this plan was therefore unimplementable in the phase that owns it). It ships as its own operator-owned ConfigMap **`seam-upstream-allowlist`** at `k8s/rs-manager/seam/`, mounted into SEAM as **its own volume at the fixed path `/etc/gateway/allowlist.yaml`** — outside every `routes/<svc>/` directory and deliberately **not** a source of the `routes.d/` projection, so no fragment can shadow it, extend it, or be mistaken for it by the merge. Three consequences worth stating: it is a mounted file rather than an env var **because Phase 6a grants SEAM no ConfigMap API permission at all**, so a mount is the only channel available — and because a list that grows per upstream, carried in the pod template, would make every allowlist edit a rollout; SEAM re-reads it on change on the same watch that serves `routes.d/`, so **widening the allowlist is a restart-free ConfigMap edit** even though onboarding the fragment that uses it is not; and if the file is **absent, empty, or unparseable SEAM fails closed** — no host is permitted, every fragment naming an upstream quarantines, `/_seam/readyz` reports not-ready, and `/config/status` names the condition. A missing allowlist that defaulted to permissive would be the exfiltration path itself.
  - **What it contains**: the tailnet suffixes `*.ts.net` and `*.ardenone.com`, the in-cluster suffix `*.svc.cluster.local` (plus bare in-cluster Service DNS), and **explicitly enumerated public hosts, one line each** — `api.twitterapi.io` and the z.ai/GLM proxy host being the Phase 4 entries. Suffix matching only, on the resolved host of the URL, never on a substring or a regex; a port may be constrained alongside a host. IP-literal upstreams are rejected outright, since they defeat the enumeration the allowlist exists to provide. **Onboarding *any* new upstream is therefore a two-PR operation** (generalized 2026-07-20 — this read "a new **public** upstream", which counted only the allowlist PR and so implied that a tailnet or in-cluster upstream already covered by a suffix needed no manifest change at all; in fact **every** upstream needs one, because the fragment's ConfigMap must also be added to the projected volume in SEAM's Deployment) — one against `k8s/rs-manager/seam/`, adding the projected-volume source and, for a host no suffix already covers, the allowlist entry; and one against `k8s/rs-manager/seam/routes/<svc>/` to ship the fragment itself. Two paths, two reviewers, and that friction is the control, not a cost of it.
  - **Enforced twice, exactly as the vault prefix is.** `seam lint` rejects any `x-upstream` or `x-upstream-map` entry `url` whose host is outside the allowlist at the CI gate, and **the gateway re-checks every resolved upstream host at merge time and quarantines the whole fragment on violation** — never trusting lint to have run, and never partially loading a fragment whose other routes happen to be fine. `/config/status` names the offending host. There is no per-fragment opt-out and no `acknowledged` form: unlike `insecureSkipVerify` or `x-unscrubbable`, which make a *known* exposure enumerable, an unlisted upstream host has no legitimate case that a manifest edit cannot express, and a per-fragment override would hand the exfiltration path straight back to the fragment author.
- **Inbound header stripping is a requirement, not an implementation detail** (decided 2026-07-20; Phase 2 deliverable). Before injection, SEAM **strips** every header it may itself set, rather than overlaying values onto whatever the caller sent: the header named by each matched route's `x-inject-as.name` (see Data Models), `Authorization`, and all `X-SEAM-*` request headers except the documented caller-supplied ones — `X-SEAM-Dry-Run` (Per-Route Guards) and `X-SEAM-API-Version` (Version Migration Strategy §2). Comparison is on the canonicalized header name (case-insensitive, per-key delete of *all* values, not a single-value overwrite), so `authorization`, `AUTHORIZATION`, and duplicate keys are all removed. This is strip-then-inject, never trust-then-overlay — an attacker-supplied `Authorization` or a custom header matching some other route's `x-inject-as` must never survive into an upstream request.

  **The rule covers the query string too, not headers only** (extended 2026-07-20 — it previously spoke of headers exclusively, which silently exempted the one injection kind where the caller controls the same namespace SEAM writes into). For a route whose `x-inject-as.kind` is `query`, SEAM **deletes every occurrence of the injected parameter name from the request query string before injecting its own** — all repetitions, not the first, since duplicate keys are legal in a query string and upstreams disagree wildly on which one wins (first, last, or an array). Without this, a caller could send `?api_key=<theirs>` and, depending on that upstream's merge order, either override SEAM's injected credential or have theirs forwarded alongside it — precisely the trust-then-overlay failure this rule exists to forbid, just relocated from the header namespace to the query namespace. Deletion is by exact parameter name (query keys are case-sensitive, unlike headers) and applies to the names declared by **any** matched route's `x-inject-as`, not just the credential kind actually in play on this request. Same treatment for the fan-out and adapter paths: the strip happens on the inbound request, before the adapter's request transform can rename a caller-supplied parameter into the injected one. Note that Go's `httputil.ReverseProxy` `Rewrite` hook removes only hop-by-hop and `X-Forwarded-*` headers (ADR-001 §3); everything in this bullet is SEAM's own responsibility on top of that.
- **Secret caching policy** (decided 2026-07-20; Phase 2 deliverable): secrets are held in an in-memory cache with a **short 30-second TTL**, never rendered to a file or held for the process lifetime. The cache is invalidated eagerly on an upstream `401` (which triggers refetch-and-retry-once, see Credential Health Sentinel), and TTL expiry forces a refetch otherwise. Rotation latency is therefore bounded at 30s worst case for a silent rotation, and effectively zero for a rotation the upstream signals by rejecting the stale credential — the point being that an OpenBao rotation takes effect without redeploying or restarting SEAM. The cache is keyed by resolved `x-vault-path`, so instances sharing a path share one entry; values are never logged, never written to disk, and never included in `/config/status` or any health surface.
- **The request/response pipeline, in order** (decided 2026-07-20; stage numbering revised 2026-07-20). Operations this plan specifies independently — header and query stripping, identity resolution, scope authorization, spec validation, dry-run short-circuit, adapter transforms, guard evaluation, secret injection, the upstream call, secret-echo scrubbing — compose into **eleven inbound stages and four outbound**, and **order determines behavior**, so it is fixed here once rather than left to the implementer. **The two numbered lists below are the authority for stage numbering**: every "stage N" reference elsewhere in this plan means a position in these lists, and no other count of these operations is normative. An implementation may not reorder these stages.

  **Request (inbound):**
  1. **Control-plane resolution** — SEAM's own surface resolves before the merged route table, unconditionally (Fragment Toolchain rule 2).
  2. **Inbound sanitation** — strip-then-inject hardening: delete every injectable header and every injectable query parameter, plus `Authorization` and all `X-SEAM-*` except the documented caller-supplied `X-SEAM-Dry-Run` and `X-SEAM-API-Version`. **First, always** — nothing downstream may ever read a caller-supplied value in a namespace SEAM writes into.
  3. **Caller identity — resolution only** — resolve *who* is calling (tailnet Grant via `WhoIs`, or verified Cloudflare Access JWT), and default-deny a request whose identity cannot be resolved at all. **Authorization is deliberately not done here** (split out of this stage 2026-07-20): `x-required-scope` is a *per-route* field, and on an adapting fragment it is the **union of the matched fragment's and its `targetVersion`'s** requirements (Version Migration Strategy §2) — so it is not computable until both the fragment and its adapter target are known, which is stage 4's job. Splitting identity out keeps the property the merged stage was reaching for: identity is established before any secret lookup or upstream contact, and nothing below runs for an unidentified caller.
  4. **Route and version match** — (path, method), then version selection per §2 (`X-SEAM-API-Version`, else oldest still-served). This picks the *fragment*, and therefore the contract everything below is relative to. For a fragment carrying `x-adapter`, it also resolves the `targetVersion` fragment the adapter delegates to, since stages 5, 8 and 9 all need it.
  5. **Authorization** — evaluate `x-required-scope` against the identity resolved at stage 3, default-deny. On an adapting fragment this is the **union** of the matched fragment's and the target version's requirements, which is why it cannot precede stage 4 — union is the only direction that cannot be gamed, since otherwise pinning an old `X-SEAM-API-Version` would be a privilege-de-escalation path (§2). Still before any secret lookup, adapter transform, guard draw, or upstream contact. Scope-filtered visibility applies here too: a route outside the caller's filtered spec is a **404 identical to a route that does not exist**, and only the visible-but-not-invocable case gets the scope-naming 403 (see the `/docs/route` rule above).
  6. **Spec validation — against the matched version's own declared schema**, not the newest one. This is why validation precedes the adapter and is nonetheless correct: an old-contract request is validated against the old fragment's schema, which is exactly the contract that caller was served and the only one it can be held to. Failure is the structured 400, whose `/docs/route` pointer carries this same version.
  7. **Dry-run short-circuit** — `X-SEAM-Dry-Run: 1` returns the validation verdict here and stops. No guard counter is incremented, no quota drawn, no secret fetched, no upstream contacted.
  8. **Adapter request transform, and re-validation against the target's schema** — the matched older fragment's `x-adapter.request` converts the validated old-contract request into the newest contract, the one the upstream actually speaks, and the transform's output is then re-validated against the *target* version's request schema. A failure is a **500 attributed to the adapter**, never a 400 (§2). A request on a fragment carrying no `x-adapter` passes through this stage unchanged. **This precedes guards** (corrected 2026-07-20 — it was specified after them, which was unbuildable): both guard rules take this stage's output as input. The loop breaker's normalized request hash is computed **after** the request transform, so two callers on different `x-api-version`s sending semantically identical requests count as one loop; and guards are charged to the **target** version's route, which only adapter resolution makes known (both from §2). A guard stage placed before the adapter could satisfy neither.
  9. **Guards** — loop breaker, then cost governor. After validation so a malformed request is a 400 rather than a budget draw, after the adapter so the hash and the charged route are the target's, and before injection so an over-budget request never causes a secret to be read out of OpenBao. Moving the adapter above guards costs nothing that argument depended on: **re-validation still concludes before secret resolution**, so a broken transform still can never cause a credential to be injected into a malformed upstream request — injection simply now follows two stages later instead of one.
  10. **Secret resolution and injection** — cache or OpenBao fetch, then inject per `x-inject-as` into the now-sanitized request.
  11. **Upstream call** — the upstream path is computed here, from the matched template and its stage-4 bindings rather than from the inbound string (Data Models, `x-upstream`: drop the `{instance}` segment, apply any `x-upstream-path-template` / `x-upstream-strip-prefix`, re-substitute the remaining parameters), then dispatched against the resolved base URL.

  **Stages 3 and 5 before Phase 7: identity resolves to nobody, and authorization is a no-op** (decided 2026-07-20 — the lists above are written in their Phase-7-and-later form and declared unconditionally normative, while Pre-Phase-7 Trust Boundary is equally categorical that "all inbound caller authentication and authorization lands in Phase 7" and that callers behind shared egress are "indistinguishable". Read together as written, **every** request in Phases 1a-6b has an identity that cannot be resolved at all, stage 3's default-deny fires on all of them, and SEAM serves nothing across the entire pilot, onboarding and cutover. Every *other* pre-Phase-7 degradation in this plan is stated explicitly — the loop breaker degrades to (route, hash), `x-quota` to a single route-wide budget, the operator tier to a listener-port split — and this one was simply missed). The degradation, in the same style as those:
  - **Before Phase 7, stage 3 resolves no identity and denies nothing.** There is no identity mechanism to resolve against — no per-worker `tsnet` node (Phase 7), no verified Access JWT (Phase 14) — so the stage records the caller as the reserved anonymous identity and passes the request on. **The tailnet ACL is the boundary in that window**, exactly as Pre-Phase-7 Trust Boundary says it is, and the residual risk that section states plainly ("coarse and node-level, not per-agent") is the whole of what stands in stage 3's place.
  - **Before Phase 7, stage 5 is a no-op.** `x-required-scope` is parsed, validated for shape, carried on the matched fragment and rendered as documentation at `/docs/route` and `/openapi.json` — but it is **not enforced**, and no request is refused for want of a scope. Scope *filtering* is likewise inert, since there is no grant set to filter against: the filtered-spec 404 rule and the narrower visible-but-not-invocable 403 both become live with the enforcement they belong to, not before it.
  - **Both activate at Phase 7, together and never separately** — that activation is a Phase 7 deliverable. They are two halves of one control: enabling stage 3's deny without stage 5's check would refuse every caller, and enabling the check without the deny would evaluate scopes against nobody. Stage *numbering* is unaffected in either window — stages 3 and 5 always exist and always occupy those positions — which is what keeps every "stage N" reference in this plan stable across the transition.

  **Where a control-plane request goes after stage 1** (decided 2026-07-20 — stage 1 resolves SEAM's own surface "unconditionally" and the list then continues in the singular, as though every request were a proxied one, so the control-plane path simply falls off the end. That left the two-tier rule unimplementable: Fragment Toolchain requires `/config/status`, `/health/credentials` and `/health/upstreams` to "require the scope `seam:ops:read`" and gates `/scopes?all=1` on `seam:scopes:read-all` — yet authorization is stage 5, and stage 5 is defined solely as evaluating `x-required-scope`, a *per-route* field taken from the fragment matched at stage 4, which a control-plane endpoint has no fragment to carry. A stage-1 match that short-circuited outright would run those checks at no stage at all. It would equally strand the scope *filtering* of the caller-facing tier, since `/openapi.json`, `/docs`, `/docs/route`, `/whoami` and `/scopes` are all required to be reduced to the caller's own grants, which needs the identity stage 3 resolves. These are precisely the endpoints carrying the reconnaissance this plan works hard to withhold everywhere else). **A stage-1 match is a branch, not an exit:**
  1. It still runs **stage 2, inbound sanitation.** A control-plane response is exactly where a forged `X-SEAM-*` header would be most valuable, since agents make authorization decisions from `/whoami` and `/scopes`.
  2. It still runs **stage 3, identity resolution**, on the same terms and with the same pre-Phase-7 degradation as above. Every caller-facing control-plane endpoint is scope-filtered to that identity, so the branch cannot precede it.
  3. It then runs **control-plane authorization** — this branch's counterpart to stage 5, evaluating the **built-in scope declarations compiled into the SEAM binary** (Per-Agent Tool Scoping point 6) in place of `x-required-scope`: `seam:ops:read` for the operator tier, `seam:scopes:read-all` for `/scopes?all=1`. That built-in set exists for exactly this reason — a control-plane endpoint has no fragment and therefore no `x-required-scope` anywhere. Refusal here is a **403 naming the missing scope**, deliberately *not* the 404-identical-to-unknown rule that governs fragment routes: these are fixed, documented paths whose existence this plan publishes, so hiding them buys nothing (Fragment Toolchain, "who may *call* the control plane").
  4. It then **short-circuits before stage 4**. There is no route-table entry to match, no version to select, no upstream, no secret and no guard draw, so stages 4 and 6-11 never run. Outbound, only stage 3's bookkeeping and SEAM headers apply — no credential was injected on this path, so stage 4 has nothing to scan.

  **The pre-Phase-7 degradation applies to this branch too:** step 3 evaluates nothing, and the operator tier's boundary is instead the **listener-port split** — two ports bound from Phase 1a, granted separately in the tailnet policy file — which is enforceable with no identity at all. Step 2 is unaffected and runs from the first commit. The caller-facing tier is unfiltered in that window because there is nothing to filter by. At Phase 7 step 3 becomes the real control and the port split degrades to defense in depth behind it, the same relationship Pre-Phase-7 Trust Boundary already describes between the ACL and Grant-based enforcement. Phase 1a owns the branch as a *routing* rule (stages 1-2 and the short-circuit); Phase 7 owns step 3's enforcement.

  **Response (outbound):**
  1. **Upstream response received.**
  2. **Adapter response transform** — `x-adapter.response` converts the newest-contract response back into the contract this caller is on.
  3. **Bookkeeping and SEAM headers** — last-2xx and breaker state, budget decrement, `X-SEAM-Spec-Version` / `X-SEAM-Scope-Version` / `X-SEAM-API-Version`, `Deprecation`/`Sunset`.
  4. **Secret-echo scrubbing — last.**

  **The invariant: scrubbing is the final operation applied to a response body, headers, and trailers before they leave SEAM.** Nothing may run after it. Placing the adapter's response transform *after* scrubbing would let an envelope unwrap or a re-encode relocate or resurface a value the scrubber had already passed over, converting the plan's one core guarantee ("the agent never sees the secret") into a function of transform authorship. Two consequences follow and are accepted deliberately:
  - The chunked scrubber's `len(secret) - 1` carry-over overlap (Credential Health Sentinel) operates on the **transform's output stream**, not the raw upstream bytes. That is fine for a pass-through stream, which is byte-identical.
  - But a body-restructuring adapter transform (envelope wrap/unwrap, field move) **cannot** operate on a stream, so any route whose `x-adapter.response` alters body structure **forces the buffered response path** and is subject to its size cap. A route that needs a body transform *and* cannot be buffered — a protocol upgrade, an oversized or opaque body — is unscrubbable by construction: injection is refused there unless the fragment carries `x-unscrubbable: acknowledged`, exactly as for any other unscannable response. `seam lint` flags this combination specifically, since it is easy to author unknowingly.

  **Where an `_all` fan-out sits in these stages** (decided 2026-07-20 — the lists above are written in the singular throughout, and a fan-out resolves N instances, fetches up to N distinct per-instance secrets, injects under up to N distinct `injectAs` shapes, issues N concurrent upstream calls and consults N breakers; leaving the split point unstated leaves an implementer to invent whether guards run once or N times and whether scrubbing sees one body or N). Stages 1-9 run **exactly once, on the single inbound request**, against the aggregate — identity, authorization, validation, dry-run, adapter transform and both guards are properties of the request the caller sent, not of the instances it happens to reach. The fan-out is the boundary between stage 9 and stage 10:

  - **Guards are evaluated once, against the aggregate**, with the cost governor drawing **per instance actually dispatched** rather than per request — the accounting rule Per-Route Guards already states, restated here as a stage placement: quota is checked once at stage 9 against the *projected* draw (the number of instances that will survive breaker filtering), and instances the breaker refuses are charged nothing. The loop breaker likewise counts the inbound request once; `_all` is one request in its normalized hash, not N. A fan-out route's `x-quota` must therefore be sized against the map's width.
  - **Stages 10 and 11 execute per surviving instance**, concurrently: each instance resolves its own `vaultPath` (so N distinct secrets may be fetched, from N distinct cache entries) and injects under its own `injectAs` kind and name, into its own copy of the sanitized request, over its own `x-upstream-map` entry `url` and per-instance `tls` policy. Instances whose breaker is open are skipped before stage 10 and never reach a secret lookup at all.
  - **Outbound stages 1 and 2 run per instance** — each instance's response is received and adapter-transformed on its own — and the **merge into the instance-labeled result is outbound stage 3's bookkeeping**, alongside the per-instance breaker and last-2xx updates.
  - **Outbound stage 4 — scrubbing — runs exactly once, over the merged body, headers and trailers, against the UNION of every value injected on this request.** Not per instance: a merged body is a single artifact that may relocate or concatenate any instance's echoed credential into a region the per-instance scrubber never saw, and the invariant above is that *nothing runs after scrubbing*. Scanning the merge against the union is the only ordering that keeps that true. This is the same rule the chunked scrubber already states — "where multiple secrets could be present, the overlap is the longest of them" — with the union now explicitly spanning instances rather than just the multiple secrets of one route. A fan-out in which any single instance is unscrubbable makes the whole fan-out unscrubbable, and is refused absent `x-unscrubbable: acknowledged` on the fragment.
- **Upstream TLS: verified by default, and plaintext is an acknowledged exception** (decided 2026-07-20; Phase 2 deliverable). SEAM injects a live credential into every forwarded request, so the transport carrying that request is part of the injection guarantee — yet nothing here previously said a word about certificate verification, and the fleet's own proxies are routinely reached with self-signed certs (`curl -sk https://argocd-ro-...:8444`). An implementer left to infer the rule copies the worked example, ships credentials over plaintext, meets a self-signed cert, and reaches for a blanket skip-verify. Three rules instead:
  - **Verify by default.** Every `https://` upstream is verified against the system trust store with hostname checking on. There is no global skip-verify switch and no environment variable that disables verification fleet-wide — a per-fragment, per-route, enumerable opt-out is the only form this takes.
  - **Self-signed upstreams pin a CA, they do not skip verification.** `x-upstream-tls.caBundle` (see Data Models) names a PEM bundle key in SEAM's own `seam-upstream-ca` ConfigMap — SEAM-manifest-owned, like the fragment `path:` binding above, so a fragment author cannot introduce trust material by editing only their own directory. **That ConfigMap reaches the gateway as a mounted volume, not through the API** (decided 2026-07-20 — stated for the same reason as the allowlist mechanism above: Phase 6a grants SEAM **no ConfigMap API permission at all**, so "resolved against the `seam-upstream-ca` ConfigMap" had no implementable reading). It is filed at `k8s/rs-manager/seam/` and mounted at the fixed path **`/etc/gateway/upstream-ca/`**, one file per key, so `caBundle: argocd-ro.pem` resolves to `/etc/gateway/upstream-ca/argocd-ro.pem`. Like the allowlist it is its own volume, **never a source of the `routes.d/` projection** and outside every `routes/<svc>/` directory, and it is re-read on change on the same watch — so rotating a pinned CA is a restart-free ConfigMap edit. A `caBundle` naming a key that is absent or unparseable quarantines that fragment with the missing key named at `/config/status`; it never silently falls back to the system trust store, which would convert a pinning failure into a quiet downgrade. `x-upstream-tls.serverName` overrides SNI/hostname verification where the cert's name differs from the dialed host. This is the intended answer for every `-k` in today's CLAUDE.md prose, and it is strictly better than what it replaces.
  - **`x-upstream-tls.insecureSkipVerify: acknowledged`** is the last resort, and — in the style of `x-unscrubbable` — `acknowledged` is the **only** accepted value, with no `false` form, because the field exists to make the exposure enumerable and an enumerable exposure cannot be expressed by a field nobody has to write. `seam lint` flags every occurrence for human review; `/config/status` enumerates every route currently forwarding a credential over an unverified connection.

  **Plaintext `http://` carve-out: it exists, narrowly, and it is not free** (decided 2026-07-20 — stated because the plan's other examples use plaintext tailnet and in-cluster URLs, so silently forbidding it would be ignored, and silently permitting it would ship credentials in the clear). Plaintext is acceptable **only** for a hop that never leaves an already-authenticated network path — a tailnet-internal address (WireGuard-encrypted end to end) or an in-cluster Service DNS name — and never for anything reachable off it. It is not inferred from the URL's shape: a plaintext `x-upstream` must carry `x-upstream-plaintext: acknowledged`, on the same only-accepted-value terms, is lint-flagged, and appears in the same `/config/status` enumeration. The point is that "it's just the tailnet" remains a defensible engineering call, but it becomes a decision someone wrote down and can be asked about, rather than a default nobody chose.

## Hosting

- Candidate host: rs-manager (already runs ArgoCD, OpenBao, `reloader`, `kubernetes-reflector`, and a Tailscale operator with Connector-based egress).
- rs-manager currently has Tailscale Connector egress to 3 clusters only: ardenone-cluster, ardenone-hub, ardenone-manager. Reaching the remaining clusters (apexalgo-iad, iad-options, iad-kalshi, iad-native-ads, iad-ci, ord-devimprint) requires adding a Connector per cluster, following the pattern of the 3 existing ones. That is the full fleet inventory — 10 clusters, of which rs-manager is itself one (decided 2026-07-20; `iad-acb` was previously listed here in error — ACB is a deployment/bucket concern, not a cluster).
- **ardenone-hub is slated for decommission** — it is one of the 3 already-reachable clusters, but no SEAM onboarding work (fragments, routes, or Connector maintenance) should be planned against it; treat the reachable set for planning purposes as ardenone-cluster and ardenone-manager.
- **The Phase 5 kubectl-proxy target set, by name** (decided 2026-07-20 — stated explicitly because Phase 5 sizes its Connector work and the width of its `x-upstream-map` off this number, and the number was previously only inferable by subtracting the decommissioned hub from a count stated elsewhere): **ardenone-cluster, ardenone-manager, apexalgo-iad, iad-options, iad-kalshi, iad-native-ads, iad-ci, ord-devimprint, and rs-manager itself — nine.** That is the 10-cluster fleet inventory above, minus ardenone-hub. **rs-manager is in the set**: SEAM runs there from Phase 6a, so it reaches rs-manager's own kubectl-proxy over in-cluster Service DNS and needs **no Connector** for it — it is the one entry that is a `x-upstream-map` row without a corresponding egress build-out. Connector work in Phase 5 is therefore the six not already reachable (apexalgo-iad, iad-options, iad-kalshi, iad-native-ads, iad-ci, ord-devimprint); the map is nine entries wide. Anything that needs this number should reference **this named set** rather than restate a count, so a future cluster addition or decommission is a one-line edit here.
- **What lives in SEAM's own manifest directory, and where each piece is mounted** (decided 2026-07-20; the delivery mechanism is Architecture's, this line is the hosting inventory it implies and a 6a checklist). `k8s/rs-manager/seam/` holds SEAM's Deployment, its Service, and **three ConfigMap concerns that reach the pod as three distinct mounts**: the per-service route ConfigMaps `seam-routes-<svc>`, projected together into **`/etc/gateway/routes.d/<svc>/<name>.yaml`** (one `items:` entry per file, owner subdirectory per service); **`seam-upstream-allowlist` at `/etc/gateway/allowlist.yaml`**; and **`seam-upstream-ca` at `/etc/gateway/upstream-ca/`**, one PEM per key. The latter two are separate volumes and deliberately **not** sources of the `routes.d/` projection — they are operator-owned controls a fragment must never be able to supply, shadow, or be merged with, and they are the reason SEAM needs no ConfigMap API permission (Phase 6a) to read either. `routes/<svc>/` subdirectories are the only part of this tree a fragment author writes to.
- **`seam-retirement-evaluator` runs on rs-manager too, as its own Deployment beside SEAM — not in SEAM's pod** (decided 2026-07-20; Components). It needs exactly two reachabilities and neither is SEAM's: **read access to the metrics store**, and **egress to Forgejo (`git.ardenone.com`)** to open the declarative-config PR. It has its own ServiceAccount, its own OpenBao Kubernetes-auth role and policy, and its own manifest and ArgoCD app under `k8s/rs-manager/seam-retirement-evaluator/` — deliberately *outside* `k8s/rs-manager/seam/`, so nothing about it is reachable by editing SEAM's directory and nothing about SEAM is reachable by editing its. It is **not** on SEAM's Tailscale node and does not share SEAM's ServiceAccount, which is the whole point: colocating them would reunite the two credentials the Phase 8 split exists to keep apart.
- **The metrics store is VictoriaMetrics — the fleet's** (decided 2026-07-20; named concretely because Version Migration Strategy §2 makes it the durable substrate for retirement and its retention window explicitly load-bearing, and "the fleet's existing metrics store" is not an address). SEAM exposes `/_seam/metrics` in Prometheus text format on the operator port (Phase 1a) and is scraped by the VictoriaMetrics instance serving rs-manager; the evaluator queries that same instance read-only. Two consequences already stated in §2 and repeated here because they are hosting facts: gap resolution is bounded below by the scrape interval (irrelevant against a 7-day floor), and a route-version older than the configured retention reports "insufficient history" and falls back to the 7-day floor rather than inventing a gap.
- The two Phase 4 onboarding targets (z.ai/GLM proxy, twitterapi.io proxy) already live on ardenone-cluster, which rs-manager already reaches — no new Connector needed for those two. (The *pilot* is the ArgoCD read-only proxy in Phase 3 — corrected 2026-07-20; this line previously called z.ai and twitterapi the "initial pilot targets", which contradicted Phase 3 and put two metered upstreams ahead of the cost governor.)

## Components

- **Gateway service** — HTTP server, written in **Go** (decided 2026-07-20, ADR-001 below): OpenAPI merge/validation (pb33f `libopenapi`/`libopenapi-validator`), secret injection, `/docs` + `/openapi.json`.
- **Route fragments** — per-service ConfigMaps, each an OpenAPI fragment + injection metadata, authored alongside the service they describe.
- **Tailscale Connectors** — per-cluster egress, added incrementally as new upstreams are onboarded.
- **`seam` CLI** — the gateway's merge/validation engine packaged as a command-line tool for CI and fragment authoring (`lint`, `diff`, `import`); see Fragment Toolchain below.
- **`seam-retirement-evaluator`** — a **second deployable, not part of the gateway** (added to this inventory 2026-07-20; introduced in Phase 8 and previously listed in none of this plan's inventories, which is how a component with its own identity, its own OpenBao path and a write credential against the GitOps repo came to have no manifest deliverable and no hosting line). A small Deployment with an internal scheduling loop (this fleet's no-CronJobs rule) that runs **read-only against the metrics store**, computes the quiet window per route-version from the durable per-route-version request counter, and opens the declarative-config retirement PR under **its own narrowly-scoped Forgejo credential**. It is deliberately separate from SEAM: SEAM's OpenBao policy grants `read` on `seam/routes/*` and nothing else, and the hostile-fragment threat model rests on that being literally true, so the Forgejo write token lives at its own OpenBao path readable only by this job's identity and never by SEAM (Phase 8). It consumes no gateway API, no gateway memory and no gateway credential; SEAM's only contribution is the exported metric.

## Data Models

- Route fragment schema (OpenAPI 3.1 fragment + extension fields) — not yet designed; needs a concrete JSON Schema before the first fragment is written. Extension fields accepted so far:
  - `x-upstream` — the single upstream base URL this fragment's routes forward to (e.g. `https://argocd-ro-ardenone-manager-ts.ardenone.com:8444` — **`https://`, corrected 2026-07-20**; this example previously read `http://` for an endpoint documented everywhere else as HTTPS, and since SEAM injects a live credential into every forwarded request, an implementer copying it would have shipped that credential in the clear). Required for an ordinary single-instance route. **Its host must resolve inside SEAM's operator-owned upstream-host allowlist** — the peer control to the `x-vault-path` allowlist and this field's single most important constraint, since without it a correctly-filed, correctly-owned fragment can name its own permitted secret and an attacker-controlled `url`. **Architecture owns that rule in full** (decided 2026-07-20): what the allowlist contains, that it lives in SEAM's own Deployment manifest and never in a fragment, how it is enforced twice, and that there is no per-fragment opt-out. The identical rule binds every `x-upstream-map` entry `url`. **Mutually exclusive with `x-upstream-map`** — a fragment declares exactly one of the two. `x-upstream-map` supersedes `x-upstream` when (and only when) the path declares an `{instance}` parameter; a fragment carrying both, or carrying `x-upstream-map` with no `{instance}` parameter, is a lint error and is quarantined at merge time. OpenAPI's native `servers:` is **not** used to express the upstream target (decided 2026-07-20): `servers:` in the merged spec is reserved for SEAM's own base URL — that is the address agents must actually call — and its root/path/operation inheritance rules make a merged multi-fragment document ambiguous about which server a given operation resolves against. Keeping upstream targets in a SEAM-owned extension also keeps them out of the served spec: `/openapi.json` never discloses upstream hostnames, only SEAM's front door.

    **How a SEAM-facing path becomes an upstream path** (decided 2026-07-20 — this bullet previously said only "the request path is appended to it", which is wrong for every worked example in this plan: appending yields `<upstream>/k8s/ardenone-cluster/api/v1/pods` where the kubectl-proxy serves `/api/v1/pods`, and `<upstream>/argocd/api/v1/applications` where the Phase 3 ArgoCD pilot's proxy serves `/api/v1/applications`. Nothing stated whether a prefix is stripped, whether `{instance}` is removed, whether a rewrite field exists, or how remaining parameters are re-substituted — so an implementer had to invent the whole mapping and every fragment authored before it settled would encode a different guess). The upstream path is **computed from the matched OpenAPI path template, never from the raw request path**, in four steps:
    1. **Start from the matched path template** (pipeline stage 4), not the inbound string — the raw path may carry `%2F`, dot-segment and duplicate-slash spellings that template resolution has already normalized away, and re-deriving from it would reintroduce every one of them on the upstream hop.
    2. **Delete the `{instance}` parameter's segment entirely.** It is routing metadata SEAM consumes to select an `x-upstream-map` entry, it names nothing the upstream has ever heard of, and it is therefore never re-substituted in step 4 and never appears in a forwarded request. A single-instance route has no such segment and skips this step.
    3. **Apply the fragment's declared rewrite, if any** — `x-upstream-path-template` on the path item (explicit, and it wins), else `x-upstream-strip-prefix` at fragment root (the shorthand for the common case where SEAM prefixes a service name the upstream does not use). With neither declared, the path is step 2's result unchanged, so a fragment whose SEAM-facing paths already match its upstream's declares nothing at all.
    4. **Re-substitute the remaining path parameters** from the bindings resolved at stage 4, each percent-encoded as **exactly one path segment** — a binding containing `/` or `..` is encoded, never expanded, so a path parameter can never smuggle extra segments into an upstream path.

    The result is appended to the resolved base (`x-upstream`, or the selected `x-upstream-map` entry's `url`), **preserving that base's own path prefix** and the template's trailing slash. The **query string carries over as-is from stage 2's sanitized request** — SEAM rewrites paths, never query semantics — with any `kind: query` injection added afterwards at stage 10. **The rewrite is evaluated at stage 11**, once per surviving instance on a fan-out (N base URLs, one shared computed path), and is deliberately **not** part of the adapter: `x-adapter`'s closed vocabulary transforms fields and parameter *names*, never paths, so a version migration that changes the upstream's path shape ships as a new fragment with its own rewrite rather than as an adapter step. Nothing upstream of stage 11 sees any of this — the loop-breaker's normalized hash keys on the matched template plus its bindings (Per-Route Guards), which is rewrite-independent by construction.

    The two flagship cases, worked, so no fragment has to guess: `/k8s/{cluster}/api/v1/pods` with `x-upstream-strip-prefix: /k8s` — a `GET /k8s/ardenone-cluster/api/v1/pods` forwards to `<that entry's url>/api/v1/pods`, `{cluster}` having been consumed as `{instance}`; and the Phase 3 ArgoCD pilot's `/argocd/api/v1/applications` with `x-upstream-strip-prefix: /argocd` forwards to `<x-upstream>/api/v1/applications`.
  - `x-upstream-path-template` — **path-item-level** explicit upstream path, the authoritative form of the rewrite in step 3 above (decided 2026-07-20). A path string in the *upstream's* own terms, which may reference any path parameter of the matched template by name except `{instance}` — e.g. `/api/v1/applications/{name}/sync` on a SEAM-facing `/argocd/{name}/sync`. It wins over `x-upstream-strip-prefix` where both are present, since it is the specific statement and the other is a default. `seam lint` checks that it begins with `/`, that every `{param}` it names exists in the matched template, and that it names no `{instance}`; the gateway re-checks at merge time and quarantines the fragment, never trusting lint to have run.
  - `x-upstream-strip-prefix` — **fragment-root** literal path prefix removed from every matched template after `{instance}` removal, the ergonomic form of the same rewrite (decided 2026-07-20). A literal (no parameters, leading `/`, no trailing `/`), applied to all of the fragment's paths, which is what makes it a one-line answer for the overwhelmingly common "SEAM namespaces this service under `/<svc>/` and the upstream does not" case. `seam lint` fails a value that is not a prefix of **every** path the fragment declares — a partly-applicable strip is a fragment whose routes silently forward to two different path shapes. Both this and `x-upstream-path-template` are **upstream-facing fields**: an `x-adapter`-bearing fragment may not declare either, taking both from its `targetVersion` like every other upstream-facing field (Version Migration Strategy §2).
  - `x-upstream-tls` — per-fragment upstream transport policy (decided 2026-07-20; see the upstream-TLS rule in Architecture). `{caBundle, serverName, insecureSkipVerify}`, all optional; absent means **verify against the system trust store with hostname checking**, which is the default and needs no declaration. `caBundle` names a PEM key in SEAM's manifest-owned `seam-upstream-ca` ConfigMap, resolved as a file under the mount `/etc/gateway/upstream-ca/` since SEAM holds no ConfigMap API permission (Architecture) — the correct answer for a self-signed fleet proxy, and the thing that replaces every `curl -k` in today's CLAUDE.md prose. `serverName` overrides SNI/hostname verification. `insecureSkipVerify` accepts the single value `acknowledged` and nothing else (no `false` form), is lint-flagged, and is enumerated at `/config/status`. A per-instance `tls` key inside `x-upstream-map` overrides it, since instances of one API can present certificates from different issuers.
  - `x-upstream-plaintext: acknowledged` — required on any fragment whose `x-upstream` is `http://`. Only accepted value, lint-flagged, enumerated at `/config/status`. Justified only for a hop that never leaves an authenticated network path — tailnet-internal or in-cluster Service DNS (see Architecture). **On a fragment using `x-upstream-map` this field does not apply**: acknowledgement there is the per-entry `plaintext: acknowledged` key, because the exposure is per-instance and a fragment-wide marker would misstate which instances are actually in the clear (decided 2026-07-20; see `x-upstream-map` below). Declaring this field alongside `x-upstream-map` is a lint error.
  - `x-seam-owner` — the owning service `<svc>`, required in every fragment from day one (decided 2026-07-20). It is what makes the `x-vault-path` co-ownership rule enforceable *by the gateway at runtime* and not by lint alone: see Architecture, where the authoritative binding is the projected **parent directory** `<svc>/` (a service ships N fragment files into it, all sharing one owner) and this field is the redundant in-file declaration checked against it. Deliberately **not** trusted on its own — a hostile author would simply lie in it — so a mismatch against the projected parent directory quarantines the whole fragment rather than being reconciled in either direction.
  - `x-vault-path` — which OpenBao secret to fetch; must resolve inside SEAM's OpenBao allowlist prefix and be co-owned with the upstream — i.e. inside `seam/routes/<x-seam-owner>/*`, verified against the projected parent directory (see Architecture)
  - `x-inject-as` — how to inject it. **An object, not a bare enum** (decided 2026-07-20 — this previously read only "header / bearer / query param", naming the *kind* and never the **name** of the header or query parameter, which left both Phase 2's injection and the strip rule that refers to "the header named by each matched route's `x-inject-as`" unimplementable against a field that carried no name). Shape: `{kind, name}`.
    - `kind: header` — `name` is required and is the header to set (e.g. `X-API-Key`). Comparison and deletion are on the canonicalized, case-insensitive name.
    - `kind: bearer` — `name` is **omitted and rejected if present**; it implies `Authorization: Bearer <secret>` exactly. It exists as its own kind rather than as `{kind: header, name: Authorization}` because the `Bearer ` prefix is part of the injection, not part of the secret, and making it a spelled-out convention is how it stays out of the stored value.
    - `kind: query` — `name` is required and is the query parameter to set (e.g. `api_key`). Query keys are case-**sensitive**, unlike headers. Per the strip rule (Architecture), every occurrence of `name` is deleted from the inbound query string before injection.

    The same shape is used by a per-instance `injectAs` override inside `x-upstream-map`, so a multi-instance fragment can hold instances that inject in different kinds *and* under different names.
  - `x-required-scope` — scope(s) a caller must hold (per the per-agent scoping design below). **Operation-level, with an optional fragment-root default that any operation overrides by replacement, never by merge** (decided 2026-07-20 — this closes the placement question for this field specifically; the remaining fields' fragment-root-vs-operation placement stays open under Open Questions). The value is an **array of scope strings**, a bare string being sugar for a one-element array, interpreted **conjunctively**: the caller must hold every scope listed. Duplicates collapse and order is insignificant. Per-method granularity is not a nicety — it is what makes the visible-but-not-invocable 403 in Architecture expressible at all (a caller holding `k8s-ro:get` sees the path and is refused `DELETE`); a fragment-root-only field could not say that, and the enumeration-oracle rule that 403 belongs to would have no case left to cover. The set-valued form is likewise load-bearing for Version Migration Strategy §2's adapter union and for point 6's `/scopes` inversion, both of which need a set rather than a scalar.
  - `x-credential-probe` — a designated safe request (path + method + **per-fragment probe interval**; decided 2026-07-16 — some credentials rotate every ~3 days, others essentially never, so cadence is per-fragment, not a global setting) the credential sentinel uses to validate this fragment's secret against its upstream — on a multi-instance fragment the interval is a default that each `x-upstream-map` entry may override per instance, since instances of one API can hold credentials with very different rotation cadences
  - `x-seam-schema` — an explicit fragment-schema version marker (e.g. `v1`) carried by every fragment from day one, so the fragment schema itself can evolve detectably — `seam lint` and the gateway can distinguish a v1 fragment from a v2 instead of guessing from shape
  - `x-breaker` — optional fragment-root tuning of the per-upstream circuit breaker (decided 2026-07-20; full policy and defaults in Passive Route Health). `{threshold, openSeconds, maxOpenSeconds, enabled}`, all optional; absent means the SEAM default policy — **the failure definition, thresholds, open durations, backoff and half-open rule are specified once in Passive Route Health, which owns them, and are deliberately not restated here.** It sits at fragment root because a fragment names exactly one upstream target by construction, and a per-instance `breaker` key inside `x-upstream-map` overrides it per instance. Unlike `x-loop-guard` and `x-cost-per-call`/`x-quota`, absence is **not** "no guard" — the breaker is infrastructure-level and on by default; `enabled: false` is the lint-flagged, `/config/status`-enumerated opt-out.
  - `x-loop-guard` — per-route repeated-identical-request thresholds (max repeats, window)
  - `x-cost-per-call` / `x-quota` — per-route cost annotation and per-caller budget; guards are configured individually per endpoint, never SEAM-wide. **Both are objects carrying an explicit unit, and `x-quota` additionally carries an explicit window** (decided 2026-07-20 — these two fields cap real money on the twitterapi.io credit balance and the z.ai quota, and neither a unit nor a reset period had ever been defined. `$50` was used illustratively throughout while twitterapi.io meters in credits and z.ai in quota units, with no conversion and no per-fragment unit declaration; and "budget window" appeared with no statement of whether it was hourly, daily, calendar-monthly, or rolling. An implementer had to invent both, and either invention silently moves the spend cap by orders of magnitude — which is the one thing this guard exists to prevent).
    - **`x-cost-per-call: {amount, unit}`** — `amount` a positive number, `unit` a short lowercase string naming what is being spent: `usd`, `credits`, `tokens`, `requests`. **SEAM never interprets a unit and never converts between units** — it holds no price table and no exchange rate, and inventing one would be a silent mispricing on exactly the routes that cost money. The unit is a label that makes the number unambiguous to the human sizing the budget and to the agent reading `X-SEAM-Budget-Remaining`; the governor's entire arithmetic is repeated subtraction within one unit.
    - **`x-quota: {amount, unit, window}`** — the budget the governor enforces. `unit` **must be byte-identical to the same route's `x-cost-per-call.unit`**; a mismatch is a lint error and a merge-time quarantine, since a mixed pair would draw a credit balance down in dollars. `x-quota` without `x-cost-per-call` on the same route is likewise a lint error — a budget with no per-call draw can never be decremented, so it caps nothing while reading as though it does.
    - **`window` is a plain duration matching `^[0-9]+(s|m|h|d)$`, and it is a tumbling window anchored at process start — not a calendar period.** A window opens when SEAM starts, closes `window` later, and the next opens immediately with `amount` restored. **A SEAM restart resets it unconditionally**, discarding whatever was drawn, which follows inescapably from guard state being per-process and in-memory (Per-Route Guards; Version Migration Strategy §1). That is also why **calendar-aligned windows are deliberately not expressible**: a `1mo` form would promise a monthly cap that any Tuesday-afternoon binary deploy quietly converts into two monthly caps inside one month — the gap between "per-month" and "per-restart" being precisely the ambiguity this decision closes. Since the honest ceiling on a window is SEAM's own uptime, `seam lint` **warns** on any `window` exceeding `168h` (7 days) and `/config/status` enumerates those routes, so a budget nominally longer than the deploy cadence is visible rather than merely believed. Sizing `window` at or below that cadence is what makes the cap mean what it says.
    - **What `X-SEAM-Budget-Remaining` reports** — on every response from a route carrying `x-quota`: the remainder, its unit, the window length, and the absolute instant the current window closes. `X-SEAM-Budget-Remaining: amount=37.5; unit=credits; window=24h; resets=2026-07-21T04:12:09Z`. All four are load-bearing — a bare number is unreadable without the unit, and without `resets` an agent cannot tell "wait eleven minutes" from "wait eleven hours", which is the difference between self-throttling and hammering. `resets` is the projected close of the current window and moves forward on restart like everything else here. Before Phase 7 the reported remainder is the **shared route-wide** remainder, not the caller's own (Per-Route Guards).
  - `x-upstream-map` — for multi-instance routes: maps an `{instance}` path parameter to a **per-instance object**, not a bare base URL (decided 2026-07-20). **This entry object is specified here and nowhere else** (consolidated 2026-07-20 — it had been restated in four places with four different key sets, and the phase that *builds* map resolution carried one of the short ones; every other mention is now a reference to this one). Each entry is **`{url, vaultPath, injectAs, tls, plaintext, probeInterval, breaker}`**; only `url` is required — and each entry's `url` host is bound by the same operator-owned upstream-host allowlist as `x-upstream` (see Architecture), so a wide map cannot smuggle one attacker-controlled instance in among nine legitimate clusters. The fragment-level `x-vault-path`, `x-inject-as`, `x-upstream-tls`, `x-credential-probe` interval and `x-breaker` act as defaults for any key an entry omits — `tls` overriding `x-upstream-tls` per instance, since instances of one API can present certificates from different issuers.

    **`plaintext` is the one key that is per-entry and never inherited** (decided 2026-07-20). Plaintext exposure is triggered by a *per-entry* `url` being `http://`, so a fragment-wide acknowledgement misrepresents it in both directions: a map with one plaintext instance among eight HTTPS ones would take a fragment-wide marker that reads at `/config/status` as though all nine ship credentials in the clear, and a reader auditing the enumeration could not tell which instance to fix. So on a map-bearing fragment the acknowledgement is `plaintext: acknowledged` **on each plaintext entry**, on the same only-accepted-value terms as everywhere else; a fragment-level `x-upstream-plaintext` alongside `x-upstream-map` is a lint error rather than a blanket cover, and an `http://` entry without its own `plaintext` is a hard error. `/config/status` therefore enumerates **(fragment, instance)** pairs here, matching the reporting key every other per-secret surface already uses. This exists because the motivating case demands it: the per-cluster kubectl-proxies expose a byte-identical API but each needs a *different* credential, differing in kind — long-lived ServiceAccount tokens for the read-only observers, ~3-day OIDC `cloudspace-admin` tokens for the read/write ones — so a single scalar `x-vault-path` cannot serve the collapsed fragment, and a 3-day token needs a far tighter probe cadence than one that never expires. The reserved instance `_all` fans out. Consequently every per-secret surface keys on **(fragment, instance)** rather than fragment alone — see `/health/credentials` below; for a single-instance fragment the instance key is the reserved value `_default`.
  - `x-api-version` — the route contract's version marker, **`v<positive integer>` matching `^v[1-9][0-9]*$` and ordered numerically** (decided 2026-07-20; full grammar, rationale and enforcement in Version Migration Strategy §2). No dotted, dated, or `v1beta1` forms; `seam lint` rejects a non-conforming value and the gateway quarantines the fragment at merge time. A fragment carrying no `x-api-version` is keyed under the reserved `_unversioned`, which **sorts strictly oldest** — so it remains the default for callers sending no `X-SEAM-API-Version` until it drains and is retired; `_unversioned` is assigned by SEAM and is a lint error if written literally.

    **The prohibition is on *authoring* `_unversioned`, not on *addressing* it** (decided 2026-07-20 — the two were conflated, and the conflation was not academic: `_unversioned` is the key under which every fragment shipped in Phases 3-5 lands and the only version that exists at all from Phase 3 through Phase 8, while Architecture requires every SEAM-generated pointer to carry a `?version=` equal to the version that actually served the request. Read as a blanket ban, the reserved key would make that pointer requirement unmeetable for the overwhelming majority of routes — and for *every* route during the phases when the pointer is SEAM's stated differentiator). `_unversioned` names a real served contract, so it is addressable exactly like a `v<n>`:
    - **Rejected** as a value an author writes into a fragment's `x-api-version` — `seam lint` fails it and the gateway quarantines at merge, so the reserved slot cannot be occupied deliberately.
    - **Accepted** as a caller-supplied `X-SEAM-API-Version: _unversioned`, selecting the unversioned contract explicitly rather than by omission. The two are equivalent only while `_unversioned` is still the oldest served version; sending it explicitly is how a caller pins to that contract and keeps landing on it, and it is the honest thing for a worker that fetched the spec before any `v1` existed.
    - **Accepted** as `?version=_unversioned` on **`/docs/route`, and there only**, and **emitted** by SEAM in every pointer it generates for an unversioned route (the structured 400's, the loop-breaker 429's, the `docsUrl` in `/changes`, the `Link rel="deprecation"`), each carrying `&version=_unversioned`; every response served by such a route echoes `X-SEAM-API-Version: _unversioned`. There is no bare-pointer exception for unversioned routes.

      **`/openapi.json?version=` is *not* one of those surfaces, and never was** (corrected 2026-07-20 — this bullet previously listed it, which assigned one query parameter two mutually incompatible value spaces, both stated as normative. Version Migration Strategy §3 and Phase 8 define `/openapi.json?version=<v>` as **level 3 of progressive disclosure**: prior *full merged specs*, served from the ring buffer and keyed by `X-SEAM-Spec-Version`, "a hash of the currently-served merged spec". `_unversioned` is an `x-api-version` — a *per-route contract marker* with an entirely different value space, a different cardinality, and a different lifetime. No implementer can resolve `?version=_unversioned` against a ring buffer of spec hashes, nor a spec hash against one route's version list, so one of the two read paths would have been built wrong — and `/openapi.json` is the endpoint every perpetually-live worker learns the contract from). The binding is therefore fixed and exclusive: **`/openapi.json?version=` takes a spec version (an `X-SEAM-Spec-Version` value) and nothing else; `/docs/route?version=` takes an `x-api-version` (a `v<n>` or `_unversioned`) and nothing else.** A spec-version value on `/docs/route`, or an `x-api-version` value on `/openapi.json`, is a **400 naming which of the two that endpoint expects** rather than an empty result — the two alphabets are distinguishable on sight, so the error can always say which mistake was made.

      **No `&apiVersion=` companion parameter is added** (decided 2026-07-20). Restricting a whole merged spec to a single API version was weighed and is not worth a parameter: nothing in this plan asks for it, `/docs/route?path=&version=` already answers the per-route form of the question that callers actually have, and the archived-spec read path is already scope-filtered at serve time (§3), so a second orthogonal filter multiplies the surface for no stated consumer. The name **`apiVersion`** is nonetheless **reserved on `/openapi.json`** — rejected today rather than ignored — so that if the need ever appears it lands as a distinct parameter with a distinct alphabet, and never by widening `?version=` back into the ambiguity this decision closes.
    - **Listed** in the `versions[]` array of a `/docs/route` payload like any other coexisting version — its own entry, its own `docsUrl` carrying `&version=_unversioned`, sorted first because it sorts oldest, and carrying `isDefaultForUnversionedCallers: true` for as long as it remains the oldest served version. A `versions[]` that omitted it would misreport the contract most callers are actually on. Distinct from `x-seam-schema`: `x-seam-schema` versions the *fragment format SEAM parses*, `x-api-version` versions the *contract SEAM serves to callers*. A breaking route change ships as a new fragment bearing a new `x-api-version` (or a new path segment), never as an in-place edit — see Version Migration Strategy and `docs/notes/versioning.md`.
  - `x-seam-deprecated` — a **root-level object on the fragment**, `{since, sunset?, brownout?}`, marking every route in that fragment deprecated and feeding it into the per-route-version request metric that gates retirement. **It is an object rather than a bare boolean because both headers it drives carry dates** (decided 2026-07-20 — it was previously specified as a boolean, which made the deliverable unimplementable: `Deprecation` (RFC 9745) and `Sunset` (RFC 8594) are both date-valued, and a boolean supplies neither, leaving an implementer to invent a value or silently omit the header). `brownout` is an **ordered, non-overlapping array of `{start, end}`** RFC 3339 instants, every window falling inside `[since, sunset]`, and it **requires a resolved `sunset`** — a brownout block without one is a hard error, since a brownout is defined relative to a retirement date. Its absence means the brownout scheduler is dormant for that fragment, which is the fail-safe direction. Unlike `since`, this member is **machine-written by `seam-retirement-evaluator` and human-merged**, never hand-authored in normal operation: it is the one channel by which the out-of-band evaluator's verdict reaches the gateway (see Phase 8), travelling the existing ConfigMap hot-reload path so the gateway needs no metrics credential, no new permission, and no durable state of its own.
    - **`since` is required** — the date the deprecation was declared, an ISO date the author writes in the same PR that adds the marker. It populates `Deprecation` directly. This is a fact the author knows, so it needs no computation and can never be wrong.
    - **`sunset` is optional and advisory.** When present it is emitted as `Sunset` verbatim — the author's honest best guess at a removal date, useful for signaling intent on a route somebody has actually planned around. When absent, **`Sunset` is emitted only once the quiet window has actually started and its projected end is computable** — that is, once the route-version's request counter has read zero long enough for the adopted window to resolve to a date (the formula is fixed in Phase 8, which owns it) — and is omitted entirely before then. `Deprecation` alone is emitted in the interval between marking and the quiet window opening, which is correct and RFC-conformant: the route *is* deprecated, and no sunset is yet foreseeable.
    - **Either way `Sunset` is advisory and may move later, never earlier.** Retirement stays usage-gated: a caller appearing during the window resets it, a brownout that finds a caller **cancels** retirement outright, and final removal waits on a human merging the declarative-config PR. So an emitted `Sunset` is a projection, and SEAM will happily blow through it rather than retire a route somebody is calling. A bare `x-seam-deprecated: true` is a lint error with a fix hint naming the object form, rather than being silently coerced to a dateless deprecation.
    - Responses from a deprecated fragment additionally carry `Link` with `rel="deprecation"` pointing at `/docs/route?path=<path>&version=<v>` (the query-parameter shape corrected 2026-07-20 in Architecture) and at `/changes`, so an agent that reads the header lands on the migration path in one hop instead of being told only that something is ending.

    Fragment-scoping is the authoritative representation (decided 2026-07-20): OpenAPI's standard `deprecated` is an **operation-level** boolean with no document-level equivalent, so "a fragment marked `deprecated: true`" has no meaning in the spec — since SEAM's unit of versioning is the whole fragment (a breaking change ships as a new fragment), the marker must be fragment-scoped, hence a SEAM extension. Standard per-operation `deprecated: true` remains valid and is honored for deprecating an individual operation within an otherwise-live fragment; a present `x-seam-deprecated` is equivalent to setting it on every operation in the fragment, and supplies the dates that the bare OpenAPI boolean cannot.
  - `x-adapter` — declarative request/response transforms carried by an **older** coexisting fragment, converting its contract into that of the newest version so SEAM does not need N parallel implementations (decided 2026-07-20; Phase 8 deliverable). Shape: `{targetVersion, request: [...], response: [...]}`, where `targetVersion` names the live `x-api-version` this fragment adapts into and each list is an ordered sequence of transforms drawn from a **closed** vocabulary — rename/move a field by JSON Pointer, default a missing field, drop a field, wrap/unwrap an envelope, rename a query or header parameter. No expressions, no scripting, no code hooks: adapters are data, so a contract migration is a ConfigMap edit and never a gateway binary deploy (see Version Migration Strategy §2). A breaking change that cannot be expressed in this vocabulary gets no adapter — the old fragment simply stays standalone until usage-gated retirement drains it. **An adapting fragment delegates to `targetVersion`'s route rather than forwarding to an upstream of its own** (decided 2026-07-20; full rule in Version Migration Strategy §2): `x-adapter` is mutually exclusive with `x-upstream` / `x-upstream-map`, and `x-vault-path`, `x-inject-as`, `x-upstream-tls`, `x-upstream-plaintext`, `x-credential-probe` and `x-breaker` are likewise taken from the target fragment and are lint errors if declared here — so an adapter can never redirect a credential. Caller-facing fields (schemas, `x-seam-deprecated`, the echoed `X-SEAM-API-Version`) stay this fragment's own; `x-required-scope` is enforced as the **union** of both fragments' requirements; and guards (`x-quota`, `x-cost-per-call`, `x-loop-guard`) are charged to the target's route so coexisting versions share one budget and one loop counter. The transform's output is re-validated against the target's request schema before injection, and a failure is a 500 attributed to the adapter.
  - **Fan-out response envelope** — not a fragment field but the response body an `_all` request returns, specified here because it is a data model two other sections now depend on (decided 2026-07-20; see Multi-Instance Routes, which fixes the status code, and Per-Route Guards, which fixes the billing). The body is a single JSON object with two members and nothing else at the top level:
    - **`instances`** — an object keyed by instance name, one entry per mapped instance including the ones nothing was sent to. Each entry carries `status` ∈ `ok` | `error` | `timeout` | `breaker-refused` | `truncated`, and: for `ok`, `httpStatus` and `body` (that instance's own response body, adapter-transformed and merged in as-is); for `error`, `httpStatus` and the upstream's scrubbed body or SEAM's structured transport error; for `timeout`, the elapsed budget; for `breaker-refused`, the breaker's `openedAt`, `lastError` and `retryAfter` — the same fields the structured 503 carries, so a refused instance is diagnosable without a second call; for `truncated`, `httpStatus`, the omitted body's `bytes`, and no `body` at all (see the size rule below). `breaker-refused` is deliberately its own status rather than an error with a synthetic 503: it means SEAM declined to dispatch, no credential was read, and nothing was charged, which is a materially different fact from an upstream that answered badly.
    - **`summary`** — `{total, dispatched, ok, error, timeout, breakerRefused, truncated}`, integers, always present even on a clean fan-out. `total` is the map's width and `dispatched` is what survived breaker filtering, so `total - dispatched` is exactly what the cost governor did not charge for. A `truncated` instance was dispatched, answered, and was charged — it is counted in `dispatched` and in `truncated`, and not in `ok`.

    Responses additionally carry **`X-SEAM-Fanout-Partial: 1`** whenever the status is 207, so a client streaming or proxying the body can branch before parsing it. Scrubbing runs once over this whole merged artifact against the union of every value injected on the request (Architecture, pipeline) — which is why the envelope is a single buffered object rather than a stream of per-instance bodies.

    **What happens when the envelope exceeds the buffered cap** (decided 2026-07-20 — the fan-out design and the scrubbing size cap collided head-on over this plan's own flagship example, and nothing resolved it. The pipeline requires scrubbing of a fan-out to run "exactly once, over the merged body"; this envelope is therefore "a single buffered object"; the buffered path is capped at `maxBufferedResponseBytes`, default **1 MiB**; and a response needing buffering but unable to be buffered "is unscrubbable by construction: injection is refused there unless the fragment carries `x-unscrubbable: acknowledged`". Now take `/k8s/_all/api/v1/pods?fieldSelector=status.phase!=Running` across **nine** clusters: a single cluster's unfiltered PodList JSON routinely clears 1 MiB on its own. Under the stated rules that fan-out was **unbuildable at the default** — over the cap, unstreamable because it is a merged artifact, and so either refused outright or forced to carry `x-unscrubbable: acknowledged` on the fleet's most credential-dense fragment, nine cluster tokens injected into a route explicitly running unscrubbed. Neither the envelope spec, the pipeline invariant, nor Phase 10 said a word about it). Two rules, composed, and neither touches the scrubbing invariant:
    - **A fan-out envelope has its own cap, `maxFanoutEnvelopeBytes`, sized against map width** — a third independently configurable limit alongside `maxBufferedResponseBytes` and `maxReplayableRequestBytes`, defaulting to `min(maxBufferedResponseBytes × total, 64 MiB)`. This is legitimate where simply raising the response cap would not be: SEAM *constructs* this artifact, knows the map's width before it dispatches, and knows each instance's length as it arrives, so the ceiling is a computed property of a known-N merge rather than a blanket increase applied to every streaming upstream. The absolute ceiling is what keeps a single-replica gateway's memory a number rather than a hope.
    - **Above that cap, instances are dropped to `truncated` rather than the whole fan-out failing.** Instances are admitted in the order their responses complete; the first instance whose body would push the envelope past `maxFanoutEnvelopeBytes` — and every instance after it — is recorded as a `truncated` entry carrying its `httpStatus` and its body's `bytes`, with the body itself omitted. The envelope therefore always fits, always buffers, and is **always scrubbable**, so the fan-out never falls into the unscrubbable path and `x-unscrubbable: acknowledged` is never the price of a wide map. The summary counts stay exact — every instance is still represented, still classified, and still billed as dispatched — which preserves the "what is broken across the fleet" answer even when the bodies behind it will not fit. A response containing any `truncated` entry is a **207** (Multi-Instance Routes owns the derivation) and carries `X-SEAM-Fanout-Partial: 1`, so a naive client cannot mistake a partial answer for a complete one. The fix for a chronically truncating route is to narrow it — a `fieldSelector`, a projection, a per-instance call — and the entry's `bytes` is what tells a caller by how much.

    **`seam lint` warns when a map is plausibly too wide for the cap** *(a Phase 10 check)* — when `x-upstream-map` width × a plausible per-instance body size exceeds the fragment's effective `maxFanoutEnvelopeBytes`, lint flags the fragment for review with the arithmetic shown, in the same spirit as the `x-quota` window warning: not an error, because the truncation behavior above is well-defined and safe, but a condition an author should see at authoring time rather than discover as a routinely half-empty fleet query.
  - `x-unscrubbable: acknowledged` — a fragment- or route-level opt-in, and the **only** accepted value (decided 2026-07-20). Secret-echo scrubbing (Credential Health Sentinel) cannot scan a protocol-upgraded, opaque-binary, or undecodable-encoding response, so on such a route SEAM refuses injection by default; this marker is the explicit, auditable acknowledgement that a secret is being injected into a route whose responses cannot be scrubbed. `seam lint` flags every occurrence for human review rather than passing it silently, and `/config/status` enumerates every route currently running unscrubbed. Absence of the field is refusal, not permission — there is no `false` form, because the field exists to make the exposure enumerable, and an enumerable exposure cannot be expressed by a field nobody has to write.

## Future: Per-Agent Tool Scoping

Near-future requirement (see `docs/notes/objective.md`): configure specifically which routes a given agent/NEEDLE-worker may call, rather than every caller getting every route. Researched in detail in `docs/research/` (the **four scoping-related threads** of the six indexed there — secret-injection prior art, agent tool-access-control state of the art, Tailscale-native identity mechanisms, NEEDLE's current identity model; the other two, version-migration precedent and language/runtime choice, inform Version Migration Strategy and ADR-001 respectively). Combined recommendation, from `docs/research/README.md`:

1. **Identity:** each NEEDLE worker/agent gets its own `tsnet`-embedded ephemeral Tailscale node, tagged (e.g. `tag:needle-worker`), so it's a first-class tailnet peer calling SEAM directly. Required, not optional — today's shared Tailscale Connector/egress proxy pods collapse every worker in a cluster into one indistinguishable caller from SEAM's `WhoIs` point of view. No SPIFFE/SPIRE or custom PKI — Tailscale already provides workload identity for this topology (precedent: Tailscale's own TailSQL/Setec/Golink).
2. **Authorization data:** scope claims (`service:action`, e.g. `k8s-ro:get`, `argocd:sync`) travel in the Tailscale Grant's opaque `app` capability field, read via `WhoIs` on the inbound connection. No separate JWT-issuing auth server needed for tailnet-native callers; fall back to a bearer-claim-in-header model only for callers that can't be first-class tailnet peers. The fallback bucket is real, not hypothetical (decided 2026-07-16): **"foreign" NEEDLE workers running outside the tailnet** will still need these tools, likely ingressing via something like a Cloudflare Tunnel — so the bearer-claim path is a first-class (if secondary) auth mode, and the design must cover how that non-tailnet ingress itself authenticates (e.g. Cloudflare Access service tokens) before any scope claim is trusted. That design is **point 7 below** (written 2026-07-20); until it ships, non-tailnet callers are default-denied, not provisionally trusted.
3. **Enforcement:** every SEAM route is tagged with its required scope(s) (`x-required-scope` in the route fragment); the gateway checks the caller's Grant-supplied scopes against them, default-deny. Never rely on the calling agent's own tool list as the boundary — client-side restriction is UX, not security (OWASP Agentic Top 10 consensus, and a live AutoGen cross-agent-tool-leak issue found in research).
4. **Gap to close first:** NEEDLE currently mints no identity/credential for spawned agents at all (`docs/research/needle-identity-model.md`). This needs new NEEDLE-side work in `run_process` (`~/NEEDLE/src/dispatch/mod.rs:703`) — provision a per-worker `tsnet` identity, inject it via the existing `adapter.environment` passthrough (or a new template variable) — before SEAM-side scoping can be enforced against anything real.
5. **Self-service surface:** authorization must be discoverable, not opaque. `/whoami` returns the caller's resolved tailnet identity, tags, and effective scopes; `/openapi.json` and `/docs` filter to the routes the caller may actually invoke, so agents never spend context on unreachable routes; and a 403 names the exact missing scope plus the Grant snippet that would fix it — authorization debugging becomes "ask the gateway," not "read the tailnet policy file." **The scope-naming 403 applies only where the route is already visible in the caller's own filtered spec** (cross-reference, 2026-07-20): a route the filter removed returns **404, identically to a route that does not exist**, on `/docs/{route}` and on level-2 `/changes` alike — see the scope-filtering rule under `/docs/{route}` in Architecture, which owns this decision. So the 403 covers the visible-but-not-invocable case (right path, wrong method), which is where naming the scope helps the caller and leaks nothing; it never covers a route the caller was not meant to know exists, because that would turn this self-service surface into the enumeration oracle filtering exists to prevent. **That per-method case is exactly what fixes `x-required-scope` at operation level** (decided 2026-07-20; the placement rule and the field's array cardinality are in Open Questions) — a fragment-root-only declaration cannot say "GET needs `k8s-ro:get`, DELETE needs more", so this 403 would have had no case left to describe. **"Wrong guard" is deliberately not one of the cases** (corrected 2026-07-20 — all three statements of this rule previously read "the specific method **or guard**", and no guard-level scope exists anywhere in this plan: `x-loop-guard`, `x-cost-per-call`, `x-quota` and `x-breaker` carry no scope at all, `x-required-scope` is a route-and-operation field, and Phase 13 and Passive Route Health define guard refusals as **429**, **402** and a structured **503**. An implementer would have gone looking for a mechanism that was never specified and either invented one or left a stated 403 case unimplemented). Guard refusals have their own status codes and are not an authorization outcome.
6. **Scope taxonomy:** grows route-by-route as fragments declare `x-required-scope` — no up-front `service:action` catalog (decided 2026-07-16). The live taxonomy must stay dynamic and discoverable: enumerable from the merged spec (e.g. a `/scopes` view) rather than maintained as a separate document. A future SEAM shows different taxonomy slices to different agents, RBAC-style — an agent sees only the portion of the taxonomy its grants intersect, consistent with the scope-filtered spec in point 5. Since discoverability *replaces* a maintained catalog here, `/scopes` is a concrete Phase 7 endpoint, not a figure of speech (decided 2026-07-20): it is **scope-filtered by default, exactly like `/openapi.json`** — returning the scopes the caller's grants actually intersect, each with a one-line meaning and the (likewise filtered) routes that require it, so "what may I do here" is one request that costs no context on scopes the caller can never hold. The **unfiltered** taxonomy is available at `/scopes?all=1`, but is itself gated on a scope (`seam:scopes:read-all`): a complete inventory of every capability across every service SEAM fronts is precisely the reconnaissance that filtering `/openapi.json` exists to withhold, so it is an operator view, never an ambient one. Both forms are derived at request time from **two sources, merged**: the merged spec's `x-required-scope` declarations, and a small **built-in set of control-plane scope declarations compiled into the SEAM binary** (decided 2026-07-20). The built-in set exists because SEAM's own control plane resolves *before* the merged route table (Fragment Toolchain rule 2) and therefore has no `x-required-scope` in any fragment — so under a fragments-only derivation the two scopes an operator most needs to discover, **`seam:ops:read`** (gating `/config/status`, `/health/credentials`, `/health/upstreams`) and **`seam:scopes:read-all`** (gating `/scopes?all=1`), would be exactly the two `/scopes` could not show, while being hardcoded in the binary regardless. Each built-in declaration carries the same fields a fragment-derived one does — the scope, its one-line meaning, and the control-plane paths requiring it — and is labeled `source: builtin` so an operator can tell a capability SEAM owns from one a service declared. It is scope-filtered on the same terms: a caller holding neither sees neither in `/scopes`, and both appear in `/scopes?all=1`. The claim this replaces — "there is no stored catalog, and therefore nothing that can drift" — is corrected to the accurate one: **there is no *separately maintained* catalog. Both sets are computed at request time**, one from the live merged spec and one from the binary's own routing table, so neither can drift from what SEAM actually enforces; what would drift is a hand-written scope document, and there still is none. Adding a control-plane scope is therefore a binary change that updates enforcement and discovery in the same commit, by construction.
7. **Non-tailnet ingress authentication** (decided 2026-07-20) — the design point 2 defers to, written down here because a declared first-class auth mode with no design gets built ad hoc as "trust the `X-Scopes` header," which on a secret-injecting proxy is a trivially forgeable authorization bypass that hands an attacker every credential SEAM holds. **Four rules, none of them per-deployment negotiable — plus one phasing constraint, which is scheduling rather than a rule and is set apart below for that reason** (clarified 2026-07-20; this read "four rules" over five bullets, and on a list whose whole claim is non-negotiability an ambiguous count invites reading the odd one out as advisory). The four rules:
   - **The ingress identity is verified at the gateway, not at the edge.** Foreign workers reach SEAM through a Cloudflare Tunnel fronted by Cloudflare Access; Access issues a service-token JWT, and **SEAM itself validates that JWT on every request** — signature against Cloudflare's published JWKS for the team domain, `aud` matching SEAM's own Access application ID, plus `iss`/`exp`/`nbf`. SEAM never treats "this request arrived on the tunnel listener" as proof of anything: a tunnel is transport, and edge-only verification means anyone who reaches the origin by any other path is authenticated by default.
   - **Scopes are bound to the verified ingress identity and never read from a client header.** The caller's scope set is looked up from the verified token subject (`sub` / `common_name`) against a SEAM-side service-token→scopes mapping — structurally the same move as looking up a tailnet caller's scopes from the `WhoIs` Grant. There is no request header, query parameter, or body field through which any caller, in this mode or any other, can assert its own scopes.
   - **Client-supplied scope headers are stripped before evaluation, not merely ignored.** `X-SEAM-Scopes` and every equivalent are deleted on ingress by the same strip-then-inject rule that removes `Authorization` and `x-inject-as` headers (Architecture), so a forged header cannot be read even by a future code path that mistakenly looks for one, and cannot survive into an upstream request.
   - **Default-deny on the mode itself.** The foreign-worker path is off unless explicitly enabled, and a request presenting no valid Access JWT is rejected *before* route matching, secret lookup, or any upstream contact. The failure mode is a 403 — never "fall through to the tailnet path with no identity resolved."

   And the phasing constraint, which is not one of the four rules — it says *when* this mode ships, not *how* it must behave, and unlike the rules above it is discharged once, by the schedule, rather than enforced on every request:

   - **Phasing: this is Phase 14** (assigned 2026-07-20; dependency edge `7 → 14`). Phase 7 ships the tailnet-native Grant path only and default-denies every non-tailnet caller. The foreign-worker mode is gated on Phase 7's enforcement being real and exercised, because a second identity source is only safe once the enforcement point it feeds already works.

## Version Migration Strategy

NEEDLE workers run perpetually-live sessions — a session may learn the API contract once (fetch `/openapi.json` early) and keep calling routes on that understanding for hours, with no guarantee it ever restarts or refetches the spec. Full design in `docs/notes/versioning.md`; precedent research in `docs/research/version-migration-precedent.md`. **Three** distinct problems (corrected 2026-07-20 — this read "two" from before passive drift signaling was adopted as item 3). The numbering here maps one-to-one onto the section labels in `docs/notes/versioning.md`, so a `§`-reference means the same thing in either document: **§1 = §A** (gateway process deploys), **§2 = §B** (route contract versioning), **§3 = §C** (passive drift signaling). Cite them as "Version Migration Strategy §2" / "versioning.md §B" interchangeably.

1. **Gateway process deploys** (SEAM's own code changes, contract unchanged) — standard Kubernetes rolling-update mechanics: readiness-gated rollout and graceful `SIGTERM` draining. **SEAM runs as a single replica** (amended 2026-07-20 — this bullet previously mandated ≥2 replicas and `PodDisruptionBudget minAvailable: 1`). Every stateful feature in this plan is per-process and in-memory by design — loop-breaker counters and cost-governor budgets (Per-Route Guards), last-2xx tracking and circuit-breaker state (Passive Route Health), the credential sentinel's probe loop — and two replicas behind one Service silently double all of it: a loop-guard threshold of 10 permits 20 identical requests, a `$50` `x-quota` permits `$100` against a metered upstream, `X-SEAM-Budget-Remaining` depends on which pod answered, `/docs` shows a different last-success on each refresh, and the breaker opens on one replica while the other keeps hammering a dead upstream. On the twitterapi.io credit and z.ai quota routes that is real money, so correctness of the guards outranks a second replica of a stateless proxy. The Deployment is therefore `replicas: 1` with `strategy.rollingUpdate: {maxSurge: 0, maxUnavailable: 1}` — a rollout never runs two SEAM pods at once — at the cost of a **real outage window during a binary deploy, on the order of seconds to tens of seconds** (corrected 2026-07-20; this previously claimed a "sub-second gap", which was wrong by one to two orders of magnitude and was the *sole* justification offered for accepting the single-replica tradeoff, so it would have been used to size agent retry behavior at cutover). With `maxSurge: 0` the old pod is fully terminated **before** the replacement is scheduled, so the gap is the sum of `terminationGracePeriodSeconds` plus in-flight drain, pod scheduling, image pull (usually cached), process start, initial fragment merge and OpenBao login, and the first passing readiness probe — **typically 5-30 seconds**, not a blip. The tradeoff itself is unchanged and still holds, because binary deploys are rare and route and contract changes are ConfigMap edits that never restart the pod at all; what changes is what callers must be built to absorb. **The corresponding requirement on callers: any agent calling SEAM must implement retry-with-backoff spanning at least 60 seconds**, and an agent whose retry budget is one second will fail across every SEAM deploy. That is a checklist item in the 6b cutover, not an aspiration. No `PodDisruptionBudget` demanding an available replica is set, since with one replica it would only deadlock node drains. The single-replica invariant is load-bearing: **no feature may be built assuming it can be replicated**, and each stateful section below states its own multi-replica semantics explicitly. If SEAM ever genuinely needs HA, the documented upgrade path is a shared state store for guard counters, quota ledgers, and breaker state (Redis or equivalent) plus leader election for the sentinel — a scoped, later piece of work, not something to be back-doored by raising `replicas`. The credential sentinel's probe loop is leader-elected regardless (see Credential Health Sentinel), so the invariant survives both that future scale-out and any accidental transient overlap.
2. **Route contract versioning** (a route's params/response/injection shape changes in a breaking way) — borrows Kubernetes' API deprecation policy (old and new versions coexist as long as anything calls the old one — this falls out of the existing ConfigMap-per-fragment model for free, a breaking change just ships as a new fragment rather than an in-place edit) and Stripe's adapter model (implement the route once against the newest contract, add a thin request/response adapter per older version still being served, rather than N parallel full implementations). Deprecated fragments emit `Deprecation`/`Sunset` headers (advisory only) and are retired when a per-route-version request-count metric has read zero for a quiet window. **`Deprecation` is emitted from the moment the marker lands; `Sunset` only once a removal date is actually projectable** (decided 2026-07-20) — retirement here is deliberately dateless, so there is no date to emit until the quiet window has opened and its projected end resolves (the window's formula is fixed in Phase 8, which owns it), unless the fragment's author supplied an advisory one. See the `x-seam-deprecated` entry in Data Models, which carries `{since, sunset?}` for exactly this reason. **Zero observed traffic is a necessary condition for retirement; the length of the quiet window is traffic-adaptive with a calendar floor** (clarified 2026-07-20 — this read "usage-gated, never calendar-gated", which was heard as forbidding the calendar floor the recommended composition in `docs/notes/quiet-window-options.md` depends on). The distinction that matters is the one originally intended: no route is ever retired because a date arrived, only because it stopped being called — a calendar term may only ever *lengthen* the wait, never trigger a removal on its own. See Phase 8 for the adopted composition.

   **Where the gap history lives: outside the gateway process, in the metrics store** (decided 2026-07-20). The adopted window (Phase 8, which owns the formula) is traffic-adaptive with a calendar floor, sized off the **observed max inter-request gap on that route-version** — and `docs/notes/quiet-window-options.md` Option B flags the catch — that history "needs to be persistent to be meaningful across gateway restarts." Every other stateful feature in this plan is deliberately per-process, in-memory and restart-scoped (§1, Per-Route Guards, Passive Route Health), and if the gap history were too, each binary deploy would reset both the observed maximum and the quiet clock: retirement would either never converge, or converge on the 7-day floor forever — i.e. silently degrade Option B back into Option A, the option it was chosen over. The resolution is **not** to weaken the in-memory invariant but to observe that this is the one consumer that does not need in-process state at all. §2 already exports a **per-route-version request-count metric** (mirroring Kubernetes' `apiserver_requested_deprecated_apis`), and that time series is already durable, already retained, and already survives deploys in the fleet's existing metrics store. **The retirement evaluator is therefore a query over that series, not a reader of gateway memory:** the observed max inter-request gap is the longest interval between increments of the counter for that (route, `x-api-version`), and "quiet since" is the timestamp of the last increment. The gateway keeps only a live in-process view of the same counter, which may reset freely because nothing depends on it. Three consequences, stated so nobody re-derives them: the evaluator runs as the small out-of-band job Option D already assumes (it is the thing that opens the declarative-config PR), so this adds **no** request-path state, **no** PVC, and **no** second stateful component to a single-replica deployment; gap resolution is bounded below by the scrape interval, which is irrelevant against a 7-day floor; and the metrics store's retention window is now load-bearing — a route-version older than retention reports "insufficient history" and falls back to the 7-day floor rather than inventing a gap, exactly as a cold route-version does. Retirement remains usage-gated throughout: this decides only *where the evidence is read from*, never that a date may trigger a removal.

   **Adapters are declarative transforms in the old fragment's own YAML, never Go code in the gateway binary** (decided 2026-07-20). Stripe's model assumes a route implementation to adapt *toward*; SEAM is a proxy and has no implementation of its own — the implementation is upstream. So the model translates as: the **newest** fragment is the one whose contract matches what the upstream actually speaks today, and every older coexisting fragment carries an `x-adapter` block (see Data Models) declaring the request-in / response-out transforms that convert its older contract into that newest one. Keeping adapters in the fragment is what preserves SEAM's config-only property: a route-contract change stays a ConfigMap edit synced by ArgoCD, with **no gateway binary deploy** — "nothing about the pod changes" (Architecture) holds for contract migrations too. Adapters as gateway Go code would invert that, making every route-contract change a fleet redeploy and dragging per-service transform logic back into exactly the monolithic file the ConfigMap-per-fragment model exists to abolish. The transform vocabulary is deliberately **small and closed** — rename/move a field by JSON Pointer, supply a default for a field the old contract omits, drop a field, wrap/unwrap an envelope, rename a query or header parameter — and there is **no escape hatch into arbitrary code or expressions**. A change that cannot be expressed in that vocabulary is simply not adapted: the old fragment stays mounted as a standalone fragment against its own upstream target and lives out its usage-gated retirement unmodified. Accordingly, **Phase 8 must deliver** the `x-adapter` schema and its transform vocabulary, the request/response transform executor in the proxy path, `seam lint` validation of adapter blocks (including that `x-adapter.targetVersion` names a version that is actually live and coexisting), and the version-selection mechanism below.

   **Adapter resolution: an adapting fragment delegates to its target version's route and owns none of the upstream-facing fields** (decided 2026-07-20). The paragraph above says the newest fragment "is the one whose contract matches what the upstream actually speaks today" and that an *unadaptable* old fragment "stays mounted as a standalone fragment against its own upstream target" — which implies, without ever stating, that an *adapted* fragment does **not** use its own upstream. Left implied, an implementer must invent the whole resolution, and the invented answers have direct security consequences (which secret gets injected) and money consequences (which budget gets charged). The rule, in full:

   - **A fragment carrying `x-adapter` must not declare `x-upstream` or `x-upstream-map`** — the two are mutually exclusive, a lint error and a merge-time quarantine, exactly like `x-upstream` vs `x-upstream-map`. Declaring an adapter *is* declaring "my upstream is whatever `targetVersion` forwards to." The Data Models rule that `x-upstream` is required for an ordinary single-instance route stands unchanged for every fragment without an adapter, including an unadaptable deprecated one, which is precisely why that fragment keeps its own target.
   - **Every upstream-facing field comes from the target version's fragment, never the adapting one:** `x-upstream` / `x-upstream-map`, **`x-upstream-path-template`**, **`x-upstream-strip-prefix`** (added to this list 2026-07-20 — the two path-rewrite fields are upstream-facing by construction, since they are stated in the *upstream's* own path terms, and an adapting fragment permitted to declare either could redirect a delegated request to a different upstream path while `x-adapter`'s closed vocabulary is explicitly barred from touching paths at all), `x-vault-path`, `x-inject-as`, `x-upstream-tls`, `x-upstream-plaintext`, `x-credential-probe`, and `x-breaker` (the breaker keys on the resolved upstream origin, which is the target's — an adapter that could declare its own would fragment one upstream's breaker state across coexisting versions and defeat the guard). An adapting fragment declaring any of them is a lint error rather than a silent override. This is the security-critical half: if an old fragment could name its own `x-vault-path` while delegating to a live route, "ship an adapter" would become a way to point any secret in the allowlist at any upstream, defeating the co-ownership rule in Architecture. One route-version-family, one credential.
   - **Every caller-facing field comes from the matched (older) fragment:** its own request/response schemas — validated at pipeline stage 6 against "the matched version's own declared schema", which is exactly the contract that caller was served — plus its own `x-seam-deprecated`, and the `X-SEAM-API-Version` echoed back to the caller, which is the **old** version, not the target's.
   - **`x-required-scope` is enforced as the union of the two, at pipeline stage 5.** This union is why authorization is a stage of its own *after* route-and-version match rather than part of identity resolution at stage 3: the requirement is not computable until both fragments are known (Architecture, pipeline). The caller's own fragment declares what it always did, but a scope requirement added on the newer version applies as well — otherwise pinning an old `X-SEAM-API-Version` would be a privilege-de-escalation path, letting a caller shed a guard by claiming an older contract. Union is the only direction that cannot be gamed.
   - **Guards are charged to the target version's route, not per version.** `x-cost-per-call` / `x-quota` and `x-loop-guard` are keyed on the **target** (path, method, `targetVersion`), so all coexisting versions of one route draw from one budget and one loop counter. Otherwise N coexisting versions would multiply the spend cap N-fold on a metered upstream, and a looping agent could reset its own counter by changing a header — both of which defeat the guards outright. The loop breaker's normalized request hash is taken **after** the request transform, so two callers on different versions sending semantically identical requests are correctly counted as the same loop. Both properties are what force the adapter transform (pipeline stage 8) to precede guards (stage 9) rather than follow them, as the pipeline previously had it: this bullet's charged-to-the-target rule and the hash rule both consume the transform's output (Architecture, pipeline). Where the old and target fragments both declare guard fields, the target's govern.
   - **The transform's output is re-validated against the target version's request schema, and a failure is SEAM's fault, not the caller's.** Re-validation concludes pipeline stage 8, before guards at stage 9 and secret resolution at stage 10 — so a broken transform can never cause a credential to be injected into a malformed upstream request. (That argument is unchanged by the 2026-07-20 reordering that moved the adapter ahead of guards: injection still follows re-validation, one stage further down.) The response is a **500** naming the adapter, the failing JSON pointer, and both versions, never a 400: the caller sent a valid request against the contract it was given, and reporting a 400 would send an agent to re-read a `/docs/{route}` that already agrees with what it sent. `seam lint` checks the same property statically where it can (that the declared transform sequence carries the old schema into the target schema), so most such defects never reach runtime. The **response** transform's output is not re-validated — SEAM does not validate upstream responses on any path — but it remains subject to the buffered-path and scrubbing rules in Architecture.

   **Selecting among coexisting versions** (decided 2026-07-20). When a breaking change ships as a new **path segment**, selection is the URL and needs no mechanism. When it ships as a new **`x-api-version`** under the same (path, method), the caller selects with an explicit `X-SEAM-API-Version: <v>` **request header** — and when that header is absent, the request resolves to **the oldest still-served version, not the newest**. That default is the only one consistent with the rest of this strategy: a perpetually-live worker that learned the contract hours ago sends no version header at all, so defaulting to newest would break precisely the callers coexistence exists to protect, turning every breaking change into a silent fleet-wide outage. Newly-arriving and re-reading callers are steered forward instead of shoved: `/openapi.json` and `/docs` advertise the newest version as canonical and render older coexisting ones as deprecated, `Deprecation`/`Sunset` and `/changes` do the pushing, and the effective default advances on its own the moment usage-gated retirement removes the drained old fragment. Every response echoes the version that actually served it in `X-SEAM-API-Version`, so a caller is never guessing which contract it is talking to. Note that `X-SEAM-API-Version` is an inbound `X-SEAM-*` header and must therefore be added to the documented exception list in the inbound-header-stripping rule (Architecture), alongside `X-SEAM-Dry-Run`.

   **The grammar and total ordering of `x-api-version`** (decided 2026-07-20). "The oldest still-served version" is load-bearing — get it wrong and the result is the silent fleet-wide outage this default exists to prevent — but nothing defined an ordering over a field exemplified only as the free-form string `v1`. Without one, SEAM cannot know that `v2` succeeds `v1`, cannot order `v10` against `v9` (lexically `v10` sorts *first*), and cannot place a date-form value at all. So the value space is closed:

   - **Grammar: `v<positive integer>`, matching `^v[1-9][0-9]*$`.** No leading zeros, no dotted or suffixed forms, no `v1beta1`, no dates. **Ordering is numeric on the integer** — `v9 < v10` — which is total, decidable without a calendar, and stable under any future addition.
   - **Why integers rather than Stripe's dates**, since §2 cites Stripe's model approvingly: SEAM borrows Stripe's *adapter* idea, not its version namespace, and the two design pressures differ. Dates serve a SaaS shipping continuously to callers pinned at first contact — and per-caller version pinning is explicitly **parked** here (`docs/notes/per-caller-version-pinning.md`), so the property dates buy is one SEAM has deliberately not bought. What SEAM has instead is hand-authored fragments in git, where a date is a value an author types: two fragments can collide on one date, a backdated value silently reorders the default, and a reader cannot tell a contract's date from its authoring date. An integer is checkable at a glance, collides visibly, and matches both the `v1` already used throughout this plan and the Kubernetes API-versioning precedent §2 already borrows its coexistence policy from. The narrower alphabet is the point: this field selects which contract a caller is held to, and a field with one legal shape cannot be mis-sorted.
   - **`_unversioned` sorts strictly oldest** — below every `v<n>`, unconditionally. This is the case that matters most and was previously undefined: every fragment shipped in Phases 3-5 predates versioning and therefore carries no `x-api-version`, so it is keyed `_unversioned` (Fragment Toolchain). The **first** breaking change on any such route ships as `v1` while the incumbent keeps its `_unversioned` key — and because `_unversioned` sorts oldest, an unversioned caller sending no header keeps landing on the contract it already knows, and keeps doing so until that fragment drains and is retired. Any other sort position would make the very first use of this mechanism break every existing caller at once. `_unversioned` is a **reserved key SEAM assigns, never a value an author writes**: a fragment declaring `x-api-version: _unversioned` literally is a lint error and is quarantined at merge, so the reserved slot cannot be occupied deliberately. **That prohibition is on authoring it, not on addressing it**: `_unversioned` names a real served contract and is a valid value on `X-SEAM-API-Version`, on **`/docs/route?version=` specifically** (narrowed 2026-07-20 — a bare "on `?version=`" spanned two endpoints with two incompatible value spaces: **`/openapi.json?version=` takes a *spec* version, an `X-SEAM-Spec-Version` hash, and nothing else**, so `_unversioned` is a **400** there naming which of the two that endpoint expects, per Data Models, `x-api-version`), in SEAM-emitted pointers and echoes, and in a `versions[]` list — see Data Models, `x-api-version` (decided 2026-07-20).
   - **Enforced twice, as everywhere else in this plan:** `seam lint` rejects any non-conforming `x-api-version` at the CI gate, and the gateway re-checks the pattern at merge time and quarantines the whole fragment, never trusting lint to have run. Ordering is evaluated **per (path, method)** — versions of different routes are never compared, so nothing requires version numbers to be globally synchronized across services.
3. **Passive drift signaling** (helping perpetually-live callers notice contract change mid-session) — every response carries an `X-SEAM-Spec-Version` header (a hash of the currently-served merged spec), so a worker that cached the contract hours ago sees the version change on a call it was already making, at no extra request cost. The catch-up diff is **progressively discoverable** (decided 2026-07-16), giving schema-diff fidelity without the token cost of dumping full schemas: level 1, `/changes?since=<version>`, returns a token-cheap route-level list — path, verb, change kind, one-line summary, and **two drill-down links, not one** (respecified 2026-07-20, matching the `/docs/{route}` bullet in Architecture): **`diffUrl`** → `/changes?since=<version>&route=<path>`, the level-2 field-level schema diff, answering *what changed*; and **`docsUrl`** → `/docs/route?path=<path>&version=<v>`, answering *what the contract now is* (the normalization-safe query form fixed in Architecture 2026-07-20; the older `/docs/{route}?version=<v>` spelling this clause carried is superseded). They are different questions and an agent catching up mid-session wants both — the diff to decide whether it cares, the docs to rewrite its call if it does. Level 2, `/changes?since=<version>&route=<path>`, returns the full field-level schema diff for that one route; prior full specs stay fetchable at `/openapi.json?version=<v>` — where `<v>` is an **`X-SEAM-Spec-Version`**, a hash of a whole prior merged spec, and never an `x-api-version` (clarified 2026-07-20; the two value spaces are separated once and for all in Data Models under `x-api-version`, and `/openapi.json?version=` is bound to the spec-version one exclusively). An agent notices the header change, pulls level 1, and drills into only the routes it actually uses. Backed by a ring buffer of the last N merged specs; this gives the advisory `Deprecation`/`Sunset` headers the actionable catch-up path they lacked, and usage-gated retirement remains the enforcement backstop. Full design in `docs/notes/versioning.md`.

   **Archived specs and diffs are stored unfiltered and filtered at serve time** (decided 2026-07-20) — the reconciliation between this section and the scope-filtered `/openapi.json` of Phase 7 (Per-Agent Tool Scoping point 5). The ring buffer holds the **unfiltered global** merged spec, one entry per spec version, and explicitly *not* one entry per caller-grant combination: that would multiply the buffer by the number of distinct grant sets in the fleet and is not a cost this design will carry. Instead, **every read path applies the caller's *current* scope filter at serve time.** `/openapi.json?version=<v>` returns the archived spec reduced to the routes that caller may invoke today; `/changes?since=<version>` (level 1) omits routes the caller cannot see; `/changes?since=<version>&route=<path>` (level 2) returns **404 for a route outside the caller's grants — byte-identically to a route that has never existed** (aligned 2026-07-20, superseding this clause's earlier "403 naming the missing scope"; the governing decision is the scope-filtering rule under `/docs/{route}` in Architecture, and level 2 is bound by it for the same reason: a 403/404 split here is an enumeration oracle that lets a caller reconstruct the withheld route inventory of every service SEAM fronts, one archived-diff probe at a time). The scope-naming 403 of point 5 survives on this endpoint only in the visible-but-not-invocable case — a route the caller *can* see in its own filtered spec but lacks the scope for the specific **method** ("or guard" struck 2026-07-20: no guard-level scope exists anywhere in this plan, and guard refusals are 429/402/503 rather than an authorization outcome — see Per-Agent Tool Scoping point 5, which owns the correction). As on `/docs/{route}`, the 404 body names `/whoami` and the caller's `X-SEAM-Scope-Version`, and SEAM logs the resolved route server-side so an unreachable-but-believed-real route stays answerable off the request path. An archived spec is **never** served verbatim — serving it raw would make the archive a one-request bypass of the entire filtering guarantee, disclosing the full route inventory of every service SEAM fronts to anyone who asks for yesterday's version.

   `X-SEAM-Spec-Version` hashes the **global** merged spec, not the caller's filtered view. It is a change *signal*, not an authorization surface, and a per-caller hash would fragment the version namespace so thoroughly that a `since=` value could not be reasoned about across two callers, across logs, or across the ring buffer at all. The deliberate consequence is that a caller whose *grants* changed can see routes appear or disappear in a diff for reasons having nothing to do with the spec changing — and that is signaled explicitly rather than left to look like contract drift. Level-1 change kinds therefore separate **contract** changes (`added` / `removed` / `params-changed` / `response-changed` / `deprecated`) from **visibility** changes (`granted` / `revoked`), and responses carry `X-SEAM-Scope-Version` — a hash of the caller's effective scope set — alongside `X-SEAM-Spec-Version`, with `/whoami` reporting the same value. A caller may pass `&scope-since=<scope-version>` to `/changes`; SEAM retains the scope set behind each `X-SEAM-Scope-Version` it has issued (a small hash→scope-set map, which is SEAM's own derived data and cheap, unlike a per-caller spec archive), so it can diff the two filtered views and label each route's delta as contract or visibility. Omitting `scope-since` yields a diff flagged `scopeChanged: unknown`, telling the agent it cannot distinguish the two causes for itself. Net effect: a worker can always tell "the contract moved" from "my access moved" without guessing.

   **Both stores here are in-memory and restart-scoped, and both degrade to an already-defined behavior rather than to an error** (decided 2026-07-20 — stated because §1, Per-Route Guards and Passive Route Health all repeat that state is per-process and restart-scoped, while this section introduced two stores and said nothing, leaving an implementer to guess at persistence for a surface whose entire purpose is serving sessions that outlive a deploy).

   - **The ring buffer of prior merged specs is in-memory, bounded to the last N spec versions, and lost on restart.** It is not persisted, and deliberately so: it is a cache of derived data, and the alternative — a PVC or an external store — buys durability by adding the first piece of request-path state to a deployment whose single-replica invariant exists precisely to avoid that. What makes this safe is the fallback, which `docs/notes/versioning.md` §C already specifies and this plan previously omitted: **a `since=` value that is unknown, evicted, or from a pre-restart buffer is not an error.** `/changes` answers **200** with `sinceKnown: false`, no diff, and a pointer to the current full spec (`/openapi.json`, plus the caller's current `X-SEAM-Spec-Version`), telling the agent plainly that it must re-read rather than catch up incrementally. That is strictly the pre-`/changes` behavior — refetch the spec — so the worst case of a deploy is that live workers lose the *cheap* catch-up path for one cycle, never the *correct* one, and no `since=` value a worker holds can ever produce a wrong answer. Should perpetual history ever be wanted, note the cheap escape hatch: prior merged specs are **deterministically reconstructible from git history in declarative-config**, since the fragments are the inputs and the merge is deterministic — so the archive can be rebuilt offline by an out-of-band job without the gateway ever holding it. That is an available upgrade, not a runtime dependency; nothing in the request path may be built assuming it.
   - **The `X-SEAM-Scope-Version` → scope-set map is in-memory and explicitly bounded** — it had no stated eviction rule, and an unbounded map keyed by a hash of caller-controlled-adjacent input is a slow leak. Retention is: **the most recent 4 distinct scope versions per caller identity, evicted after 24 hours idle for that identity, under a global LRU cap on total entries.** Four covers the realistic case (a caller's grants change rarely, and only the last few are diffable against), 24 hours comfortably exceeds a perpetually-live session's useful catch-up horizon, and the global cap makes the memory ceiling a number rather than a hope. A `&scope-since=` value that is unknown, evicted, or pre-restart yields exactly the behavior already defined for **omitting** it — the diff is flagged `scopeChanged: unknown` — so this failure mode needs no new client handling, and a restart simply means every caller gets `scopeChanged: unknown` until it has been issued a fresh scope version. Current-scope *filtering* is unaffected by any of this: it is computed from the caller's live grants on every request and never read from this map, so a restart can never widen what a caller sees.

   **Everything this section introduces is phased, and the phase entries own the assignment** (decided 2026-07-20 — recorded because §3 grew a whole subsystem, and an unassigned deliverable is an unbuilt one). The split is: **Phase 7** owns the scope-identity half, because it is the phase that first makes a scope set exist; **Phase 8** owns the drift-signaling half, because it is the phase that first makes a spec archive exist; and the **`7 → 8` forcing edge** between them is recorded in the dependency-edge list under Implementation Phases. The per-deliverable enumeration lives in the **Phase 7 and Phase 8 entries** and is deliberately not duplicated here, so a future correction has exactly one place to land.

## Credential Health Sentinel

SEAM holds both the secret (`x-vault-path`) and a way to test it (the upstream), so it can detect dead credentials before an agent does. Recurring pain this targets: cloudspace-admin OIDC tokens expiring every ~3 days, and a dead ESO static token going unnoticed for 16 days.

- A background loop validates each fragment's credential against its upstream via the fragment's `x-credential-probe` (a designated safe authenticated request), on a per-fragment cadence.
- `/health/credentials` reports per **(fragment, instance)** — the reporting key, since one multi-instance fragment can hold N distinct credentials (see Data Models); single-instance fragments report under the reserved instance `_default` — giving last verified, current status, known expiry — never secret values.
- Self-healing on rotation: an upstream `401` invalidates the cached secret, refetches from OpenBao, and retries once (safe for any method — a 401 means the upstream rejected the request before processing it). **The retry needs the request body, so retry eligibility is bounded by an explicit request-body buffer** (decided 2026-07-20 — stated because ADR-001 treats streaming passthrough as a hard requirement and this plan set a buffering cap only for *responses*, leaving an implementer to invent whether a request body can be replayed at all, and to silently not retry when it cannot). **The buffer is a Phase 2 deliverable; the retry and the error envelope below are Phase 12's** (assigned 2026-07-20, on exactly the reasoning that put scrubbing in Phase 2 — see the scrubbing bullet further down): the tee belongs with inbound request handling, beside strip-then-inject and the secret cache, and it has **two** consumers rather than one — Phase 13's loop-breaker hash canonicalizes the request body and therefore cannot be built without it, and Phase 13 ships at ship-order position 11 against this sentinel's position 14. Leaving the buffer here would have left the earlier phase depending on an unbuilt one; leaving `maxReplayableRequestBytes` unassigned entirely would have made a named configuration knob nobody's deliverable. SEAM buffers the inbound request body up to **`maxReplayableRequestBytes`, default 1 MiB** — an **independently configurable limit** that merely shares a default with the response-scrubbing cap `maxBufferedResponseBytes`, and does not track it (specified as two settings 2026-07-20; this previously read "the same 1 MiB cap used for response scrubbing" and was cited elsewhere as a fixed constant, leaving it unclear whether one knob moved both. It matters: raising the response cap for a chatty upstream would otherwise silently change 401-retry eligibility *and* loop-hash canonicalization for requests, two behaviors nobody was tuning). It tees the body upstream as it streams, and discards it as soon as the response status is known. Within the cap the retry is a genuine replay: the buffered bytes are re-sent, with headers re-stripped per the inbound-sanitation rule and the freshly-refetched secret re-injected. Above the cap — and for any body SEAM cannot replay at all, such as a request-side protocol upgrade or an unbounded chunked stream with no declared length — the body streams through unbuffered and **the request is not retried**. That case is reported rather than silently degraded: the cache is still invalidated and the secret still refetched, so the caller's *next* request succeeds, and SEAM answers with a structured error envelope naming the condition **`credential-refresh-not-retried`**, carrying the upstream's own status and (scrubbed) body plus the plain statement that resending the request will now use the refreshed credential. The self-healing guarantee is therefore conditional, and — the point of naming it — the condition is one the caller can actually detect.
- **Probe traffic is tagged, and its exclusions are per-consumer rather than blanket** (decided 2026-07-20). The sentinel issues a byte-identical authenticated request to every probed fragment on a fixed cadence, which is indistinguishable — to three other subsystems that consume exactly that signal — from the pathology each of them exists to catch. Every sentinel-originated request therefore carries an **in-process origin tag** set by the probe loop itself, never a wire header: all `X-SEAM-*` request headers are stripped on ingress (Architecture), so the tag cannot be forged and no caller can exempt itself from a guard by claiming to be a probe. Each consumer then decides for itself, and they do not all decide the same way:
  - **Excluded from the per-route-version request-count metric** (Version Migration Strategy §2, Phase 8). A deprecated fragment keeps its `x-credential-probe`, so without this exclusion SEAM's own probes hold the retirement counter above zero forever, the quiet window never opens, and **no probed fragment can ever retire**. For the same reason a probe can never reset a quiet window or cancel a brownout (Phase 8 (C)) — only a real caller does.
  - **Excluded from the loop-breaker counter** (Per-Route Guards). A fixed-interval, byte-identical request is precisely what that guard trips on; unexcluded, the sentinel 429s its own probes, and before Phase 7 — where the key degrades to (route, hash) — it would 429 every real caller of the probed route alongside them.
  - **Excluded from quota accounting and from `X-SEAM-Budget-Remaining`.** A probe on a route carrying `x-cost-per-call` would otherwise draw the route-wide budget down on a timer, on exactly the metered upstreams (twitterapi.io credits, the z.ai quota) SEAM was installed to protect. The exclusion is from SEAM's *governor*, which is a runaway-spend brake and not an accounting ledger (Per-Route Guards) — the upstream may well still meter the call, so the probe's own draw is a fixed, known rate of one call per `probeInterval`, and `/health/credentials` reports that rate per (fragment, instance) so the spend is enumerable rather than invisible. On a metered route, prefer a probe the upstream does not bill at all and size the interval against the meter.
  - **Counted, deliberately, in the last-2xx passive-health signal and in circuit-breaker state** (Passive Route Health). This is the one consumer for which probe traffic is the whole point: that section's claim that probe traffic "keeps at least one authenticated route per upstream warm, bounding staleness" is only true if probes count. `/docs` and `/health/upstreams` label a last-success that came from a probe as `source: probe`, so "verified working" is never silently a claim about synthetic traffic alone.
  - **A deprecated fragment keeps probing** for as long as it is still served: its remaining callers are the ones coexistence exists to protect, and a dead credential there is an outage for them. Because probes are excluded from the retirement metric, continuing to probe cannot delay the retirement it would otherwise block. Probing stops when the fragment is removed. An **adapting** fragment never probes at all — it declares no `x-credential-probe` and holds no credential of its own (Version Migration Strategy §2); its target version's probe covers the single credential the version family shares.
- **The probe loop is leader-elected, always** (decided 2026-07-20). SEAM runs single-replica (Version Migration Strategy §1), so today exactly one process holds the loop — but the loop is the one stateful feature whose duplication is visible *outside* SEAM: two copies double authenticated probe traffic against credentials that several upstreams rate-limit, which turns a health check into the thing that breaks the credential. So the loop acquires a Kubernetes `Lease` before probing and drops it on `SIGTERM`, and a process that does not hold the Lease serves traffic normally but probes nothing. This costs almost nothing at one replica and makes both the future shared-state HA path and any accidental transient two-pod overlap safe by construction. `/health/credentials` is served by every replica from its own last-known results, which is why the endpoint reports `lastVerified` timestamps rather than implying live truth.
- **Secret-echo scrubbing belongs to the injection guarantee, not to this sentinel — it is a Phase 2 deliverable, not Phase 12** (decided 2026-07-20). Some upstreams echo the token back in an error body; if SEAM proxies that through, the agent sees the secret and the plan's single core guarantee ("the agent never sees the secret", Overview) is void. Injection ships in Phase 2 and agents are cut over in Phase 6b, so scheduling the scrub with the sentinel in Phase 12 would leave a multi-phase window in which the guarantee is simply false for any echoing upstream. Scrubbing therefore ships **with injection, in the same phase**, and applies to every injected route whether or not that route declares an `x-credential-probe`. Mechanics, stated because "scan the response" is not implementable as written against the streaming and WebSocket passthrough ADR-001 treats as a hard requirement:
  - **Buffered responses** (known `Content-Length` under **`maxBufferedResponseBytes`, default 1 MiB** — one of the two independent limits named above, the other being `maxReplayableRequestBytes`): decompress if `Content-Encoding` is one SEAM can decode, scan for the exact injected value(s) for that request, redact to `[REDACTED-BY-SEAM]`, re-encode. Response headers and trailers are scanned on the same terms — an echoed token in a `WWW-Authenticate` or a vendor debug header leaks exactly as well as one in a body.
  - **Chunked and event-stream responses:** scanned incrementally with a carry-over overlap — SEAM holds back the trailing `len(secret) - 1` bytes of each chunk and prepends them to the next, so a secret straddling a chunk boundary is still matched. This preserves streaming (no full buffering) and bounds added latency to one chunk's worth of tail. Where multiple secrets could be present, the overlap is the longest of them.
  - **Unscannable responses** — protocol upgrades (WebSocket and anything else that leaves HTTP framing), opaque binary bodies, and content encodings SEAM cannot decode — **cannot be scrubbed, and SEAM does not pretend otherwise.** On such a route, injection is refused by default: the fragment must carry `x-unscrubbable: acknowledged` to opt in, `seam lint` flags it for human review, and `/config/status` lists every route currently running unscrubbed so the exposure is enumerable rather than discovered. A route that both injects a secret and upgrades the connection is a deliberate, visible exception — never a silent one.
  - Scrubbing is **defense in depth, not the primary control.** It cannot catch a secret the upstream returns transformed (truncated, re-encoded, hashed), so it never licenses treating an echoing upstream as safe; such an upstream is a bug to report upstream and a candidate for a narrower credential.

## Per-Route Guards: Loop Breaker and Cost Governor

SEAM is the sole chokepoint for metered upstreams (twitterapi.io per-call credits; the z.ai/GLM quota that has no queryable API) and for runaway agent retry loops (precedent: NEEDLE re-dispatching one failing bead 310×/$500). Guards are **configured individually per endpoint** in the route fragment — there are no SEAM-wide defaults, and a route without guard fields gets no guard.

**Guard state is per-process, in-memory, and single-replica by construction** (decided 2026-07-20). Counters, budget ledgers, and their windows live in the gateway process and are lost on restart — which is safe only because SEAM runs exactly one replica. **A second replica would double every threshold in this section** and make `X-SEAM-Budget-Remaining` depend on which pod answered — the worked numbers and the full rationale live in Version Migration Strategy §1, which owns that decision and was amended for this reason. Guards are therefore never to be built assuming replication, and raising `replicas` is not a valid operational response to load; if HA is ever required, the shared quota/counter store named in §1 lands first. Restart-scoping is deliberate and stated so callers can rely on it: a SEAM restart resets budget windows and loop counters, so budgets are a runaway-spend brake, not an accounting system of record.

- **Loop breaker** (`x-loop-guard`): counts repeated identical requests keyed by (caller, route, normalized request hash); exceeding the route's threshold returns a structured 429 telling the agent, in plain language, that it has sent the same failing request N times and should stop and re-read `/docs/{route}`. Before Phase 7 identity exists, callers behind shared egress are indistinguishable, so the key degrades to (route, hash) — thresholds must be set with that in mind. **This ships in Phase 13 without waiting for identity** (decided 2026-07-20, resolving the corresponding Open Question): the pathology being caught is one agent replaying a byte-identical failing request, and the degraded (route, hash) key *aggregates* callers rather than splitting them, so it is strictly more conservative — it trips earlier than the identity-aware version, never later, and can never let a loop through that per-caller keying would have caught. The failure mode it does introduce is collateral: a second, innocent caller sending the same request can be 429'd by the first caller's loop. That is bounded and acceptable because the guard is opt-in per route (no `x-loop-guard`, no guard), thresholds on shared-key routes are set with headroom for legitimate concurrent repeats, and the 429 body states plainly that the guard is currently route-wide rather than per-caller so an agent can tell "I am looping" from "someone else is." Phase 7 narrows the key to (caller, route, hash) with no change to fragment config or thresholds' meaning — it only removes the collateral.

  **What "normalized request hash" means** (defined 2026-07-20 — the term carried the whole discriminating burden of this guard and was never specified, which decides whether it fires at all: with query strings included verbatim, an agent retrying with reordered parameters evades the guard entirely; with `X-SEAM-Dry-Run` included, a dry-run and its real counterpart hash apart. It matters most before Phase 7, where the key is only (route, hash)). The hash is taken over, in this order: the **uppercased method**; the **matched OpenAPI path template** together with its **resolved path-parameter bindings** sorted by name — the template alone would collapse every cluster of a multi-instance route into one counter, and the raw path would split `%2F`-vs-`/` spellings of the same request; the **query parameters sorted by name then value**, percent-decoded before sorting so encoding variants collapse; and the **canonicalized body** — JSON bodies in a canonical form (keys sorted, insignificant whitespace removed, RFC 8785-style), non-JSON bodies as raw bytes, an absent body as empty. **Everything else is excluded**, explicitly: all `X-SEAM-*` headers (so dry-run, version selection, and their real counterparts hash identically), all other caller-supplied headers, every header or query parameter SEAM injects or strips, and the credential itself. Headers are excluded wholesale rather than filtered, so no future injected header can accidentally become part of the key. Two consequences already stated elsewhere follow from this definition and are repeated here so nobody re-derives them: the hash is computed **after** the adapter request transform, so two callers on different `x-api-version`s sending semantically identical requests count as one loop (Version Migration Strategy §2); and a body larger than the replay cap `maxReplayableRequestBytes` (default 1 MiB — Credential Health Sentinel, where it is specified as a limit independent of the response-scrubbing cap, so tuning one never silently moves this behavior) is hashed over its streamed bytes without canonicalization, which is strictly coarser and can only cause the guard to under-match, never to over-match.
- **Cost governor** (`x-cost-per-call`, `x-quota`): per-caller budgets on routes that cost money; responses carry `X-SEAM-Budget-Remaining` so agents can self-throttle. **Both fields are objects carrying an explicit `unit`, and `x-quota` carries an explicit tumbling `window` that any restart resets** (decided 2026-07-20) — the shape, the rule that the two units must match, the 7-day lint ceiling on `window`, and the four-field `X-SEAM-Budget-Remaining` format (`amount`, `unit`, `window`, `resets`) are specified once in Data Models and deliberately not restated here. The `$50` figures used illustratively in this section and in §1 are shorthand for `{amount: 50, unit: usd, window: <duration>}`; SEAM converts between no two units, so a twitterapi.io budget is denominated in credits and a z.ai one in that upstream's own quota units. Doubles as the usage meter z.ai never shipped. Per-caller precision also sharpens with Phase 7. **Before Phase 7, `x-quota` degrades symmetrically with the loop breaker: it is enforced as a single route-wide budget** (decided 2026-07-20) — the key is (route) alone, every caller draws from one shared pool, and the pool is what `x-quota` names. Stating this is not pedantry: "per-caller budget" with no callers is unimplementable, and an implementer left to invent the degradation could plausibly choose per-source-IP (meaningless behind shared egress) or, worse, no enforcement at all — on the twitterapi.io credit and z.ai quota routes that is the difference between a capped spend and an uncapped one. Consequently `X-SEAM-Budget-Remaining` reports the **shared remainder**, and callers must not treat it as their own allowance: another agent can consume it between one response and the next request, so it is a fleet-wide fuel gauge, not a reservation. The guard is deliberately fail-closed — an exhausted route-wide budget **402s** every caller, including well-behaved ones — because the alternative on a metered upstream is spending money SEAM was installed to stop spending. **The status code is 402 and only 402** (fixed 2026-07-20 — this read "402/429", while Multi-Instance Routes enumerates SEAM's own refusals as "validation 400, authorization 403/404, guard 429, quota 402" and Phase 13 follows that. An alternation is not a stylistic choice here: agents branch on status codes, this plan's whole premise is that they self-throttle from SEAM's responses, and a caller written against 429 treats a 402 as an unhandled error. Worse, the two carry **opposite** retry semantics — 429 says retry later, 402 says stop — so an implementer picking either one at random inverts the behavior the guard is trying to produce. **429 stays the loop breaker's alone.**) **The 402 carries `Retry-After`, set to the seconds remaining until the current window's `resets` instant** — a value `X-SEAM-Budget-Remaining` already computes, so it costs nothing to emit and converts "stop" into "stop until this time", which is the only form an agent can act on without either hammering or giving up. `X-SEAM-Budget-Remaining` is carried on the 402 itself as well, so the refusal shows `amount=0` alongside the instant the budget returns. Phase 7 splits the pool per caller with no fragment change; `x-quota` is then read as the per-caller figure it always claimed to be. Note that an `_all` fan-out (Multi-Instance Routes) is billed as one call per instance actually dispatched, not one call, so a fan-out route's `x-quota` must be sized against the map's width.

  **The accounting point, stated once and generally** (decided 2026-07-20 — the `_all` clause above was the only case ever specified, and it implies dispatch-time billing without generalizing it; the unstated cases decide real spend). **A call is charged at dispatch, per upstream request actually issued.** Everything else falls out of that one rule:
  - **A request the upstream rejects still charges**, 5xx included. SEAM cannot know whether a metered upstream counted the call it just served, and on twitterapi.io credits or the z.ai quota the conservative direction is to assume it did — a governor that refunds on failure is a governor a failing loop can run for free.
  - **The 401-triggered refetch-and-retry charges twice**, because two upstream requests were issued. It is a real double draw and is not smoothed over.
  - **Anything refused before dispatch charges nothing**, because nothing was issued: a circuit-breaker 503 (Passive Route Health), a validation 400, a loop-breaker 429, a quota refusal itself, and a dry-run. This is the same boundary the pipeline already draws — guards sit at stage 9, dispatch at stage 11.
  - **An `_all` fan-out charges once per instance actually dispatched** — now a consequence of the rule rather than an exception to it. Instances the breaker refuses charge nothing, so a partly-open fan-out costs less than a full one.
  - **Sentinel probes charge nothing against the caller-facing budget** (Credential Health Sentinel), which is the one deliberate departure from "every issued request charges" and is documented there with its own reporting.
- **Dry-run** (`X-SEAM-Dry-Run: 1` request header): validate a request against the spec and return the verdict without injecting a secret, proxying, or spending quota — agents can test their contract understanding against expensive routes for free. **A dry-run also does not increment the loop-breaker counter** (decided 2026-07-20, completing the pipeline's stage-7 short-circuit): dry-run is offered as the *safe* way to iterate on a route, so counting repeats would 429 a caller for doing exactly what this plan recommends — iterating on one request's shape until it validates is the expected use, and it is byte-near-identical by construction. Dry-runs are still rate-limited by the ordinary transport, and they contact no upstream, so nothing about them is worth spending a guard on.

## Multi-Instance Routes and Fan-Out

Many upstreams are N near-identical instances of the same resource — the per-cluster kubectl-proxies all expose a byte-identical API. Instead of N copy-paste fragments, one fragment declares an `{instance}` path parameter (e.g. `/k8s/{cluster}/...`) resolved against an `x-upstream-map` lookup table, collapsing the config N:1. Each map entry is an object, **whose authoritative key set is specified once in Data Models** (`x-upstream-map`) and is not restated here — the short list this sentence used to carry was one of four disagreeing copies. What it exists for is the point: near-identical APIs routinely need per-instance credentials of differing kind and rotation cadence, and instances can differ in transport policy and in whether they are plaintext at all.

- The reserved instance `_all` fans the request out to every mapped upstream concurrently and returns one merged result labeled by instance — e.g. `/k8s/_all/api/v1/pods?fieldSelector=status.phase!=Running` answers "what's broken across the entire fleet" in one call.
- **Breaker state is per instance, not per fragment** (decided 2026-07-20): each `x-upstream-map` entry is its own upstream origin and gets its own circuit breaker under the policy in Passive Route Health, tunable by the entry's own optional `breaker` key (Data Models owns the full entry shape). One dead cluster therefore opens exactly one breaker and the other eight keep serving — without this, a single unreachable cluster would fail every instance of a collapsed fragment, and the N:1 collapse would have traded config duplication for a shared failure domain. An `_all` fan-out skips instances whose breaker is open, reports them as breaker-refused entries distinguishable from live errors, and is charged nothing for them (Per-Route Guards).
- **Partial-failure semantics for fan-out: 207 Multi-Status with per-instance result entries; fail-closed is rejected** (decided 2026-07-20, closing the corresponding Open Question, which this plan had already half-answered twice without noticing). The question listed fail-closed — any instance error fails the whole request — as a live candidate, but two decisions elsewhere had already foreclosed it: the breaker rule immediately above specifies that `_all` "skips instances whose breaker is open, reports them as breaker-refused entries distinguishable from live errors", which is a per-instance error-entry response body and is expressible only under fail-open or 207; and Per-Route Guards' "a partly-open fan-out costs less than a full one" presupposes that a partly-successful fan-out is a valid outcome that gets billed. An implementer choosing fail-closed would have contradicted both. It is also wrong on the merits for this route's whole reason to exist: `/k8s/_all/api/v1/pods?fieldSelector=status.phase!=Running` answers "what is broken across the fleet", and a fleet with one broken cluster is precisely when the answer is wanted — fail-closed makes the query fail exactly when it matters, and makes one dead cluster hide the other eight, restoring the shared failure domain the per-instance breaker was added to abolish.
  - **207 rather than fail-open 200**, because the summary must survive a naive client. An agent that checks `response.ok` and moves on is the expected consumer, and under a 200 a fan-out in which eight of nine instances failed is indistinguishable at the status line from a clean one — hard-to-miss summary counts in the body are only hard to miss for a caller that reads the body. 207 puts the partiality where no consumer can skip it, while remaining a 2xx, so the response body is still delivered and still useful.
  - **A fan-out that outgrows the buffered cap truncates instance bodies; it never fails and never runs unscrubbed** (decided 2026-07-20; the rule and its two limits are specified once in Data Models under "fan-out response envelope"). The collision was real and sat on this section's own flagship example: `/k8s/_all/api/v1/pods` across nine clusters will routinely exceed `maxBufferedResponseBytes` (default 1 MiB), while the pipeline requires the merged envelope to be scrubbed exactly once as a single buffered object — which, under the rules as they stood, made the plan's showcase route either refusable or dependent on `x-unscrubbable: acknowledged` across nine injected cluster credentials. The resolution keeps both the scrubbing invariant and the route: the envelope gets **its own width-sized cap, `maxFanoutEnvelopeBytes`** (default `min(maxBufferedResponseBytes × total, 64 MiB)`, legitimate here because SEAM builds the artifact and knows both N and each instance's length), and instances that would overflow it are recorded as **`status: truncated`** — body omitted, `httpStatus` and `bytes` kept, still counted in `dispatched`, still billed. The envelope always fits and is always scrubbable. Streaming per-instance bodies with per-instance scrubbing was considered and rejected: it would require amending the pipeline's "exactly once, over the merged body" invariant, which is what stops one instance's echoed credential from being relocated by the merge into a region no per-instance scrubber saw — and that invariant is not to be amended implicitly, in a size-limit decision, to make one route fit.
  - **The status code is derived, never authored:** **200** when every dispatched instance returned 2xx and none was breaker-refused **or truncated**; **207** when any instance errored, timed out, was breaker-refused, **or was truncated** — a truncated instance is a partial answer, and 207 is exactly the signal a client must not be able to skip; and — the one case that is not a fan-out result at all — **the structured 503 of Passive Route Health when every instance was breaker-refused and nothing was dispatched**, naming each refused instance and its `Retry-After`, since no upstream was contacted and nothing was charged. SEAM's own refusals (validation 400, authorization 403/404, guard 429, quota 402) keep their own status codes and never appear as 207: those resolve at stages 5-9, before the fan-out boundary, and describe the request rather than the instances.
  - **The response envelope shape is specified in Data Models** ("fan-out response envelope"), so instance results, the summary counts, and the `X-SEAM-Fanout-Partial` header have one authoritative definition.

## Passive Route Health — Last-Success Tracking

`/docs` shouldn't just describe routes; it should demonstrate them. The signal is passive, derived from live traffic rather than synthetic probes:

- The gateway records the timestamp of the most recent successful (2xx) response per route (its "last-200"). `/docs` renders it per path — "last succeeded 2m ago" — an honest signal distinguishing verified-working from merely-documented. **Three states are rendered, not two** (decided 2026-07-20, superseding the two-state rendering of 2026-07-16). The original decision was sound in what it avoided — "never succeeded" reads as broken for a freshly added route nobody has called yet — but it conflated two distinct states under one string, and it did so in the case that matters most: the condition tested was *no successful call* while the text asserted *no attempt*, so a route called fifty times since restart and failing every one of them rendered "no attempt since last restart" and read as untested-and-new when it actually meant consistently-broken. That inverts this section's entire stated purpose on the single worst route in the fleet. So the render is keyed on attempts and successes separately:
  - **No attempt since restart** → "no attempt since last restart". The freshly-added-route case, and the only one the original wording described correctly.
  - **Attempted, none successful** → "no success in N attempts since last restart", carrying the **last error** alongside it — which costs nothing to surface, since the circuit breaker already tracks exactly that value per upstream origin for its own structured 503. This is the state that was previously invisible, and it is the one an operator most needs to see.
  - **At least one success** → "last succeeded 2m ago", unchanged, with `source: probe` labeling where the success came from sentinel traffic rather than a real caller (Credential Health Sentinel).

  The last-success value itself stays **intentionally empty** in the first two states rather than being filled with a sentinel timestamp; it is the surrounding text that now distinguishes them. `/health/upstreams` renders the same three states on the same terms. Tracking is in-memory and restart-scoped, deliberately not persisted — which is why every one of these strings is explicit that it is scoped to the current process lifetime.
- `/health/upstreams` aggregates the same data per upstream into a single status page for the whole mesh: most recent success across the upstream's routes plus current breaker state.
- Per-upstream circuit breaker: consecutive failures open the breaker, and callers get a structured 503 naming the upstream, when it started failing, the last error seen, and a retry-after — so agents stop burning tokens retrying a dead service.

  **The breaker's policy, and why it has a default at all** (decided 2026-07-20 — an open breaker fails *every* route on an upstream, so leaving the threshold, the failure definition, the open duration and the close condition to the implementer makes a wrong guess a self-inflicted fleet outage; and no fragment field existed for any of it):
  - **What counts as a failure: transport and server faults only.** A connection refused or reset, a DNS or TLS handshake failure, and a request timeout count; so does any upstream **5xx**. **No 4xx ever counts** — a 4xx proves the upstream is alive and answering, which is the only question the breaker asks. A **401 never counts either**, even one that triggers refetch-and-retry: a credential rotation is not an upstream outage, and counting it would let a routine rotation open the breaker on a perfectly healthy service. Credential state is the sentinel's job and is reported at `/health/credentials`. Failures SEAM produces itself — a validation 400, a guard 429, an adapter 500 — never count, because they never reached the upstream.
  - **Threshold and open duration: 5 consecutive failures opens the breaker for 30 seconds.** Consecutive, not a rate: any qualifying success resets the count to zero, so an upstream that is merely flaky stays closed and only one that is genuinely down trips.
  - **Half-open closes on evidence, not on a timer.** When the open interval elapses the breaker admits **one** real caller request as a trial — no synthetic probe, so a dead upstream is never kept warm by the breaker itself. Any response that is not a failure by the definition above (including a 4xx) closes it; another failure re-opens it with exponential backoff — 30s, 60s, 120s, capped at 5 minutes — so a long outage decays to roughly one trial request every five minutes rather than one every thirty seconds.
  - **`Retry-After` is derived, never guessed:** it is the seconds remaining in the current open interval, floored at 1, and the same value appears in the structured 503 body. An agent that honors it arrives exactly when the trial slot opens.
  - **Where the config lives, given that fragments are per-route and the breaker is per-upstream:** breaker state keys on the **resolved upstream origin** (scheme + host + port), and for a multi-instance route on the resolved origin of each `x-upstream-map` entry — so one dead cluster opens only its own breaker and the other eight keep serving, which is the behavior the `_all` fan-out needs. Config is declared at fragment root as **`x-breaker`** (see Data Models), which is well-defined because a fragment names exactly one upstream target by construction, with a per-instance `breaker` override inside `x-upstream-map`. Where two fragments resolve to the *same* origin and declare disagreeing `x-breaker` blocks, `seam lint` fails the PR, and at runtime the strictest values govern (lowest threshold, longest open duration) with the disagreement named at `/config/status` — the gateway never trusting lint to have run, as everywhere else here.
  - **This does not violate "no SEAM-wide defaults, guards configured individually per endpoint"** — that principle (Per-Route Guards) was written about the **cost governor and loop breaker**, and its reason does not transfer. Those guards spend money and refuse traffic on the author's behalf, so an implicit default would either cap a route nobody sized or, worse, silently fail to; absence of the field there is correctly read as "no guard." The breaker is **infrastructure-level availability protection**, where absence of a default is itself the failure mode: an upstream with no breaker means every agent hammering a dead service burns tokens indefinitely, which is the exact waste this section exists to stop. So the breaker is **on by default for every upstream** with the policy above, and `x-breaker` only *tunes* it — the two rules are not in tension because they answer different questions, and this is stated rather than left as an apparent contradiction. `x-breaker.enabled: false` exists for the rare upstream where failures are routine and a breaker would do more harm than good; it is lint-flagged and enumerated at `/config/status`, like every other opt-out in this plan.
- **Both signals are per-process and single-replica by construction** (decided 2026-07-20). Last-2xx tracking is already declared in-memory and restart-scoped; breaker state is the same. This is coherent only at one replica (Version Migration Strategy §1, amended for this reason): with two, `/docs` would show a different "last succeeded" on each refresh depending on which pod rendered it, and the breaker would open on one replica while the other kept hammering a dead upstream — the breaker's entire purpose defeated by the deployment topology. Neither signal may be built assuming replication; if HA ever lands, both move into the shared state store named in §1. The restart-scoping remains deliberate, and its honest rendering — "no attempt since last restart" — is what keeps a restarted gateway from reading as a broken one.
- Synergy with the credential sentinel: its probe traffic keeps at least one authenticated route per upstream warm, bounding staleness of the signal for probed upstreams.

## Fragment Toolchain (`seam` CLI)

The config surface is YAML fragments authored largely by agents and merged at runtime — one bad file must never break the gateway, and mistakes should be caught before merge, not at reload.

- **Runtime quarantine:** a fragment that fails schema validation or collides with an already-loaded fragment is skipped, logged, and surfaced at `/config/status` (loaded fragments, versions, quarantined ones with their errors) — never fatal to the remaining routes. **The collision key is the triple (path, method, `x-api-version`)** (decided 2026-07-20), never the path alone: two fragments declaring the same path and method but *different* `x-api-version` values are a legal coexistence pair, not a collision — that is precisely the shape a breaking change ships as (see Version Migration Strategy §2), so keying on path alone would quarantine every versioned migration and silently dismantle the coexistence model the whole version strategy rests on. A fragment carrying no `x-api-version` is keyed under the reserved version `_unversioned`, so two unversioned fragments on the same (path, method) still collide as before. Only an exact triple match is a collision, and the loser is always the later-merged fragment (deterministic filename order), never the incumbent — a bad new fragment cannot evict a working route.
- **Reserved control-plane namespace** (decided 2026-07-20; Phase 1a deliverable). The collision rule above is fragment-vs-fragment; nothing in it protects SEAM's *own* surface, which now spans `/docs`, `/docs/{route}`, `/openapi.json`, `/whoami`, `/scopes`, `/changes`, `/health/credentials`, `/health/upstreams`, and `/config/status` and keeps growing. Since fragments are authored largely by agents, a fragment declaring `/whoami` or `/config/status` is not a hypothetical: depending on a match order this plan never specified, it would either shadow SEAM's endpoint or be silently shadowed by it, and the first case is a spoofing surface the moment agents make authorization decisions from those responses. Three rules close it. **(1) The reserved set is explicit and closed:** the exact paths listed above, plus everything under the prefixes `/docs/`, `/health/`, `/config/`, `/approvals/`, and `/_seam/`. `/_seam/` is reserved *forward* — every control-plane endpoint added after this decision lives under it, so the reserved set stops accreting top-level names into the space fragments legitimately use. **Its first concrete users are `/_seam/healthz`, `/_seam/readyz` and `/_seam/metrics`** (Phase 1a), alongside the `/_seam/approvals/{id}` example below: all four post-date the closed enumeration and therefore take the prefix, and all fall under the already-reserved `/_seam/` so none needs a new reservation. The top-level names that stay put do so on a criterion that is actually true (**corrected 2026-07-20** — this previously grandfathered "the names already published to agents and CLAUDE.md", which was false in both halves: SEAM has zero implementation and no endpoint of its appears anywhere in CLAUDE.md, and `/scopes` was itself invented on 2026-07-20, in the same editing round as the rule grandfathering it as pre-existing. As written the "closed" set was reopened by the decision that closed it, and the precedent invited the next new endpoint to claim grandfathering too). The honest criterion, and the whole of it: **the grandfathered names are exactly those fixed in this plan as of 2026-07-20 — `/docs`, `/docs/{route}`, `/openapi.json`, `/whoami`, `/scopes`, `/changes`, `/health/credentials`, `/health/upstreams`, `/config/status`.** That enumeration is closed and may not be appended to; every endpoint conceived after this date takes `/_seam/`, with no exceptions and no second grandfathering clause. **`/approvals/` is reserved but not served** (decided 2026-07-20): approval-gated routes (`docs/notes/approval-gated-routes.md`, accepted 2026-07-16 and unphased) declare a top-level `/approvals/{id}` that agents poll for an approve/deny verdict — post-dating nothing in the enumeration above and therefore, by the forward rule, shipping as **`/_seam/approvals/{id}`** when it ships. The bare `/approvals/` prefix is reserved anyway, as a squat guard rather than a served surface: as written today a fragment could legally claim it, and a fragment occupying the obvious name for an approval verdict is a forgeable-approval surface, so denying the near-miss name costs a prefix nobody needs and closes the hole in both directions. **(2) Control-plane routes always win the match**, unconditionally: SEAM resolves a request against its own surface before consulting the merged route table, so no fragment can shadow a control-plane endpoint even if one is somehow loaded. **(3) A fragment declaring any reserved path is rejected twice** — `seam lint` fails the CI gate in declarative-config, and the gateway quarantines the whole fragment at merge time with the offending path named at `/config/status`, never trusting lint to have run (the same belt-and-braces posture as the OpenBao allowlist in Architecture). Quarantine is fragment-wide rather than per-path so a fragment cannot smuggle a reserved declaration through alongside valid routes.
- **Reserved is not the same as protected: who may *call* the control plane** (decided 2026-07-20). The rules above stop a fragment from *shadowing* SEAM's own surface and say nothing about who may read it — and three of those endpoints return precisely the reconnaissance this plan works hard to withhold everywhere else. `/config/status` enumerates every loaded fragment, every quarantined fragment with its error text, every route forwarding a credential over an unverified or plaintext connection, and "every route currently running unscrubbed" — a directory of routes where a secret provably echoes into responses. `/health/credentials` enumerates every (fragment, instance) credential SEAM holds, with expiry. `/health/upstreams` enumerates the upstream mesh. `/scopes?all=1` is gated behind `seam:scopes:read-all` on exactly this reasoning; these three bypassed it. The control-plane surface is therefore **two tiers, not one**:
  - **Caller-facing and scope-filtered** — `/openapi.json`, `/docs`, `/docs/{route}`, `/whoami`, `/scopes`, `/changes`. Any authorized caller may call them; what they *contain* is reduced to the caller's own grants (Per-Agent Tool Scoping point 5), so there is nothing to withhold at the endpoint level.
  - **Operator-only and default-denied** — `/config/status`, `/health/credentials`, `/health/upstreams`. These require the scope **`seam:ops:read`** and are refused to every caller without it. They are not scope-filterable in any useful sense: a fleet-wide inventory reduced to one caller's grants stops being the operational view it exists to be, and the quarantine, unscrubbed-route and credential-expiry data it carries has no per-caller slice at all. Refusal is a **403 naming `seam:ops:read`**, not the 404-identical-to-unknown rule that governs fragment routes — these are fixed, documented paths whose existence this plan publishes, so hiding them buys nothing and a scope-naming 403 leaks nothing. `/scopes?all=1` keeps its own narrower `seam:scopes:read-all` gate unchanged.
  - **Before Phase 7 there are no scopes, so the boundary is the network one.** Until identity exists, `seam:ops:read` cannot be evaluated, and leaving these three ambiently readable in the meantime would hand the whole inventory to anything that clears the ACL — during exactly the window (Phases 1b-6b) when the Pre-Phase-7 Trust Boundary is honest that the boundary is "coarse and node-level, not per-agent." So in that window the operator surface is **bound to a separate listener port**, and the tailnet policy file grants that port only to the operator source set while the proxy port is granted to the worker tags. That is enforceable with no identity at all, at the layer the trust boundary already mandates. At Phase 7 the `seam:ops:read` check becomes the real control and the port split degrades to defense in depth — the same relationship Pre-Phase-7 Trust Boundary already describes between the ACL and Grant-based enforcement.
- **`seam lint`:** the gateway's own merge/validation engine packaged as a CLI, run as a CI gate in declarative-config — validates fragment schema and detects collisions — on the same (path, method, `x-api-version`) key defined above — before a ConfigMap ever ships.
- **`seam diff`:** renders the effective merged-spec change a PR would cause, for review.
- **`seam import --from-url`:** bootstraps a fragment from an upstream's own published OpenAPI spec (ArgoCD and Kubernetes both publish one), filtered to selected paths/verbs; injection metadata (`x-vault-path` etc.) is then curated by hand. Onboarding a new upstream drops from authoring YAML from scratch to curating a generated file.

## Pre-Phase-7 Trust Boundary

**All inbound caller authentication and authorization lands in Phase 7. Everything before it runs behind a network trust boundary, and that boundary is a hard requirement, not an operational detail** (decided 2026-07-20). Stating it is not belt-and-braces: by the 6b cutover SEAM is a live credential-injecting proxy — it holds the z.ai/GLM and twitterapi.io credentials that cost money per call, and (after Phase 5) reaches cluster API servers — while CLAUDE.md prose is being deleted to push agents onto it. Without this section an implementer could read the phase list and reasonably ship that as an unauthenticated open proxy on the tailnet, which is the worst outcome available: every credential SEAM holds, reachable by anything that can route to it.

- **Tag-restricted tailnet ACL, enforced in the tailnet policy file.** SEAM's Tailscale node is reachable only from an explicit, enumerated source set (e.g. `tag:needle-worker`, `tag:devpod`, the operator workstation) via a grant in the tailnet policy file. Default-deny at the network layer — not "on the tailnet, therefore allowed." Tailnet membership is not the boundary; the ACL is.
- **The ACL is written per port, because the control plane is two tiers** (decided 2026-07-20). SEAM binds two listeners from Phase 1a — the caller-facing proxy port and an operator-only port carrying `/config/status`, `/health/credentials` and `/health/upstreams` (Fragment Toolchain, "who may *call* the control plane") — and the policy-file grant enumerates them separately: worker tags reach the proxy port only, and the operator source set alone reaches the operator port. This is what makes the two-tier rule enforceable in the window where it must be, since `seam:ops:read` cannot be evaluated before Phase 7 and a node-wide grant would publish the whole fragment/credential/unscrubbed-route inventory to every caller that clears the ACL. At Phase 7 the scope check becomes the real control and the port split degrades to defense in depth, exactly as the ACL itself does below.
- **No Funnel, no public ingress, no Cloudflare Tunnel.** SEAM is not exposed to the public internet in any phase before 7. The foreign-worker/non-tailnet ingress path (Per-Agent Tool Scoping point 7) is explicitly a post-Phase-7 phase of its own — **that phase is now numbered: Phase 14** (assigned 2026-07-20; edge `7 → 14`) — and must not be stood up early "just for testing" — it is the one path that would put an unauthenticated credential-injecting proxy on the open internet. Phase 14 is the *only* phase that lifts this rule, and it lifts it having first satisfied point 7's four rules; no earlier phase may lift it for any reason.
- **The ACL governs tailnet ingress and does not govern the in-cluster pod network** (stated 2026-07-20 — an omission that reads as a contradiction the moment SEAM has probes). From Phase 6a SEAM is a pod on rs-manager, and **kubelet reaches `/_seam/healthz` and `/_seam/readyz` at the pod IP, and the VictoriaMetrics scrape reaches `/_seam/metrics` at the pod IP on the operator port** (Phase 1a) — neither traverses the tailnet, so neither is subject to the policy-file grant, and no ACL construction could make them so. This is not a hole to close but a boundary to state: the pod network is a distinct trust surface governed by the cluster's own posture (namespace isolation, NetworkPolicy if it is ever wanted), and the two-tier port split is what keeps the *tailnet* side honest. It is stated because an operator reading the ACL as *the* boundary would otherwise conclude the operator port is unreachable to the scraper, go looking for a third listener, and find none.
- **The 6b cutover is GATED on that ACL existing.** Deploying SEAM (6a) with a permissive ACL is tolerable while nothing is cut over and no real agent traffic flows; deleting a service's CLAUDE.md prose and pointing agents at SEAM (6b) is not. The ACL grant is a checklist item in the 6b change, verified before the first service is cut over, not after.
- **Residual risk, stated plainly.** This boundary is **coarse and node-level, not per-agent**. Every caller that clears the ACL gets every route SEAM serves; a compromised or merely buggy worker on an allowed tag has the same access as any other. Guards (Phase 13) cap the *spend*, not the *reach*, and before Phase 7 they key on (route, hash) rather than per-caller — so a single misbehaving agent can consume a route-wide budget and 429 well-behaved peers. Accept this only because the alternative — deferring all of Phases 1-6 until identity exists — trades a bounded, known exposure for no delivery at all.
- **Phase 7 replaces this boundary; it does not merely add to it.** When Grant-based scope enforcement lands, per-route authorization becomes the real control and the ACL degrades to defense in depth (a coarse outer fence around a fine inner one). Nothing in Phase 7 is designed on the assumption that the ACL will still be there, and nothing before Phase 7 may be designed on the assumption that Phase 7 will arrive soon.

## Implementation Phases

**Phase numbers are stable identifiers, not execution order** (decided 2026-07-20). They are referenced from a dozen places in this document and from filed beads, so renumbering them to express sequence would break more than it fixes — and the previous convention of implying order by number had already produced a contradiction the plan flagged but never resolved (Phase 10 "should land before (or with) Phase 5"). Order is therefore stated once, explicitly, below; a phase's number says only which phase it is.

**Ship order (topological):**

`1a → 1b → 9a → 2 → 6a → 3 → 4 → 10 → 5 → 11 → 13 → 6b → 7 → 12 → 8 → 9b → 14`

(**Phase 11 moved ahead of 6b and Phase 14 appended, 2026-07-20** — see the `11 → 6b` and `7 → 14` edges below.)

**The dependency edges that force it** — anything not implied by these is scheduling preference, free to reorder:

- `1a → 1b → everything`: nothing merges, validates, or proxies without the core.
- `1b → 9a → 3`: `seam lint` must be a live CI gate **before the first fragment ships**, not after the last. Fragments start shipping in Phase 3 and every phase through 6b adds more; landing lint at the end would mean the entire pilot and cutover ships with runtime quarantine as its only validation — exactly the outcome Fragment Toolchain's design intent ("caught before merge, not at reload") rejects. It also lands the `x-vault-path` allowlist and reserved-namespace checks *before* the surface they protect, rather than after.
- `2 → 6a → 3`: fragments arrive as mounted ConfigMaps, so the gateway must be running in the cluster before the fragment-delivery phase, not two phases later.
- `OpenBao role/policy provisioning → 2`: the `seam` Kubernetes-auth role and the `read`-on-`seam/routes/*` policy must exist in OpenBao **before** Phase 2, because Phase 2 is the phase that authenticates against them. They were previously listed as Phase 6a deliverables, which put the server side of the login one phase *after* the client side that needs it (decided 2026-07-20; see Phase 2). This is a precondition rather than a phase — it is an OpenBao configuration change with no SEAM dependency of its own — so it constrains the order without appearing in the topological line above.
- `10 → 5`: multi-instance routes precede kubectl-proxy onboarding. Reversed, every cluster in the Phase 5 target set named in Hosting gets a copy-pasted single-instance fragment and Phase 10 becomes a migration of already-shipped ConfigMaps instead of a greenfield build — the N:1 collapse is the entire point of onboarding those upstreams that way.
- `4 + 13 → 6b`: the cost governor is live before agents are cut over onto metered upstreams. Phase 4's z.ai and twitterapi routes spend real money per call; 6a/4 can carry them with no agent traffic on them, 6b cannot.
- `11 → 6b`: **the circuit breaker is live before agents are cut over — a forcing edge on exactly the reasoning that forced `4 + 13 → 6b`** (recorded 2026-07-20, and Phase 11 moved ahead of 6b in the topological line above to satisfy it). Passive Route Health argues the breaker is categorically different from the opt-in guards precisely because "absence of a default is itself the failure mode: an upstream with no breaker means every agent hammering a dead service burns tokens indefinitely, which is the exact waste this section exists to stop." The plan then created the forcing edge for the cost governor and none for the breaker, leaving the `6b → 7` cutover window — the first window in which real agent traffic meets live metered upstreams — as the one stretch where that failure mode is live and unmitigated, with nothing bounding token burn against a dead z.ai or twitterapi.io but agent-side retry policy nobody controls. Its own argument therefore contradicted its own sequencing. The move is free: **Phase 11 depends only on 1b and 2**, both of which already precede 6b, so nothing else resequences and no fragment config changes.
- `OpenBao role/policy for seam-retirement-evaluator → 8`: **a precondition mirroring the `OpenBao role/policy provisioning → 2` edge above** (recorded 2026-07-20). Phase 8 ships `seam-retirement-evaluator` as its own deployable with its own identity (Components, Hosting), and it authenticates to OpenBao for a Forgejo write credential stored at its own path **outside** `seam/routes/*`. That path, its `read` policy, and the Kubernetes-auth role bound to the evaluator's own ServiceAccount must exist **before** Phase 8, or Phase 8 has precisely the server-side-missing problem Phase 2 was corrected for — a client authenticating against a role nobody created. Read access to the VictoriaMetrics instance is provisioned in the same precondition, since the evaluator is a query and cannot run without it. Like the Phase 2 precondition this is an OpenBao/metrics configuration change with no SEAM dependency of its own, so it constrains order without appearing in the topological line.
- `7 → 14`: **non-tailnet ingress authentication lands after per-agent scoping — a forcing edge, not a preference** (recorded 2026-07-20). Phase 14 adds a second *identity source* and reuses Phase 7's enforcement model wholesale (stage-5 authorization, scope filtering, `/whoami`, `/scopes`); there is no scope set to bind a verified Access-token subject to until Phase 7 exists. This is the phasing constraint stated in Per-Agent Tool Scoping point 7 — "gated on Phase 7's enforcement being real and exercised, because a second identity source is only safe once the enforcement point it feeds already works" — recorded here as an edge because that constraint previously had no phase to attach to.
- `2 → 13` and `2 → 12` **for the inbound request-body buffer** (recorded 2026-07-20): the tee and its `maxReplayableRequestBytes` cap are a **Phase 2** deliverable, and both consumers sit later — Phase 13's loop-breaker hash canonicalizes the body, and Phase 12's 401 replay re-sends it. The ship order already runs 2 first, so **nothing resequences**; the edges are recorded because the buffer was previously specified only inside Credential Health Sentinel and assigned to no phase, which had Phase 13 (position 11) depending on a mechanism described as part of Phase 12 (position 14) — a dependency running backwards against the ship order is exactly what an unrecorded edge hides.
- `Pre-Phase-7 trust boundary ACL → 6b`: see the section above.
- `7 → 8's per-caller breakdown`, `7 → 13's per-caller keying`, `7 → 8's Option-E drain`: identity-dependent refinements, all designed to land without changing fragment config.
- `7 → 8` **for the scope-aware drift-signaling pieces — a forcing edge, not a preference** (recorded 2026-07-20): `&scope-since=`, the `granted`/`revoked` visibility change kinds, `scopeChanged: unknown`, and serve-time scope filtering of archived specs and diffs cannot be built before Phase 7, because until then there is no scope set to hash, no `X-SEAM-Scope-Version` to pass as `scope-since`, and no filter to apply to an archived spec. The ship order already runs 7 before 8, so **nothing needs resequencing** — the edge is listed because an unrecorded forcing edge reads as reorderable scheduling preference, and reordering these two would silently make Phase 8 unbuildable. Phase 8's non-scope pieces (adapters, version selection, the ring buffer, unfiltered levels 1 and 2) carry no such dependency.

**Where the gateway runs, per phase** (decided 2026-07-20 — the plan previously never said, while Phases 3-5 onboarded upstreams whose reachability is a property of rs-manager): **Phases 1a-1b, 9a and 2 run locally, off-cluster** — a developer machine or devpod, fragments read from a static local directory, OpenBao reached over the tailnet. This is what Phase 1's "provable standalone" means and it is deliberately preserved: the core must be demonstrable without a cluster. **From 6a onward SEAM runs on rs-manager**, and every subsequent phase's upstream reachability (Connectors, `x-upstream` targets) is a property of that host.

**Phase 2 is the one entry that needs a caveat, and it is a real one** (added 2026-07-20). Phase 2's OpenBao client authenticates via **Kubernetes auth**, using a projected ServiceAccount token a developer machine does not have — so "Phase 2 runs locally" and "Phase 2 authenticates with the pod's SA token" cannot both be true as stated. Phase 2 stays local, because everything else it delivers (injection, strip-then-inject across headers *and* query, TLS policy, the 30s cache, scrubbing) is exactly the security-critical detail that benefits most from a fast local iteration loop and least from a deploy cycle per change. What makes that legal is the **sanctioned dev-token auth mode specified in Phase 2**: non-production, refused when SEAM detects an in-cluster environment, and enumerated at `/config/status`. Every behavior under test is identical in both modes, so the Kubernetes-auth path is the *only* thing Phase 2 cannot prove locally — and **Phase 6a proves precisely that one thing**, as its first-login deliverable. The production auth story is unchanged: Kubernetes auth, no static token, no AppRole, no credential file on disk.

- [ ] Phase 1a: Gateway scaffold (Go, ADR-001) — HTTP server, config, container build, plus `/docs`, `/docs/{route}`, and `/openapi.json` served over a **hand-written whole spec** (no fragment merge yet), request validation with structured error responses, the `X-SEAM-Spec-Version` **and `X-SEAM-API-Version`** response headers (the latter constant at `_unversioned` until Phase 8 — see below), and the **`/_seam/healthz`, `/_seam/readyz` and `/_seam/metrics`** endpoints (see below). Genuinely unblocked today: it needs the language decision (ADR-001) and nothing else. Ships the reserved control-plane namespace as a *routing* rule — and the rule is the **whole control-plane branch, not merely the match** (extended 2026-07-20 — "control-plane paths resolve before any route table" named stage 1 alone and left the branch's other three steps unassigned, while Architecture's "where a control-plane request goes after stage 1" specifies a stage-1 match as a branch rather than an exit): a control-plane hit runs **stage 1 → stage 2 (inbound sanitation) → stage 3 (identity resolution) → control-plane authorization → short-circuit before stage 4**, so no route table is ever consulted and stages 4 and 6-11 never run. 1a owns the *routing* half of that — the branch itself, stages 1-2, and the short-circuit — which is buildable with no identity and no scopes; **stage 3's resolution and the control-plane authorization step are inert until Phase 7, which owns their enforcement**, with the listener-port split below standing in their place until then. Also ships the **two-listener split**: the operator-only surface (`/config/status`, `/health/credentials`, `/health/upstreams`) binds to its own listener port, separate from the caller-facing proxy port (Fragment Toolchain, "who may *call* the control plane"). **The split lands in 1a rather than 6a** (decided 2026-07-20) because it is a property of the binary's own listener wiring, not of the cluster it runs in: Phase 1b ships `/config/status`, the first operator endpoint, so binding the split any later would mean birthing control-plane endpoints on the caller port and re-routing them afterwards — and a port that exists from the first commit is one no phase can forget to create. 6a adds the ACL grant that turns the split into a boundary; 1a only guarantees there are two ports to grant separately.

  **What `/docs/route` actually returns in 1a, and what Phase 8 adds to it** (decided 2026-07-20 — the endpoint was assigned here while its *specified* contract depends entirely on machinery Phase 8 delivers, which left the Phases 1a-7 behavior undefined for the one endpoint this plan calls its differentiator). Architecture requires the response to carry `version`, `isDefaultForUnversionedCallers`, `canonicalVersion`, `deprecated` and a `versions[]` list of every coexisting version, and requires every SEAM-generated pointer to carry `?version=` equal to the `X-SEAM-API-Version` that served the request — but 1a serves a hand-written whole spec with no fragments, no `x-api-version` and no version selection, and version selection and `X-SEAM-API-Version` are explicitly Phase 8 deliverables. The deliverable is therefore split at the seam that actually exists:
  - **Phase 1a ships `GET /docs/route?path=<openapi-path-template>` with an optional `&method=`**, returning that route's slice of the spec — parameters, request and response schemas, any annotations in force, and one worked example — and **no version-selection machinery**. There is exactly one contract, so there is nothing to select among.
  - **It is nonetheless version-*labeled* from day one, using `_unversioned`.** Every route SEAM serves before Phase 8 is keyed `_unversioned` (Data Models, `x-api-version`), and a prior decision established that `_unversioned` is a legal caller-addressable value that SEAM itself emits. So 1a **emits `X-SEAM-API-Version: _unversioned` on every response** — an addition to this phase's header deliverable, which previously named only `X-SEAM-Spec-Version` — and **every pointer it generates carries `&version=_unversioned`**: the structured 400's, and later the loop-breaker 429's and the `docsUrl` in `/changes`. There is no bare-pointer form and no "no version to name" case, which is the point: an agent's pointer-parsing never changes shape at Phase 8, and the plan's own no-bare-pointer-exception rule holds from the first commit rather than acquiring an undocumented seven-phase exemption.
  - **`?version=` is accepted in 1a with the single value `_unversioned`** and rejected otherwise, so SEAM never emits a pointer it cannot itself resolve. The response carries `version: _unversioned` and `isDefaultForUnversionedCallers: true`; `canonicalVersion`, `deprecated` and `versions[]` are **Phase 8 additions and are absent before it**, since each is a statement about coexisting versions and none coexist yet.
  - **Phase 8 adds the rest**: `?version=` as a real selector over live versions, the `canonicalVersion` / `deprecated` / `versions[]` fields with their per-entry `docsUrl`s, and the requirement that the emitted `&version=` track the version that *actually served the request* rather than the sole one that exists. (Architecture's `/docs/route` bullet carries the matching "when each half lands" clause, added 2026-07-20; the two now agree.)

  **Probe and metrics endpoints, and which listener each binds to** (decided 2026-07-20 — Version Migration Strategy §1 promises a "readiness-gated rollout" and §2 makes a per-route-version request counter the durable substrate for retirement, while the plan named **no** endpoint for either; and the entire `/health/` prefix is bound to the operator-only listener, which kubelet — which probes the pod, not the tailnet — has no business reaching). Three endpoints, all under **`/_seam/`**: the grandfathered top-level enumeration is closed as of 2026-07-20 and the forward rule admits no second grandfathering, so these take the prefix like every other post-dating endpoint.
  - **`/_seam/healthz` (liveness) and `/_seam/readyz` (readiness) bind to the caller-facing proxy port.** They disclose nothing — a bare serving/not-serving verdict, no route names, no fragment inventory, no credential state — so they cost the two-tier rule nothing, and binding them to the caller port keeps kubelet's probe target and the Service's traffic target the same port, which is what makes a readiness gate mean what it says. `readyz` reports not-ready until the initial merge has completed and (from Phase 2) the OpenBao login has succeeded, so a pod never receives traffic before it can inject; `healthz` reports only that the process is alive and its listeners are bound, and deliberately does **not** fail on a quarantined fragment or an open breaker — restarting the pod fixes neither, and a liveness probe that restarts on upstream trouble converts one dead upstream into a crash-loop.
  - **`/_seam/metrics` binds to the operator-only port**, Prometheus text format. It carries the per-route-version request counter (§2, Phase 8) plus guard, breaker, quarantine and cache counters — which is a route inventory and a traffic profile for every service SEAM fronts, squarely the operator tier's material, so it is grouped with `/config/status` rather than published to every caller.
  - **Both ports are reachable at the pod IP on the in-cluster pod network**, which is how kubelet probes and how the metrics scrape arrives. The tailnet ACL of Pre-Phase-7 Trust Boundary governs *tailnet* ingress and neither does nor can govern that path. Stated rather than assumed, because an operator reading the ACL as *the* boundary would otherwise conclude the operator port is unreachable to the scraper and go looking for a third listener.
  - (Fragment Toolchain's reserved-namespace bullet enumerates the reserved set and now names these three alongside the `/_seam/approvals/{id}` example as the first concrete users of the forward rule; they fall under the already-reserved `/_seam/` prefix and need no new reservation.)
- [ ] Phase 1b: Fragment merge — OpenAPI merge from a static local directory, per-fragment schema validation, collision detection on the (path, method, `x-api-version`) triple, quarantine + `/config/status`, runtime rejection of fragments declaring a reserved control-plane path (Fragment Toolchain rule 3), and **quarantine of any fragment whose `x-seam-owner` disagrees with the projected parent directory it was loaded from, or that omits the field entirely** (Architecture; corrected 2026-07-20 from "the basename it was loaded under" — the authoritative binding is the **parent directory**, since a service ships N fragment files into one owner directory and the basename is free-form and load-bearing for nothing). The owner check lands here, with the other merge-time quarantine rules, even though the `x-vault-path` prefix it ultimately guards does not arrive until Phase 2: at 1b the "projected parent directory" is simply the **containing directory** of the local file, which is enough to prove the mismatch-quarantines-whole-fragment behavior — and to prove it with N files under one directory — before any volume projection or secret exists to get it wrong. **Gated on the route-fragment schema** (Open Question 1, bead `bf-2wt`) — that schema is the critical path, not the language choice; see ADR-001 Consequences. Still no secret injection or k8s — provable standalone.
- [ ] Phase 2: Secret injection — OpenBao client authenticating via **Kubernetes auth** with the `seam/routes/*` path allowlist and per-service co-ownership rule enforced at merge time (Architecture), **the peer `x-upstream` / `x-upstream-map`-entry upstream-host allowlist** — the operator-owned, manifest-sourced set of permitted hosts and suffixes (`*.ts.net`, `*.ardenone.com`, `*.svc.cluster.local` plus explicitly enumerated public hosts), read from SEAM's own Deployment manifest and **never from a fragment**, re-checked by the gateway at merge time on every resolved upstream host with **fragment-wide quarantine** on violation and the offending host named at `/config/status`, IP-literal upstreams rejected outright, and no per-fragment opt-out (Architecture; added to this phase 2026-07-20 — the vault-path allowlist bounds *which secret* a fragment may name and this is the control that bounds *where that secret is sent*, and shipping injection without it means a correctly-filed, correctly-owned fragment can point its own permitted credential at an attacker-controlled host), `x-vault-path`/`x-inject-as` extension handling, **the SEAM-facing-path → upstream-path computation and its two rewrite fields — `x-upstream-path-template` (path-item-level, explicit, and it wins) and `x-upstream-strip-prefix` (fragment-root shorthand)** (added to this phase 2026-07-20 — the four-step rule is Data Models', under `x-upstream`, and it is what makes the *first real forward* land on the upstream's own path rather than on SEAM's: without it a `GET /argocd/api/v1/applications` forwards to `<upstream>/argocd/api/v1/applications` against a proxy that serves `/api/v1/applications`, so every fragment authored in Phase 3 would encode a different guess. Step 2, `{instance}`-segment deletion, has no subject until Phase 10 and lands there; steps 1, 3 and 4 ship here with the forward they make correct), strip-then-inject inbound hardening **of both namespaces SEAM writes into — headers *and* the injected query-parameter names, every occurrence deleted before injection** (Architecture), **`x-upstream-tls` handling** — per-fragment `caBundle` resolved against the manifest-owned `seam-upstream-ca` ConfigMap, `serverName` SNI override, `insecureSkipVerify: acknowledged` and `x-upstream-plaintext: acknowledged` honored and enumerated at `/config/status`, verification against the system trust store as the unconfigurable default — the 30-second secret cache TTL with eager 401 invalidation, **the inbound request-body tee and its `maxReplayableRequestBytes` limit (default 1 MiB)** — the buffer itself, not the retry that consumes it (assigned here 2026-07-20; the mechanism is specified in Credential Health Sentinel and the 401 replay ships in Phase 12). It belongs with inbound request handling, beside the strip-then-inject and cache work already in this phase, and it must precede **both** its consumers rather than one of them: Phase 12 replays the buffered bytes on a 401, and **Phase 13's loop-breaker hash canonicalizes the request body, which cannot be done without buffering it** — and Phase 13 ships at ship-order position 11 against Phase 12's position 14, so leaving the buffer with the sentinel had the earlier consumer inheriting a dependency from three positions later. SEAM tees the body upstream as it streams and discards it as soon as the response status is known; above the cap, and for any body that cannot be replayed at all, the body streams through unbuffered and each consumer degrades exactly as its own section specifies. The limit is **independently configurable** and merely shares a default with `maxBufferedResponseBytes` — tuning one never moves the other. And **secret-echo scrubbing of responses** — buffered scan under the size cap, incremental scan with `len(secret)-1` carry-over overlap for chunked/event-stream bodies, headers and trailers included, and refusal to inject on unscrubbable or protocol-upgraded routes absent `x-unscrubbable: acknowledged` (Credential Health Sentinel). Scrubbing ships **here, with injection** — not with the sentinel — because otherwise the plan's one core guarantee is false for every echoing upstream from Phase 2 until Phase 12.

  **How Phase 2 authenticates to OpenBao while it still runs off-cluster** (decided 2026-07-20 — resolving a contradiction that made this phase unbuildable as specified: Phase 2 was assigned to run locally, on a developer machine or devpod, while its central deliverable authenticates with "the pod's projected ServiceAccount token", which a developer machine does not have; and the OpenBao Kubernetes-auth role and policy it would authenticate *against* were listed as Phase 6a deliverables, one phase *later* in the ship order). Two changes, and the production auth story is untouched by both:
  - **The OpenBao server side moves earlier, and out of 6a: provisioning the `seam` Kubernetes-auth role, the ServiceAccount it binds, and the `read`-on-`seam/routes/*` policy is a precondition of Phase 2, not a deliverable of 6a.** It always was an OpenBao configuration change rather than a SEAM deployment — nothing about it needs SEAM to be running on rs-manager — and leaving it in 6a meant Phase 2 had no server-side role to authenticate against at all. 6a keeps the *cluster* side (the ServiceAccount object, the pod's `projected` SA-token volume, and the first end-to-end proof that the Kubernetes-auth login works from inside a real pod), which is the half that genuinely requires the cluster.
  - **A sanctioned, narrow local-development auth mode, explicitly non-production.** Locally, SEAM authenticates with a **developer-supplied OpenBao token** passed by environment variable or flag — never a file on disk, never checked in, and never AppRole, so the fallback the Architecture reserves for "a future non-Kubernetes deployment" is not quietly spent on developer convenience. It is fenced on three sides so it cannot leak into production: SEAM **refuses to start in this mode when it detects an in-cluster environment** (a projected SA token present, or `KUBERNETES_SERVICE_HOST` set), the mode is named in startup logs and enumerated at `/config/status` as `authMode: dev-token` exactly like every other acknowledged exception in this plan, and `seam lint`/config validation treat it as an error in any manifest-sourced configuration. Production is Kubernetes auth and nothing else; the allowlist, co-ownership, cache-TTL, injection, TLS and scrubbing behavior under test are byte-identical in both modes, so what Phase 2 proves locally is what Phase 6a runs.
  - **The same carve-out is owed to Phase 2's other two cluster dependencies, and for a sharper reason** (added 2026-07-20 — the dev-mode above covers the OpenBao token and stops there, while this phase delivers **two more** controls that reach the gateway only as ConfigMap mounts a developer machine does not have: the upstream-host allowlist at `/etc/gateway/allowlist.yaml` and the pinned CA bundles at `/etc/gateway/upstream-ca/`. The allowlist is the sharper case because it **fails closed** — absent, empty or unparseable permits no host at all — so a locally-run Phase 2 would quarantine every fragment naming an upstream, `/_seam/readyz` would report not-ready, and the phase would prove nothing it was assigned to prove). Locally both are named by flag or environment variable — **`--allowlist-file`** and **`--upstream-ca-dir`** — pointing at a developer-supplied file and directory, and they are fenced on exactly the three sides the dev token is: **refused when SEAM detects an in-cluster environment** (the same projected-SA-token / `KUBERNETES_SERVICE_HOST` detection), named in startup logs, and **enumerated at `/config/status`** alongside `authMode: dev-token` as **`allowlistSource: dev-file`** and **`upstreamCaSource: dev-dir`**, so a running SEAM always states which of its operator-owned inputs came from a developer rather than from a manifest. In-cluster the two paths are fixed and unoverridable — the flags relocate a file a developer supplies, they never change what a pod reads, which is what keeps the "operator-owned, never fragment-owned" property intact in the mode that could most easily erode it. Fail-closed is unchanged in both modes: a missing or unparseable local allowlist permits no host, exactly as a missing ConfigMap does, so the developer sees the production failure rather than a permissive local one.
- [ ] Phase 3: ConfigMap-mounted route fragments — projected volume, in-process file-watch hot reload with atomic route-table swap (see Architecture), first fragment (**pilot: migrate the existing hand-rolled ArgoCD read-only proxy** — unmetered and read-only, so it needs neither the cost governor nor caller auth to be safe, the lowest blast radius available for a first fragment). **Precondition: the `seam lint` CI gate (9a) is live in declarative-config** — no fragment ships through an ungated path, including this one.
- [ ] Phase 4: Onboard z.ai/GLM proxy and twitterapi.io proxy fragments (both already reachable from rs-manager — no new Tailscale Connector needed). Both are metered, so no agent is cut over onto them until Phase 13's cost governor is live (see 6b).
- [ ] Phase 5: Onboard kubectl-proxy endpoints for additional clusters as **one parametrized multi-instance fragment** (requires Phase 10), adding a Tailscale Connector per cluster on rs-manager as needed.
- [ ] Phase 6a: Deploy SEAM to rs-manager via declarative-config — single replica per Version Migration Strategy §1, the Kubernetes **ServiceAccount** and its projected SA-token volume plus the **first end-to-end proof that OpenBao Kubernetes auth logs in from inside a real pod** (the *role and policy themselves* are a Phase 2 precondition, moved earlier — see Phase 2), a Tailscale node, the tag-restricted ACL grant in the tailnet policy file (Pre-Phase-7 Trust Boundary), **the Kubernetes liveness and readiness probes wired to `/_seam/healthz` and `/_seam/readyz` on the caller-facing port** (added 2026-07-20 — §1's "readiness-gated rollout" is a promise no manifest kept until this line existed; the endpoints ship in 1a, the probe stanzas that make the gate real ship here, and `terminationGracePeriodSeconds` is set here too, since it is the dominant term in the deploy gap §1 now sizes honestly), and **the metrics scrape config or `ServiceMonitor` pointing VictoriaMetrics at `/_seam/metrics` on the operator port** (Hosting). The scrape is not a nice-to-have: from Phase 8 the per-route-version counter it collects is the *only* durable substrate for retirement (§2), so an unscraped SEAM makes `seam-retirement-evaluator` unbuildable rather than merely unobservable. **The ACL grant is written against the two-listener split from Phase 1a, not against the node as a whole** (decided 2026-07-20): the caller-facing proxy port is granted to the worker tags, and the operator port carrying `/config/status`, `/health/credentials` and `/health/upstreams` is granted *only* to the operator source set. A single node-wide grant would hand every caller the fleet inventory these two tiers exist to separate, during exactly the Phases 1b-6b window in which no scope check can yet refuse them.

  **Cluster RBAC, enumerated** (decided 2026-07-20 — the deliverable list named the OpenBao and Tailscale resources and silently omitted the Kubernetes API permissions two later phases depend on, which surfaces as a `403 Forbidden` at runtime in the phase that consumes them rather than as a missing manifest here): SEAM's ServiceAccount gets a namespaced **`Role` + `RoleBinding` granting `get`/`create`/`update` on `coordination.k8s.io` `leases`**, scoped to SEAM's own namespace and, where the API server honors it, to the single Lease name the sentinel uses. **Phase 12 consumes this** — the credential sentinel's probe loop acquires that Lease before probing (Credential Health Sentinel) — so it is created here, with the rest of SEAM's cluster footprint, rather than being discovered missing when the sentinel first fails to start its loop. Nothing else is granted: SEAM reads its fragments from a mounted `projected` volume and has **no ConfigMap API permission at all**. The Architecture's informer-based reload is an explicitly *unshipped* upgrade path, so the ConfigMap `get`/`list`/`watch` it would need is deliberately **not** granted in advance — taking that upgrade is the change that adds it, and until then least privilege is the cheaper default.

  **Is the Lease redundant at one replica?** (assessed 2026-07-20, since `replicas: 1` with `maxSurge: 0` was adopted after the leader-election decision and appears to obviate it). **No — keep it.** `maxSurge: 0` removes the *rollout* overlap, which was one of two sources, but not the other: a partitioned or `NotReady` node lets the Deployment controller schedule a replacement while the original pod is still running and still has network egress to the upstreams. That orphan is precisely the case leader election exists for, because the failure it causes is the one the sentinel section calls out as visible *outside* SEAM — doubled authenticated probe traffic against rate-limited credentials, i.e. a health check that breaks the credential. The Lease costs one Role, one RoleBinding, and a few lines of client code, and it stays cheap and correct through the future shared-state HA path; dropping it would trade that for a failure mode nobody would attribute correctly. No agent traffic yet: this phase makes the gateway *reachable*, not *adopted*.
- [ ] Phase 6b: Agent cutover — cut agents over **service by service** (decided 2026-07-16); as each service's fragment goes live, its hand-written CLAUDE.md proxy prose is deleted in the same change; never a big-bang cutover. The end state flips the default from "here are your prescribed tools" to "here is your tool interface — list the routes and choose from that list": CLAUDE.md eventually carries only the SEAM endpoint pointer. **Gated on the trust-boundary ACL existing and on Phase 13 for any metered route.** The pre-cutover checklist verifies the ACL at **both ports**, not one: that the worker tags reach the caller-facing proxy port, and — the item easiest to skip because nothing fails when it is wrong — that they are **refused** on the operator port, so `/config/status`, `/health/credentials` and `/health/upstreams` are not ambiently readable by every agent being cut over. A permissive operator-port grant is invisible in testing precisely because it only ever *works*. **The checklist additionally verifies that each service's agents retry with backoff over at least a 60-second window** (added 2026-07-20): SEAM is single-replica with `maxSurge: 0`, so a binary deploy is a real gap of **typically 5-30 seconds** and not the "sub-second" one §1 used to claim, and an agent cut over with a one-second retry budget will fail across every SEAM deploy thereafter. This is the cutover's cheapest item to skip and its most expensive to discover later, since the first deploy after cutover is when it surfaces.
- [ ] Phase 7: Per-agent tool scoping — NEEDLE-side per-worker `tsnet` identity provisioning, `x-required-scope` route tagging, Grant-based scope enforcement at the gateway, plus the self-service surface (`/whoami`, `/scopes` — scope-filtered by default, full taxonomy at `/scopes?all=1` behind the `seam:scopes:read-all` scope, and **both forms derived at request time from two merged sources, not one** (added 2026-07-20): the merged spec's `x-required-scope` declarations, **plus a small built-in set of control-plane scope declarations compiled into the SEAM binary**, each carrying the same fields a fragment-derived one does and labeled `source: builtin`. The built-in set is not an optimization: SEAM's control plane resolves *before* the merged route table (Fragment Toolchain rule 2) and therefore has no `x-required-scope` anywhere in any fragment, so under a fragments-only derivation the two scopes an operator most needs to discover — **`seam:ops:read`** and **`seam:scopes:read-all`**, both landing in this same phase — would be exactly the two `/scopes` could not show. It is scope-filtered on identical terms: a caller holding neither sees neither, and both appear in `/scopes?all=1` — scope-filtered `/openapi.json` + `/docs` + `/docs/{route}`, and 403s naming the missing scope and the Grant snippet that would fix it). Tailnet-native Grant path only; non-tailnet callers are default-denied (Per-Agent Tool Scoping point 7). Depends on 1b, 2, 3 at minimum.

  **Phase 7 is the phase where pipeline stages 3 and 5 switch on, together and never separately** (added 2026-07-20 — this deliverable list named "Grant-based scope enforcement at the gateway" and never said which pipeline stages change state, while Architecture is explicit that both stages are inert through Phases 1a-6b and that "both activate at Phase 7 — that activation is a Phase 7 deliverable". An activation named in the pipeline and in no phase is, by this plan's own principle, an unbuilt one). Four activations land here, all of them faces of one control:
  - **Stage 3 begins resolving a real caller identity and default-denying a request whose identity cannot be resolved at all.** Until now it recorded the reserved anonymous identity and denied nothing, with the tailnet ACL standing in its place (Pre-Phase-7 Trust Boundary).
  - **Stage 5 begins enforcing `x-required-scope`** — parsed, shape-validated and rendered as documentation since Phase 1b, refusing nobody until now. Enforcement is default-deny, and on an adapting fragment it is the **union** of the matched fragment's and its `targetVersion`'s requirements once Phase 8 makes adapters exist (Version Migration Strategy §2).
  - **Scope *filtering* goes live with it**, having had no grant set to filter against before: the filtered `/openapi.json`, `/docs` and `/docs/route`, the filtered-spec **404 identical to a route that does not exist**, and the narrower visible-but-not-invocable **403** naming the missing scope and the Grant snippet that fixes it.
  - **The control-plane authorization *step* becomes real** — step 3 of the control-plane branch, not stage 5, and this phase owns the step and not merely the appearance of its scopes in `/scopes`. It evaluates the **built-in scope declarations compiled into the binary** (`seam:ops:read`, `seam:scopes:read-all`) in place of an `x-required-scope` no control-plane endpoint has (Architecture, "where a control-plane request goes after stage 1"), refusing with a **403 naming the missing scope** rather than the 404-identical-to-unknown rule that governs fragment routes. Phase 1a ships that branch as routing; here it acquires its check.

  Enabling any one of the four without the others is incoherent, which is why they are one deliverable: the deny with no check refuses every caller, the check with no identity evaluates scopes against nobody, the 404/403 split with no grant set has nothing to filter by, and the control-plane step with no identity has no subject. Stage *numbering* is unaffected in either window — stages 3 and 5 always exist and always occupy those positions — which is what keeps every "stage N" reference in this plan stable across the transition.

  Phase 7 additionally owns **everything that makes a caller's scope state namable**, which is what Phase 8's drift signaling later diffs against (Version Migration Strategy §3, "Phase ownership"): the **`X-SEAM-Scope-Version` response header** — a hash of the caller's effective scope set, carried alongside `X-SEAM-Spec-Version` — **`/whoami` reporting that same value** next to the resolved identity, tags and effective scopes, and the **bounded hash→scope-set retention map** SEAM keeps so it can later diff two filtered views: the most recent **4** distinct scope versions per caller identity, evicted after **24 hours idle** for that identity, under a **global LRU cap** on total entries. The bound is the deliverable, not an optimization — an unbounded map keyed by a hash of caller-adjacent input is a slow leak, and current-scope *filtering* never reads this map (it is computed from live grants on every request), so eviction can never widen what a caller sees.

  Phase 7 also lands the **`seam:ops:read` scope and the operator gating of `/config/status`, `/health/credentials` and `/health/upstreams`** (Fragment Toolchain, "who may *call* the control plane"): these three are default-denied to every caller without it, refused with a **403 naming `seam:ops:read`** rather than the 404-identical-to-unknown rule that governs fragment routes, since they are fixed documented paths whose existence this plan publishes. This is the phase where the port split from 1a/6a stops being the boundary and becomes defense in depth behind it.
- [ ] Phase 8: Version migration tooling — **conditional `Deprecation`/`Sunset` header emission** for fragments marked `x-seam-deprecated` (or per-operation `deprecated: true`): `Deprecation` is emitted from the moment the marker lands, carrying the object's required `since`; `Sunset` is emitted **only** when the author supplied an advisory `sunset`, or once the quiet window has actually opened and the adopted window (fixed under "Retirement workflow" below) resolves to a date — and is **omitted entirely** before then, because retirement here is deliberately dateless and there is no date to emit (Data Models, `x-seam-deprecated`). Plus the `x-adapter` schema and its closed transform vocabulary, the request/response transform executor in the proxy path, `seam lint` validation of adapter blocks, `X-SEAM-API-Version` selection defaulting to the **oldest** still-served version, **the version-aware half of `/docs/route`** — `?version=` as a real selector over live versions (in 1a it accepts only `_unversioned`), the `canonicalVersion` / `deprecated` / `versions[]` fields with a per-entry `docsUrl` for every coexisting version, and the requirement that every SEAM-generated pointer carry `&version=` equal to the version that **actually served that request** rather than the sole version that exists (assigned here 2026-07-20; see Phase 1a, which ships the endpoint itself with `?path=`/`?method=` only) — the per-route-version request-count metric **exported at `/_seam/metrics` on the operator port and scraped into VictoriaMetrics** (Phase 1a ships the endpoint, Phase 6a the scrape config; this phase adds the series and is what makes the store's retention window load-bearing, per §2), and the `/changes?since=<spec-version>` diff endpoint backed by a ring buffer of prior merged specs (see Version Migration Strategy above). Adapters are declarative fragment YAML — **never gateway Go code**, so a contract migration stays a ConfigMap edit.

  **The per-route-version request-count metric is probe-free by definition** (decided 2026-07-20): the exported series counts *caller* requests and **excludes sentinel-originated traffic**, matched on the in-process origin tag the probe loop sets (Credential Health Sentinel). This is not a filter applied by the consumer, it is a property of the series itself, because a deprecated fragment keeps its `x-credential-probe` for as long as it is served — so a probe-inclusive counter would sit permanently above zero, the quiet window would never open, and **no probed fragment could ever retire**. For the same reason a probe can never reset a quiet window or cancel a brownout; only a real caller does.

  Phase 8 additionally owns the **scope-aware half of drift signaling** (Version Migration Strategy §3, "Phase ownership"), all of which is built on what Phase 7 makes exist: **`&scope-since=<scope-version>`** on `/changes`; the **`granted` / `revoked` visibility change kinds**, kept as a category distinct from the contract kinds (`added` / `removed` / `params-changed` / `response-changed` / `deprecated`) so a caller can always tell "the contract moved" from "my access moved"; the **`scopeChanged: unknown`** flag emitted when `scope-since` is omitted, unknown, evicted, or pre-restart — one behavior for all four, so no new client handling is needed for any of them — and the matching `sinceKnown: false` fallback for an unknown or evicted `since=`, which answers **200** with a pointer to the current full spec rather than an error; **serve-time scope filtering of archived specs and of level-1 and level-2 diffs**, including the rule that a level-2 route outside the caller's grants returns **404 byte-identically to a route that has never existed**, since a 403/404 split there is an enumeration oracle; and **`/openapi.json?version=<v>`** — level 3 of the progressive disclosure, named in Version Migration Strategy §3 and in `docs/notes/versioning.md` §C but until 2026-07-20 assigned to **no phase at all**. An archived spec is never served verbatim. Phase 8's non-scope pieces (adapters, version selection, the ring buffer, level 1 and level 2 as unfiltered mechanisms) carry no Phase 7 dependency; these do.

  **Retirement workflow — the adopted composition** (decided 2026-07-20, adopting the recommendation in `docs/notes/quiet-window-options.md`, which this phase previously named only as "usage-gated retirement workflow" while leaving the mechanism unselected): **(D) PR-gated human retirement is the always-on gate** — when the quiet condition is met, the declarative-config PR is opened with the traffic evidence in its description and a human merges it; nothing is ever removed autonomously, which costs a little latency and fits the GitOps rule that all cluster changes go through declarative-config anyway.

  **The evaluator and the PR-opener are an out-of-band job, and emphatically not the gateway** (decided 2026-07-20 — correcting this phase's earlier "**SEAM** opens the declarative-config PR", which was both a security escalation and inconsistent with §2's own decision that the gap history lives in the metrics store rather than in gateway memory). Two independent reasons, either sufficient:
  - **Credential scope.** Opening a PR requires a Forgejo write credential against declarative-config — the repository that controls every cluster in the fleet. SEAM's OpenBao policy grants `read` on `seam/routes/*` **and nothing else**, and the entire hostile-fragment threat model in Architecture rests on that being literally true: a fragment authored by an agent cannot reach material SEAM's own token has no capability for. Handing a credential-injecting proxy write access to the GitOps repo would make "compromise SEAM" and "commit to every cluster's desired state" the same event, and would do it for a convenience feature. The policy stays closed, with **no named exception**.
  - **State location.** §2 already establishes that the observed max inter-request gap and "quiet since" are read from the durable per-route-version counter in the metrics store, not from gateway memory, precisely because every in-process store here is restart-scoped and would reset the quiet clock on each binary deploy. The evaluator is therefore a *query*, and a query has no reason to live in the request path.

  So retirement evaluation ships as **`seam-retirement-evaluator`, a small scheduled job** (a Deployment with an internal scheduling loop, per this fleet's no-CronJobs rule) that runs **read-only against the metrics store**, computes the window per route-version, and opens the declarative-config PR under **its own narrowly-scoped Forgejo credential** — a token that may open PRs against declarative-config and holds nothing else, stored at its own OpenBao path outside `seam/routes/*` and readable only by that job's identity, never by SEAM. It consumes no gateway API, no gateway memory and no gateway credential; SEAM's only contribution is the exported metric, which it already publishes. This adds **no** request-path state, **no** PVC, and **no** second stateful component to the single-replica gateway. Brownout scheduling (C) stays in the gateway, since serving a `410` is a request-path behavior and needs no credential at all. **(B over A) the window is traffic-adaptive with a calendar floor** — `max(3 × observed max inter-request gap on that route-version, 7 days)` — since a fixed window must exceed the longest inter-call gap of any live caller, a number nobody knows; the floor covers cold route-versions with too little history to self-size. This does not reopen the usage gate: zero observed traffic remains a necessary condition, and the calendar term can only lengthen the wait (see Version Migration Strategy §2). **(C) brownout 410s in the final week** — the deprecated route-version is served as a structured `410 Gone` for short scheduled windows, body naming the replacement version and pointing at `/changes`, converting "we believe nobody calls this" into a tested claim that an LLM caller can actually read and self-migrate from. This is a bounded, scheduled, pre-announced exception to the coexistence guarantee, not a hole in it: it applies only to a route-version already past its quiet threshold and already carrying `Deprecation`/`Sunset`, and a caller that appears during a brownout **cancels the retirement** rather than being sacrificed to it. **(E) post-Phase-7 upgrade to caller-complete drain** — retire once every identity that ever called v-old has either called v-new or left the tailnet, within a bounded lookback; this replaces the window approximation with the thing it approximates and shrinks retirement latency from weeks to days. Depends on 1b (fragment loading + spec-version header); the per-caller breakdown and (E) depend on Phase 7.

  **How the evaluator's verdict reaches the gateway — the one channel, and it is the fragment itself** (decided 2026-07-20; a Phase 8 deliverable). Splitting retirement into an out-of-band evaluator left **two gateway-side behaviors keyed to a projection the gateway provably cannot compute**, with no interface between them. (C) above rules that "brownout scheduling stays in the gateway, since serving a `410` is a request-path behavior and needs no credential at all" — which is true of *serving* the 410 and false of **knowing when to**; and Data Models rules that `Sunset` "is emitted only once the quiet window has actually started and its projected end is computable", another gateway-emitted header keyed to the same projected retirement date. But §2 decides that the projection's inputs — the observed max inter-request gap and "quiet since" — live **in the metrics store**, with the gateway keeping "only a live in-process view of the same counter, which may reset freely because nothing depends on it". The evaluator is the only component that queries that store and the only identity holding VictoriaMetrics read access, and Phase 6a grants SEAM Leases and **no ConfigMap API permission at all**. So as filed, the gateway had to schedule brownouts and emit `Sunset` from a computation with no inputs, no credential, and no channel to receive one. The channel is therefore defined, and it is the cheapest one consistent with this plan's posture:
  - **The evaluator writes its verdict back into the fragment**, by the same operator-reviewed declarative-config PR mechanism it already uses to propose removal. When the quiet window opens, it opens a PR against `k8s/rs-manager/seam/routes/<svc>/<name>.yaml` populating **`x-seam-deprecated.sunset`** with the projected end and **`x-seam-deprecated.brownout`** with the scheduled windows. The edit reaches the gateway by the **existing ConfigMap hot-reload path** — no new SEAM permission, no new SEAM state, no new credential, no evaluator→gateway API, and no request-path change of any kind. The gateway keeps doing exactly what it already does: reading a fragment field and emitting a header.
  - **Three properties follow, and they are why this was chosen over a gateway-side metrics query, a shared store, or a push API.** The projection becomes **operator-reviewed before it can affect a caller** — the same human gate (D) already imposes on removal, now covering the brownout schedule that precedes it, which matters because a brownout serves `410`s to live callers. It is **declarative and inspectable in git**, so "why is this route browning out on Thursday" is answered by a PR rather than by a running process's memory. And it **adds nothing to the single-replica gateway** — no PVC, no second stateful component, no credential widening.
  - **Consequences for the two headers, stated so nobody re-derives them.** Before that PR merges, `Sunset` is emitted **only** from an author-supplied `sunset`, exactly as Data Models specifies for the interval between marking and the window opening; `Deprecation` alone is correct and RFC-conformant there. The **brownout scheduler is dormant on any fragment carrying no `brownout` block**, so a gateway that has never heard from the evaluator browns nothing out — the fail-safe direction, since the failure of the channel degrades to "coexistence continues", never to "a live route starts returning 410". A caller appearing during a brownout still **cancels** the retirement, and the cancellation travels the same way: the evaluator observes the traffic in the metric and opens the PR that removes the block and clears the projected `sunset`.
  - **`x-seam-deprecated` gains a `brownout` member** to carry this — an ordered, non-overlapping list of scheduled windows. Data Models owns that field's exact shape; `seam lint` validates it (Phase 9a's check list, marked as landing with this phase), which is the gate that keeps a bad machine-written projection from being merged by a human skimming a diff.
- [ ] Phase 9a: `seam lint` — the gateway's merge/validation engine packaged as a CLI and wired as a declarative-config CI gate. Validates fragment schema, detects collisions on the (path, method, `x-api-version`) triple, rejects any `x-vault-path` outside the `seam/routes/*` allowlist or violating per-service co-ownership, **rejects any `x-upstream` or `x-upstream-map` entry `url` whose host falls outside the operator-owned upstream-host allowlist**, **rejects any fragment declaring a reserved control-plane path** (Fragment Toolchain rule 3 — the runtime quarantine half ships with 1b; lint is the pre-merge half), and flags every `x-unscrubbable: acknowledged` for human review.

  **Why it lands here — the rationale, corrected** (2026-07-20). This previously read "marginal cost over 1b is CLI packaging, which is why it lands here rather than at the end", and the enumerated check list below has since made that false: static schema-transform composition checking, `x-api-version` ordering, `x-seam-deprecated` shape, `x-breaker` same-origin conflict detection and the buffered-transform-on-unbufferable-route combination each validate a field whose **runtime** ships in Phase 8, 11 or 13, and the first of those is substantial independent work by any measure, not packaging. The **forcing** reason is untouched and is the one that actually places the phase: **`seam lint` must be a live CI gate before the first fragment ships in Phase 3** (the `1b → 9a → 3` edge). What is corrected is the *scope* of what lands here. **9a ships the checks whose subjects exist by Phase 3** — schema, collisions on the (path, method, `x-api-version`) triple, reserved control-plane paths, the owner chain, the vault-path and upstream-host allowlists, and the transport acknowledgements — **and its remaining checks are added incrementally, each in the phase that introduces the field it validates**, marked per-bullet below. **No new phase number is created and no cross-reference moves**: 9a owns every check in the list, and each increment is a deliverable of the phase named beside it, so nothing here is unassigned. Adding a check to a CLI that already exists is genuinely packaging; writing one against a field nobody may yet legally author is not — that is the distinction the original rationale collapsed.

  **The full check list, enumerated** (decided 2026-07-20 — every rule elsewhere in this plan that says "`seam lint` rejects/flags this" is a deliverable of *this* phase, and a rule whose enforcement is asserted in one section and scheduled in none is an unenforced rule):
  - **The owner chain, end to end — walked once per file, with the parent directory authoritative** *(lands with 9a)* — declarative-config directory name (**`k8s/rs-manager/seam/routes/<svc>/<name>.yaml`** — one directory per service and **N fragment files in it, not one**, the filing rule fixed in Architecture 2026-07-20 and corrected here from the earlier `fragment.yaml` spelling, which described a one-file-per-service layout that would foreclose version coexistence outright; cited at all because lint is the only consumer that walks the chain from its first link, and a chain whose first link has no address is unwalkable) → ConfigMap name `seam-routes-<svc>` → the projected `path:` **owner segment** in SEAM's own Deployment manifest at `k8s/rs-manager/seam/` → `x-seam-owner` → the `seam/routes/<svc>/*` prefix of `x-vault-path`. **The authoritative link is the parent directory, never the basename**: `<name>` is the author's to choose (`fragment.yaml`, `v1.yaml`, `pods-v2.yaml`), carries no meaning and constrains nothing. So lint walks the chain **per file** while the chain itself is **per directory** — a service adding a second fragment adds a check, never a new chain — and a basename-keyed check would be checking the one segment the author is free to write. Lint is the only place the *whole* chain is visible at once: the gateway can re-check only the last three, from what it can observe at runtime, so the directory→ConfigMap→`path:` links are checkable nowhere else.
  - **Path-rewrite coherence** *(lands with Phase 2, which introduces both fields)* — two checks, each catching a fragment that forwards to a path shape nobody intended (Data Models, `x-upstream-path-template` / `x-upstream-strip-prefix`). **`x-upstream-path-template` must begin with `/`, every `{param}` it names must exist in the matched path template, and it must name no `{instance}`** — `{instance}` is routing metadata SEAM consumes to select an `x-upstream-map` entry and is deleted before any rewrite runs, so a template naming it references a segment that provably cannot be substituted and would forward a literal `{instance}` upstream. **`x-upstream-strip-prefix` must be a prefix of *every* path the fragment declares** — a partly-applicable strip is a fragment whose routes silently forward to two different path shapes, which is the defect least likely to be noticed in review because the fragment's flagship route works. Both are hard errors; the gateway re-checks at merge time and quarantines fragment-wide, never trusting lint to have run.
  - **Upstream-host allowlist violations, a hard error** *(lands with 9a)* — the peer of the owner-chain check above, and a **hard error rather than a flag**: every `x-upstream` and every `x-upstream-map` entry `url` must resolve to a host inside the operator-owned allowlist declared in SEAM's own manifest, and anything outside it fails the PR. It is deliberately **not** an acknowledgeable exception in the style of `insecureSkipVerify` or `x-unscrubbable` — those make a *known* exposure enumerable, whereas an unlisted upstream host has no legitimate case a manifest edit cannot express, and a per-fragment acknowledgement would hand the exfiltration path straight back to the fragment author. IP-literal upstreams fail here too. The gateway re-checks and quarantines fragment-wide, never trusting lint to have run. **This one check is inert until Phase 6a** (noted 2026-07-20): the allowlist it validates against is the `seam-upstream-allowlist` ConfigMap at `k8s/rs-manager/seam/`, and **that manifest directory does not exist until 6a creates it**, while 9a ships two positions earlier in the ship order (`9a → 2 → 6a`). Lint must therefore treat an **absent** allowlist as *inert*, not as an empty one — the gateway's fail-closed rule for a missing allowlist is a **runtime** rule, and importing it into the CI gate would fail every PR across the window where there is nothing to check and no fragment to protect. Nothing is lost by the gap: 6a still precedes **Phase 3**, where the first fragment ships, so no fragment ever passes through an ungated path. The IP-literal and well-formed-absolute-URL halves of the check are allowlist-independent and are live from 9a.
  - **Transport exceptions, flagged not failed — with two hard errors among them** *(lands with 9a)* — every `x-upstream-tls.insecureSkipVerify: acknowledged` and every `x-upstream-plaintext: acknowledged` is surfaced for human review rather than passed silently. Three failures rather than flags: a plaintext `x-upstream` carrying **no** acknowledgement is a hard error; **`x-upstream-plaintext` declared alongside `x-upstream-map` is a hard error** (acknowledgement on a map-bearing fragment is the per-entry `plaintext` key, since the exposure is per-instance and a fragment-wide marker misstates which instances are actually in the clear — Data Models); and **an `x-upstream-map` entry whose `url` is `http://` and which carries no `plaintext: acknowledged` of its own is a hard error.**
  - **`x-api-version` grammar** *(lands with 9a — the regex is cheap and the field is legal to author from Phase 3)* — reject anything not matching `^v[1-9][0-9]*$`, and **reject the literal `_unversioned` as an AUTHORED value** (wording fixed 2026-07-20 — "reject `_unversioned`" read as a blanket ban on a string that is in fact a legal caller-supplied `X-SEAM-API-Version`, a legal `?version=`, and a value SEAM itself emits in every pointer; the prohibition is on *authoring* it into a fragment's `x-api-version`, never on *addressing* it — Data Models, `x-api-version`). It is a key SEAM assigns, so the reserved slot cannot be occupied deliberately.
  - **`x-seam-deprecated` shape** *(lands with Phase 8, which introduces the field)* — a bare `x-seam-deprecated: true` is an error with a fix hint naming the `{since, sunset?, brownout?}` object form, never silently coerced to a dateless deprecation.
  - **`x-seam-deprecated.brownout` sanity** *(lands with Phase 8, which introduces the member; added to this enumeration 2026-07-20)* — the brownout block is **machine-written by `seam-retirement-evaluator` and human-merged**, which makes lint the gate that stops a bad projection from being approved by a reviewer skimming a diff of generated YAML. Each window needs a `start` and an `end`, `end` must follow `start`, windows must not overlap, and every window must fall **inside the interval between `x-seam-deprecated.since` and `sunset`** — a brownout outside it is a `410` scheduled against a retirement that is not being projected. Two hard errors besides: a `brownout` block on a fragment carrying **no `x-seam-deprecated`** at all, and one whose `sunset` is **absent** — a brownout is scheduled *against* a projected retirement and means nothing without one, so the pair must arrive in the same PR.
  - **`x-adapter` mutual exclusion** *(lands with Phase 8)* — an adapting fragment declaring `x-upstream`/`x-upstream-map`, or any upstream-facing field (**`x-upstream-path-template`**, **`x-upstream-strip-prefix`**, `x-vault-path`, `x-inject-as`, `x-upstream-tls`, `x-upstream-plaintext`, `x-credential-probe`, `x-breaker` — the two rewrite fields added 2026-07-20, per Version Migration Strategy §2), is an error; `x-adapter.targetVersion` must name a version that is actually live and coexisting; and the declared transform sequence must statically carry the old schema into the target schema where that is decidable. **This last sub-check is the substantial one** and is the specific reason the "marginal cost is CLI packaging" rationale above needed correcting — it is a schema-composition analysis, sized and scheduled with Phase 8 rather than assumed free here.
  - **The buffered-transform-on-unbufferable-route combination** *(lands with Phase 8, since it is an `x-adapter.response` property)* — a route whose `x-adapter.response` alters body structure (envelope wrap/unwrap, field move) *and* which cannot be buffered (protocol upgrade, oversized or opaque body) is unscrubbable by construction and is flagged specifically, since it is easy to author without noticing (Architecture, pipeline invariant).
  - **`x-breaker` conflicts** *(lands with Phase 11, which introduces the field)* — two fragments resolving to the same upstream origin with disagreeing `x-breaker` blocks fail the PR, and every `x-breaker.enabled: false` is flagged for review like the other opt-outs.
  - **`x-cost-per-call` / `x-quota` coherence** *(lands with Phase 13, which introduces both fields; added to this enumeration 2026-07-20)* — three checks, because each catches a defect that reads as a working budget while capping nothing or capping the wrong amount: **`x-quota.unit` must be byte-identical to the same route's `x-cost-per-call.unit`** (a mismatch is a hard error and a merge-time quarantine, since a mixed pair draws a credit balance down in dollars); **`x-quota` without `x-cost-per-call` on the same route is a hard error** (a budget with no per-call draw can never be decremented, so it caps nothing while reading as though it does); and **`window` must match `^[0-9]+(s|m|h|d)$`, with a warning — not an error — on any `window` exceeding `168h`**, since the window is tumbling and restart-scoped and a budget nominally longer than the deploy cadence is a cap that quietly resets (Data Models). The over-`168h` routes are enumerated at `/config/status` as well, so the condition is visible at runtime and not only at the CI gate.
  - **Fan-out map width against the envelope cap, a warning** *(lands with Phase 10, which introduces `x-upstream-map` and the fan-out envelope)* — when `x-upstream-map` width × a plausible per-instance body size exceeds the fragment's effective `maxFanoutEnvelopeBytes`, lint flags the fragment for review **with the arithmetic shown** (Data Models, "fan-out response envelope"). A warning rather than an error, in the same spirit as the `x-quota` window warning: the truncation behavior is well-defined and safe, so the fragment is not wrong — but a routinely half-empty fleet query is a condition an author should meet at authoring time rather than discover as a `/k8s/_all/api/v1/pods` that quietly stops answering for four of nine clusters.
  - **Reserved control-plane paths** *(lands with 9a)* — as above (Fragment Toolchain rule 3), listed here so the enumeration is complete rather than partly elsewhere.
- [ ] Phase 9b: Fragment authoring convenience — `seam diff` (renders the effective merged-spec change a PR would cause) and `seam import --from-url` (bootstraps a fragment from an upstream's own published OpenAPI spec, filtered to selected paths/verbs). Genuinely deferrable: these make fragment authoring pleasant, whereas 9a makes it safe.
- [ ] Phase 10: Multi-instance routes — `{instance}` path parameter + object-form `x-upstream-map` resolution over the **full entry key set `{url, vaultPath, injectAs, tls, plaintext, probeInterval, breaker}`** (corrected 2026-07-20 — this listed only `vaultPath`/`injectAs`/`probeInterval`, and this was one of the four disagreeing copies Data Models has since consolidated; the phase that *builds* map resolution carried one of the short ones, which is the worst place for it, since an entry key nobody implements is an inherited default nobody notices). Only `url` is required; the fragment-level `x-vault-path`, `x-inject-as`, `x-upstream-tls`, `x-credential-probe` interval and `x-breaker` are the defaults for any key an entry omits — **except `plaintext`, which is per-entry and never inherited**: a map-bearing fragment acknowledges plaintext with **`plaintext: acknowledged` on each `http://` entry**, a fragment-level `x-upstream-plaintext` alongside a map is a lint error rather than a blanket cover, and an `http://` entry without its own acknowledgement is a hard error (Data Models). Every entry `url` is bound by the operator-owned upstream-host allowlist, so a wide map cannot smuggle one attacker-controlled instance in among nine legitimate clusters.

  **Map resolution owns the `{instance}` half of the upstream-path computation** (added 2026-07-20 — this deliverable described selecting an entry and said nothing about what happens to the segment that selected it, leaving an implementer to forward `{cluster}` upstream to a kubectl-proxy that has never heard of it). **The `{instance}` segment is deleted entirely** from the matched path template before any rewrite runs, and is **never re-substituted** — it is routing metadata SEAM consumes to pick an `x-upstream-map` entry and it names nothing on the upstream. That is **step 2 of the four-step rule in Data Models** (`x-upstream`, "how a SEAM-facing path becomes an upstream path"); steps 1, 3 and 4 and the rewrite fields `x-upstream-path-template` / `x-upstream-strip-prefix` ship in **Phase 2**, and what this phase adds is step 2 plus the per-instance evaluation of the whole computation at **pipeline stage 11** — one shared computed path dispatched against N base URLs. Worked: `/k8s/{cluster}/api/v1/pods` with `x-upstream-strip-prefix: /k8s` sends `GET /k8s/ardenone-cluster/api/v1/pods` to that entry's `url` as `/api/v1/pods`.

  Then **`_all` fan-out returning the response envelope specified in Data Models** ("fan-out response envelope" — the `instances` object and the `summary` counts, defined there and not restated here), **with the status code derived, never authored** — the 200 / 207 / structured-503 derivation rule, the `X-SEAM-Fanout-Partial: 1` header, and the boundary that keeps SEAM's own refusals out of 207 are specified in **Multi-Instance Routes and Fan-Out, which owns them**. This phase builds them.

  **Including the envelope's own size rule, without which this plan's flagship fan-out is unbuildable at the default cap** (added to this deliverable list 2026-07-20 — the rule was decided in Data Models and Multi-Instance Routes and assigned to no phase, and it is the difference between `/k8s/_all/api/v1/pods` across nine clusters working and being refused outright): the third independently configurable limit **`maxFanoutEnvelopeBytes`**, defaulting to `min(maxBufferedResponseBytes × total, 64 MiB)`; the **`truncated` instance status** — instances admitted in the order their responses complete, and the first whose body would push the envelope past the cap, plus every instance after it, recorded with its `httpStatus` and its body's `bytes` and **no `body`**; the matching **`truncated` count in the `summary` object**, with a truncated instance still counted in `dispatched` and still billed, since it was dispatched, answered, and charged; and the derivation consequence that any `truncated` entry makes the response a **207** carrying `X-SEAM-Fanout-Partial: 1`. The envelope therefore always fits, always buffers, and is **always scrubbable**, so a wide map never falls into the unscrubbable path and `x-unscrubbable: acknowledged` is never the price of one. **`seam lint`'s map-width warning lands with this phase too** — width × a plausible per-instance body size against the effective cap, flagged with the arithmetic shown (enumerated in Phase 9a's check list, marked as landing here).

  Depends on 1b; **must precede Phase 5.**
- [ ] Phase 11: Passive route health — per-route last-2xx tracking surfaced in `/docs` **in the three-state rendering** (corrected 2026-07-20 — this named the older two-state one, which conflated *no attempt* with *no success* and therefore rendered the fleet's single worst route, called fifty times and failing every one, as "no attempt since last restart", i.e. untested-and-new): **no attempt since restart** → "no attempt since last restart"; **attempted, none successful** → "no success in N attempts since last restart", carrying the **last error** alongside it (free, since the breaker already tracks exactly that value per upstream origin for its structured 503) — this is the state that was previously invisible and the one an operator most needs; **at least one success** → "last succeeded 2m ago", with `source: probe` labeling a success that came from sentinel traffic. The last-success value stays intentionally empty in the first two states rather than carrying a sentinel timestamp. `/health/upstreams` renders the same three states on the same terms. Plus `/health/upstreams` aggregation, and the **per-upstream circuit breaker under the default policy specified in Passive Route Health, which owns it** — the failure definition (transport faults and 5xx only; no 4xx, no 401), the threshold and open duration, the evidence-based half-open rule, the backoff schedule and the derived `Retry-After` are all defined there and deliberately not restated here. This phase builds them, together with the rule that breaker state keys on the **resolved upstream origin** (per `x-upstream-map` entry for a multi-instance route) so one dead cluster opens exactly one breaker. Plus the structured 503 naming the upstream, when it started failing, the last error, and that retry-after — and **`x-breaker`** (Data Models), the fragment-root `{threshold, openSeconds, maxOpenSeconds, enabled}` tuning block with its per-instance `breaker` override, its same-origin conflict resolution (strictest values govern, disagreement named at `/config/status`), and its lint-flagged `enabled: false` opt-out. The breaker is **on by default** — unlike the cost governor and loop breaker, absence of the field is not "no guard". Depends on 1b and 2 (needs real proxied traffic).
- [ ] Phase 12: Credential health sentinel — `x-credential-probe` background validation loop with per-(fragment, instance) reporting at `/health/credentials`, leader-elected via a Kubernetes `Lease`, and 401-triggered refetch-and-retry-once **over the request-body buffer Phase 2 ships**, plus the **`credential-refresh-not-retried` structured error envelope** for the case that buffer cannot cover (both added to this deliverable list 2026-07-20 — the retry was named here while the mechanism it consumes and the error it emits were named in **no** phase at all, which by this plan's own principle made them unbuilt; the buffer and its `maxReplayableRequestBytes` knob are assigned to **Phase 2**, since Phase 13's loop-breaker hash consumes them three ship-order positions before this phase exists). Within the cap the retry is a genuine replay: the buffered bytes are re-sent with headers re-stripped per the inbound-sanitation rule and the freshly-refetched secret re-injected. Above the cap — and on any body SEAM cannot replay at all, a request-side protocol upgrade or an unbounded chunked stream with no declared length — the cache is **still** invalidated and the secret **still** refetched, so the caller's *next* request succeeds, and SEAM answers with that envelope: the condition named, the upstream's own status and scrubbed body carried, and the plain statement that resending will use the refreshed credential. The self-healing guarantee is therefore conditional, and the point of naming the envelope is that the condition is one the caller can actually detect. **Consumes the Lease `Role`/`RoleBinding` created in Phase 6a** — `get`/`create`/`update` on `coordination.k8s.io` `leases` in SEAM's namespace; this phase adds no RBAC of its own, and if that binding is absent the probe loop fails closed (holds no Lease, probes nothing) rather than probing unelected. Depends on Phase 2 and on 6a for that binding. (Secret-echo scrubbing was moved out of this phase to Phase 2 on 2026-07-20 — see Credential Health Sentinel.)
- [ ] Phase 13: Per-route guards — `x-loop-guard` loop breaker, `x-cost-per-call`/`x-quota` cost governor with `X-SEAM-Budget-Remaining`, `X-SEAM-Dry-Run` validation-only mode. **Both governor fields are object-valued and unit-bearing** — **`x-cost-per-call: {amount, unit}`** and **`x-quota: {amount, unit, window}`** (added to this deliverable list 2026-07-20, because an implementer building the scalar form this line previously implied would ship a governor with no unit and no reset period at all). **The full shape, the unit semantics, the tumbling restart-scoped `window` grammar and the four-field `X-SEAM-Budget-Remaining` format are specified once in Data Models, which owns them, and are deliberately not restated here.** What this phase owes is the *implementation* of all of it, plus enforcement of the two cross-field rules — **unit-match** and **`x-quota` requires `x-cost-per-call`** — as merge-time quarantines. **The loop breaker's normalized request hash consumes the request-body tee and `maxReplayableRequestBytes` shipped in Phase 2** (recorded 2026-07-20 — the hash is defined over "the canonicalized body", and canonicalizing a body requires buffering it, so this phase would otherwise have inherited a dependency on a mechanism specified only as part of Phase 12, which ships **three ship-order positions later**): within the cap a JSON body is canonicalized (keys sorted, insignificant whitespace removed, RFC 8785-style) before hashing; above it the body is hashed over its streamed bytes without canonicalization, which is strictly coarser and can only make the guard **under**-match, never over-match (Per-Route Guards). This phase adds no buffer of its own and no new configuration knob. **Ships here without waiting for identity** (decided 2026-07-20): pre-Phase-7 the loop breaker keys on (route, request-hash) and `x-quota` is enforced as a single **route-wide** budget — strictly more conservative than per-caller enforcement, never less (see Per-Route Guards). Nothing in this phase enforces per-caller limits, and `X-SEAM-Budget-Remaining` is a fleet-wide fuel gauge, not a reservation, until Phase 7. Depends on 1b **and on Phase 2** (updated 2026-07-20 — this read "1b" alone, which was true while the request-body buffer belonged to no phase; the loop-breaker hash canonicalizes the request body and consumes Phase 2's tee, so the dependency is real even though the ship order already satisfies it). Phase 7 narrows both keys with no fragment change and no change to what thresholds mean.
- [ ] Phase 14: Non-tailnet (foreign-worker) ingress authentication — **the phase number this plan owed Per-Agent Tool Scoping point 7** (assigned 2026-07-20). That mode is decided in full detail — four non-negotiable security rules — and carries a phasing constraint ("lands as a distinct later phase of its own"), but had **no phase number, no checklist entry and no position in the ship order**, while this plan establishes that phase numbers are stable identifiers referenced from filed beads and states its own principle that "an unassigned deliverable is an unbuilt one". A fully-specified, security-critical auth mode with no phase is exactly that. (Approval-gated routes are also unphased, but are *explicitly acknowledged* as such; point 7 was not.) The deliverables are point 7's four rules, implemented verbatim and none of them per-deployment negotiable:
  - **Cloudflare Access JWT validation at the gateway, on every request** — signature against Cloudflare's published JWKS for the team domain, `aud` matching SEAM's own Access application ID, plus `iss` / `exp` / `nbf`. SEAM never treats "this request arrived on the tunnel listener" as proof of anything: a tunnel is transport, and edge-only verification authenticates by default anyone who reaches the origin by any other path.
  - **The service-token→scopes mapping, SEAM-side and keyed on the verified token subject** (`sub` / `common_name`) — structurally the same move as reading a tailnet caller's scopes from the `WhoIs` Grant. There is no request header, query parameter or body field through which any caller, in this mode or any other, may assert its own scopes.
  - **`X-SEAM-Scopes` stripping — deleted, not merely ignored** — on ingress at pipeline stage 2, by the same strip-then-inject rule that removes `Authorization` and every `x-inject-as` header, so a forged header cannot be read even by a future code path that mistakenly looks for one and cannot survive into an upstream request.
  - **Default-deny on the mode itself** — off unless explicitly enabled, and a request presenting no valid Access JWT is refused *before* route matching, secret lookup, or any upstream contact. The failure mode is a **403**, never "fall through to the tailnet path with no identity resolved".

  **Dependency `7 → 14`, recorded as a forcing edge above.** Phase 14 adds an identity *source* and reuses Phase 7's enforcement model unchanged — stage-5 authorization, scope filtering, `/whoami`, `/scopes` — so there is nothing to bind a verified subject's scopes to before Phase 7 exists. **This is also the one phase that lifts Pre-Phase-7 Trust Boundary's "no Funnel, no public ingress, no Cloudflare Tunnel" rule**, and only in this phase: standing the tunnel up early "just for testing" is the single change that would put an unauthenticated credential-injecting proxy on the open internet, and that section forbids it in every earlier phase for that reason.

## Open Questions

(Resolved questions are folded into their sections above with "decided 2026-07-16" / "decided 2026-07-20" markers. Parked notes, each of which constrains something in this plan: per-caller version pinning in `docs/notes/per-caller-version-pinning.md`; quiet-window options in `docs/notes/quiet-window-options.md` — **resolved 2026-07-20**, the recommended composition is adopted verbatim in Phase 8, so this note is no longer pending review; and **approval-gated routes in `docs/notes/approval-gated-routes.md`** — accepted as a future feature 2026-07-16 but deliberately unphased, and previously invisible to anyone reading only this plan even though it declares dependencies on Phases 2 and 7 and adds a fragment field and an approval-polling endpoint — which is now **`/_seam/approvals/{id}`**, not the bare `/approvals/{id}` the note was written against: it post-dates the closed grandfathered enumeration, so by the forward rule it ships under `/_seam/` (Fragment Toolchain, reserved control-plane namespace). The bare `/approvals/` prefix stays reserved as a squat guard, unserved. Language/runtime choice resolved 2026-07-20 — see ADR-001 below.)

- **Route-fragment schema — narrowed, not open** (narrowed 2026-07-20; bead `bf-2wt`). This is the critical path: Phase 1b cannot merge, validate, or quarantine fragments whose schema does not exist, and once fragments exist across declarative-config a schema change ripples through every onboarded service's ConfigMap — SEAM's own versioning problem, with no adapter layer to hide behind. What remains open is now much smaller than "not yet designed", because the **field inventory and semantics are decided** and recorded in Data Models: `x-upstream` / `x-upstream-map` (object form, mutually exclusive, `{instance}`-gated), **`x-upstream-path-template`** (path-item-level) and **`x-upstream-strip-prefix`** (fragment-root) — the two upstream-path rewrite fields — **`x-seam-owner`** (required in every fragment, checked against the projected **parent directory**, corrected 2026-07-20 from "basename": a service ships N fragment files into one owner directory, so the basename is free-form and the directory is the authoritative link), **`x-upstream-tls`** (`{caBundle, serverName, insecureSkipVerify}`), **`x-upstream-plaintext`**, `x-vault-path` (allowlist + co-ownership rules), `x-inject-as` (object `{kind, name}`), `x-required-scope`, `x-credential-probe`, `x-seam-schema`, `x-api-version`, **`x-breaker`** (`{threshold, openSeconds, maxOpenSeconds, enabled}`, on-by-default infrastructure guard rather than an opt-in one), `x-loop-guard`, `x-cost-per-call` / `x-quota`, `x-seam-deprecated`, `x-adapter`, `x-unscrubbable`; `servers:` is explicitly not used for upstream targets. The remaining work is **mechanical and bounded**: write the concrete JSON Schema for `x-seam-schema: v1` encoding those fields and their cross-field constraints, decide the fragment-root vs. operation-level placement of each **except `x-required-scope`, whose placement and cardinality are decided below**, and settle where unknown `x-*` keys sit (reject, or accept-and-ignore for forward compatibility).

  **`x-required-scope` placement and cardinality are decided, and deliberately not left to whoever writes the JSON Schema** (decided 2026-07-20 — this field's placement sat inside the open list above while **three** rules elsewhere are stated as decided and each *requires* operation-level granularity: Architecture's narrowing of the scope-naming 403 to the visible-but-not-invocable case, worked as "a caller holding `k8s-ro:get` sees the path and is refused `DELETE`"; Per-Agent Tool Scoping point 5's "right path, wrong method"; and the same distinction applied to level-2 `/changes` in §3. A fragment-root-only `x-required-scope` **cannot** express "GET needs `k8s-ro:get`, DELETE needs something else", so the worked example in this plan's own enumeration-oracle rule was inexpressible under one of the two placements still listed as open — and since that rule decides whether SEAM leaks its route inventory, it is not a question a schema author should resolve by picking the simpler shape):
  - **Placement is operation-level, with an optional fragment-root default that any operation overrides.** The root form is ergonomics for the common case where every operation in a fragment needs the same scope; the operation form is the authority wherever both are present, and it **replaces** the default rather than merging with it — so both "this one method needs more" and "this one method needs less" are expressible, and neither is expressible against a default nobody can turn off. A fragment declaring neither at either level is a route with no scope requirement, which stays legal and is what every fragment shipped before Phase 7 is.
  - **Cardinality is a set: an array of scope strings, with a bare string accepted as sugar for a one-element array.** Both consumers need a set and neither has a scalar reading — §2 enforces the requirement as the **union** of the matched fragment's and its `targetVersion`'s, and point 6 derives `/scopes` by inverting these declarations into scope→routes, which needs each route to contribute all of its scopes. The array is a **conjunction**: the caller must hold every listed scope. That is the only reading under which union-with-the-target can never *weaken* a requirement — under a disjunctive reading, adding a scope on the newer version would widen access, which is exactly the privilege-de-escalation path §2's union rule exists to close. Duplicates collapse and order is not significant, so the union is a plain set union at stage 5.

  The **remaining fields' placement stays open exactly as the question above intends.** This closes one field, on the narrow grounds that a security rule already stated as decided depends on the answer — not the general question.

  **The cross-field constraints the schema must encode are themselves now decided**, and are the part most likely to be missed by a schema written field-by-field (recorded 2026-07-20): `x-seam-deprecated` is an **object `{since, sunset?}`** with `since` required — not the boolean it was first specified as, and a bare `true` must fail rather than coerce; `x-api-version` has a **closed grammar** `^v[1-9][0-9]*$` with the literal `_unversioned` rejected as an author-written value; `x-adapter` is **mutually exclusive with `x-upstream` / `x-upstream-map`** and with every other upstream-facing field (**`x-upstream-path-template`**, **`x-upstream-strip-prefix`**, `x-vault-path`, `x-inject-as`, `x-upstream-tls`, `x-upstream-plaintext`, `x-credential-probe`, `x-breaker`), which are taken from the target version's fragment — the two rewrite fields added to this list 2026-07-20, since they are stated in the *upstream's* own path terms and `x-adapter`'s closed vocabulary is explicitly barred from transforming paths at all; **`x-upstream-path-template` must begin with `/`, must name no `{instance}`, and every other `{param}` it names must exist in the matched path template**, while **`x-upstream-strip-prefix` must be a literal (leading `/`, no trailing `/`, no parameters) that is a prefix of *every* path the fragment declares** — a partly-applicable strip silently forwards one fragment's routes to two different path shapes — and where both are declared the path-item-level template wins, which is a resolution rule rather than a conflict; `x-upstream` and `x-upstream-map` are mutually exclusive with each other, and `x-upstream-map` requires an `{instance}` path parameter; `x-upstream-plaintext: acknowledged` is **required** whenever the resolved URL is `http://`; and `insecureSkipVerify`, `x-upstream-plaintext` and `x-unscrubbable` each accept the single value `acknowledged` with **no `false` form**. **Four further cross-field constraints, added 2026-07-20:** `x-quota.unit` must be **byte-identical** to the same route's `x-cost-per-call.unit` (a mixed pair draws a credit balance down in dollars); **`x-quota` requires `x-cost-per-call` on the same route**, since a budget with no per-call draw can never be decremented and so caps nothing while reading as though it does; `x-quota.window` obeys the closed duration grammar **`^[0-9]+(s|m|h|d)$`** with no calendar-aligned form expressible, and a value above `168h` is a **warning, not a schema failure** (the window is tumbling and restart-scoped, so a budget longer than the deploy cadence is a cap that quietly resets — visible rather than forbidden); and **`x-upstream-plaintext` is mutually exclusive with `x-upstream-map`**, where the acknowledgement is instead the **per-entry `plaintext: acknowledged`** on each `http://` entry, with an `http://` entry lacking one being a hard error. Each of these is a constraint no per-field schema catches on its own.

  **One constraint the JSON Schema must *assume* rather than encode, and must not be written as though it did not exist** (recorded 2026-07-20): the **upstream-host allowlist is manifest-level, not fragment-level**. The set of permitted `x-upstream` / `x-upstream-map`-entry hosts lives in SEAM's own Deployment manifest and deliberately **never** in a fragment — a fragment-supplied allowlist is self-certifying, since the author naming the malicious host also names the rule permitting it. So the fragment schema can validate `x-upstream` as a well-formed absolute URL and can reject IP literals, but **it cannot decide allowlist membership**, which is checked by `seam lint` against the manifest and re-checked by the gateway at merge time with fragment-wide quarantine. The schema must therefore not be written as if a syntactically valid `url` were an accepted one. Accordingly, add to the constraints-no-per-field-schema-catches list above: **`x-upstream` / `x-upstream-map` entry `url` host ∈ the operator-owned allowlist** — the peer control to the `x-vault-path` prefix, and the one that bounds *where a secret is sent* rather than *which secret is named*. Two forward constraints on that schema: it must leave room for **`x-requires-approval`** (`docs/notes/approval-gated-routes.md`, accepted but unphased — an accepted feature that constrains the schema must not be discovered after the first fragment ships), and `x-seam-schema` remains the escape hatch if evolution is needed anyway.
- ~~Fan-out (`_all`) partial-failure semantics~~ — **RESOLVED 2026-07-20: 207 Multi-Status with per-instance result entries; fail-closed rejected.** Decided in Multi-Instance Routes and Fan-Out (which also fixes the derived 200 / 207 / 503 status rule), with the response body specified as the "fan-out response envelope" in Data Models and built in Phase 10. Nothing remains open here.

## ADR-001: 2026-07-20 — Ratify Go as the SEAM Gateway Implementation Language

### Context

Phase 1 (gateway core) has been blocked since the repo was scaffolded on 2026-07-15 — as of this writing (2026-07-20) the repo contains only `docs/`, zero implementation code. `docs/research/language-runtime-choice.md` (2026-07-16) benchmarked Go, Rust, TypeScript/Node, and Python against SEAM's seven hard requirements (3.1 fragment merge, genuine 3.1 request validation with structured field-level errors, reverse-proxy with streaming/WebSocket passthrough, OpenBao client, Tailscale WhoIs/tsnet, file-watch hot reload with atomic swap, small-footprint k8s container), with library-version and 3.1-conformance claims checked against actual registries/repos/changelogs rather than recalled from memory, and the Python findings additionally executed live on this host. The plan's Open Questions section has carried "decision pending review" since that research landed, and every phase from 1 onward depends on it. This ADR closes that gap.

### Decision

Adopt **Go** as the SEAM gateway's implementation language, per the research's primary recommendation.

1. **The validator is the product.** SEAM's differentiating feature is schema-validated, structured, field-level 400 responses. pb33f `libopenapi` + `libopenapi-validator` is the only mature, off-the-shelf OpenAPI 3.1 request validator found across all four languages surveyed, and its error model (JSON pointer + spec line/col + `HowToFix`) maps directly onto SEAM's structured-error feature — it is better than what we would hand-design. Every other language means either building this by hand (Rust) or accepting a wiring/bus-factor risk on SEAM's core value proposition (Python, Node).
2. **Tailscale support is first-party and production-grade in Go alone.** `tsnet` (embedded node) and `client/local` (WhoIs against a sidecar) are maintained by Tailscale itself, directly relevant to the pilot phases and to Phase 7's per-agent Grant-based scoping. The Rust equivalent (`tailscale-rs`) is explicitly experimental with no WhoIs API as of this research.
3. **The proxy layer's security posture comes free.** stdlib `httputil.ReverseProxy`'s `Rewrite` hook strips hop-by-hop and client-spoofable `X-Forwarded-*` headers before user code runs, by design — closing the exact secret/header-injection pitfall flagged in `docs/research/secret-injection-gateways.md`, rather than requiring SEAM to reimplement that hardening itself.
4. **The cost is real but bounded and one-time.** Go is a new toolchain in a shop whose CI is otherwise Rust-centric (`rust-verify` on iad-ci), but that cost is paid once at setup, not per-route the way a hand-rolled validator's maintenance would recur.

### Alternatives Considered

- **Rust** (the shop's primary language; `rust-verify` remote CI already exists) — clears six of seven requirements with best-in-class parts (axum/hyper/tower proxy stack maps cleanly onto validate→strip→inject→forward; `jsonschema` 0.48 is the most actively maintained 2020-12 engine surveyed; best footprint at ~10-25MB). The one gap is the product's core feature: no mature off-the-shelf 3.1 request validator exists (the sole candidate is a 45-star crate with no crates.io release). Building one is a scoped, bounded effort (~1-2k LOC, 1-2 weeks, per the research) — a defensible runner-up if "no new language in this shop" is weighted above "don't own the validator," but it puts SEAM's differentiator on our own maintenance budget instead of an actively-maintained upstream's. Kept as the fallback (see Consequences).
- **Python** — every requirement clears, and uniquely among the four candidates was verified by live execution on this host (openapi-core 0.23.1: 0.59ms/request validation, 16ms full spec rebuild; a real WhoIs call over `tailscaled`'s Unix socket). Ruled out on footprint (170-200MB image vs. Go's 15-30MB) and on openapi-core being a single-maintainer dependency with a documented history of breaking 0.x API churn — a bus-factor risk on the validator, the same category of risk this decision is trying to avoid.
- **TypeScript/Node** — Ajv2020 is the most battle-tested 2020-12 schema engine surveyed, but every library wrapping it for OpenAPI made an overstated 3.1 claim under inspection (openapi-backend silently runs draft-07-mode Ajv despite its README's 3.1 checkbox; express-openapi-validator is Express-bound and drags in `multer`). WebSocket proxy support across the viable options is self-described "partial" (first subprotocol only). No footprint advantage over Go to compensate.

### Consequences

- **The LANGUAGE blocker is closed; Phase 1 is not wholly unblocked** (amended 2026-07-20 — this bullet previously read "Phase 1 (gateway core) is unblocked and can start as a Go scaffold", which overclaimed: this ADR resolved the language question and was then read as having resolved Phase 1's blocker generally). Phase 1's original deliverable included fragment merge, validation, and quarantine — none of which can be built against a fragment schema that does not exist, which is why bead `bf-4ks` ("Scaffold Phase 1 gateway core in Go") remained BLOCKED while this ADR asserted the opposite. Phase 1 is therefore split (see Implementation Phases): **Phase 1a — Go scaffold, HTTP server, `/docs` + `/docs/{route}` + `/openapi.json` over a hand-written whole spec, structured validation errors — is genuinely unblocked by this ADR and can start now.** **Phase 1b — fragment merge, collision detection, quarantine — remains gated on the route-fragment schema (Open Question 1, bead `bf-2wt`), which is now the critical path for the project.** `seam lint`/`seam diff`/`seam import` (Phases 9a/9b, Fragment Toolchain) are Go CLI tools by the same extension, and 9a inherits 1b's gate for the same reason.
- SEAM becomes the second language in this shop's container fleet alongside Rust. The existing `cargo`/`cargo-remote` CI-offload path (`~/.local/bin/cargo`, iad-ci `rust-verify`) does not apply; a Go build path is needed once code exists — tracked as a follow-on bead (see beads filed alongside this ADR), not a blocker to starting Phase 1a, since SEAM ships as a plain container per the Hosting section regardless of build tooling.
- The validator boundary is narrow (parse + merge + validate, per the comparison matrix in `docs/research/language-runtime-choice.md`) — if Go proves to be the wrong call in practice, replacing that one component (or, worst case, the whole gateway in Rust per the runner-up analysis) is a bounded rewrite, not a redesign of SEAM's architecture.
